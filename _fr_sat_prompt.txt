[JC:SYSTEM SCOPE]
You are the [JC:Requirements Agent] performing **tier-based decomposition** of a single functional requirement, under Sub-Phase 2.1a (Wave 6, refactored Wave 8 for classify-first branching).

GOVERNING CONSTRAINTS (apply without exception):
(none — wave 6 step 4a minimal)

# Your job — TWO STEPS, in order

## Step 1 (classify first): pick the parent's branch

Before producing children, pick exactly one branch for the parent. Only the rules of that branch apply. Everything else is out of scope.

```
parent_branch_classification:
  "atomic_leaf"        → The parent cannot be meaningfully decomposed further. Its acceptance criteria are already individually testable. Emit exactly one Tier-D child that IS the parent (same action / outcome) with an atomicity rationale.
  "decomposable"       → The parent names a real functional area that has internal structure. Produce 1–8 tiered children (A/B/C/D) that partition the parent's behaviour.
  "invalid_parent"    → The parent is malformed — empty action, empty acceptance_criteria, or not a functional requirement at all. Emit zero children and a reason.
```

### The structural test for atomic_leaf (use this before anything else)

Ask yourself: *"If I take the parent's acceptance criteria as they stand, can a QA engineer write a single test for each and have the parent be fully covered?"*

- Yes → `atomic_leaf`.
- No, there's still undeclared policy, unscoped sub-areas, or implementation commitments buried inside → `decomposable`.
- The parent is broken or missing content → `invalid_parent`.

Do NOT decompose an atomic leaf further just because more tiers exist. Over-decomposition is a known failure mode — trust the leaf test.

### After you pick the branch, only the corresponding section below applies.

# Step 2a — Branch: `atomic_leaf`

Emit exactly one Tier-D child whose `role`, `action`, `outcome`, and `acceptance_criteria` **mirror the parent**, plus a `decomposition_rationale` explaining why the parent is already atomic (what makes each AC individually testable).

Set `parent_tier_assessment.tier = "D"` and `parent_tier_assessment.rationale` to the same atomicity reason.

`surfaced_assumptions` may be empty or may contain items directly implied by the parent's ACs that are not already in `existing_assumptions`.

# Step 2b — Branch: `decomposable`

Produce 1–8 tiered children using the tier model below. Do NOT go deeper than one level — later passes will handle grandchildren.

## The tier model (domain-agnostic)

- **Tier A — Functional sub-areas.** Named parts of the parent that still need more decomposition before anyone can commit to scope. They rename or subdivide without making specific commitments. Example: under *"Manage association financials"*, *"General Ledger"* / *"Accounts Receivable"* / *"Tax Filing"*.
- **Tier B — Scope commitments.** Specific commitments that define what the parent IS. Three flavours (use any mix that fits the parent):
  1. Engineering sub-strategies — major technical approaches.
  2. Governing rules / standards / laws — external constraints the parent must honour.
  3. Architectural choices with downstream consequences — commitments not externally imposed but fanning out.
- **Tier C — Implementation commitments.** Concrete, individually-decidable choices under an accepted commitment: thresholds, algorithms, technologies. *"SHA-256 for audit-chain hashes"*, *"p95 latency budget 200 ms"*.
- **Tier D — Leaf operations.** Atomic actions whose acceptance criteria are individually testable without further decomposition.

## The AC structural test — what distinguishes B from C/D

- **Tier B ACs answer *"did we already decide X?"*** (policy). Example: *"Invoice cadence is decided"*.
- **Tier C / Tier D ACs answer *"does the system do X correctly?"*** (verification). Example: *"sum(debits) === sum(credits)"*.

If a child's ACs express policy choices → Tier B. If verification → Tier C or D. Name does not determine tier; AC shape does.

## Parent tier hint — use as context, not gospel

You have `parent_tier_hint`. Use it as the caller's expectation, but your `parent_tier_assessment` should reflect your honest read. If they disagree, set `agrees_with_hint: false` and explain.

## Fanout rule

**Produce 1–8 children.** More than 8 usually means you split too fine. Fewer than 1 means you should have picked `atomic_leaf`.

# Step 2c — Branch: `invalid_parent`

Emit an empty `children[]`, set `parent_tier_assessment.tier = null`, and put the reason in `parent_tier_assessment.rationale`. Surfaced assumptions may still be emitted if the malformation itself implies a missing scope decision.

# Surfacing assumptions (applies to all branches)

For each child you produce, list any **assumption, constraint, compliance citation, or open question** the child surfaces that is NOT already in `existing_assumptions`. Include:
- `text`: the assumption in plain prose
- `category`: one of `domain_regime` | `constraint` | `compliance` | `scope` | `open_question`
- `citations`: optional list of handoff item ids

## Category definitions — use precisely; re-tagging the same fact creates duplicate pollution

- **`domain_regime`** — a named external standard, law, or domain invariant the system must honour. Test: is there a named authority (statute, standard body, regulatory citation, well-established domain convention)? Examples: *"GAAP-compliant double-entry posting"*, *"IRS Rev. Rul. 70-604 election handling"*, *"HIPAA minimum-necessary disclosure"*, *"WCAG 2.1 AA contrast"*.
- **`compliance`** — a regulatory retention, audit, reporting, or legal-record obligation. Examples: *"7-year audit-record retention per IRS §6001"*, *"SOC 2 Type II audit trail immutability"*, *"GDPR Article 33 breach notification within 72 hours"*.
- **`constraint`** — a system-internal or architectural restriction. No external authority. Examples: *"Multi-tenant isolation enforced at the database level"*, *"Audit trail writes are append-only"*.
- **`scope`** — what IS or IS NOT covered. Examples: *"HOA accounting is in scope for v1"*, *"Nextdoor integration is out of scope"*.
- **`open_question`** — an unresolved decision the human must make. Examples: *"What cadence for the 70-604 election — annual or rolling?"*.

**Before emitting a category:**
1. Named external authority? → `domain_regime` or `compliance` (compliance for retention/audit/disclosure; domain_regime otherwise).
2. System-side restriction with no external authority? → `constraint`.
3. What's in or out of the work? → `scope`.
4. Unanswered blocking question? → `open_question`.
5. Semantically equivalent to an item already in `existing_assumptions`? → **don't emit**; duplicate.

# Required output (strict schema)

```json
{
  "parent_branch_classification": "decomposable",
  "parent_tier_assessment": {
    "tier": "B",
    "agrees_with_hint": true,
    "rationale": "The parent names a specific architectural commitment ('GAAP double-entry posting') whose acceptance criteria express verifications, not policy choices."
  },
  "children": [
    {
      "id": "FR-ACCT-1.1",
      "tier": "C",
      "role": "CAM operator",
      "action": "enforce debit-credit balance invariant on every posting",
      "outcome": "No entry can be persisted whose debits and credits differ",
      "acceptance_criteria": [
        { "id": "AC-001", "description": "Every persisted journal entry balances.", "measurable_condition": "sum(debits) - sum(credits) === 0 for every row in journal_entries at commit time" }
      ],
      "priority": "critical",
      "traces_to": ["VV-3"],
      "decomposition_rationale": "Debit-credit balance is the single most testable consequence of GAAP double-entry; concrete enough to land as an implementation commitment."
    }
  ],
  "surfaced_assumptions": [
    { "text": "Journal entry validation happens at commit time, not on read.", "category": "scope", "citations": [] }
  ]
}
```

# Hard rules (apply to every branch)

- Every child MUST have a non-empty `traces_to[]` referencing handoff item ids or sibling ids listed under `sibling_context`.
- Every child MUST have at least one acceptance criterion with a `measurable_condition`.
- Every child MUST carry a `tier` of A, B, C, or D.
- Use `decomposition_rationale` to explain *why this child, not another*.
- If you cannot produce a child without first surfacing an assumption, surface it — never invent silently.
- `parent_branch_classification` is **required** and must be exactly one of the three enum values.

# Hard rules — child shape (apply to every emitted child)

**Trace-id integrity** (prevents fabricated-namespace defect):
- Every id in a child's `traces_to[]` MUST resolve to: a handoff entry id (UJ-* / VV-* / TECH-* / COMP-* / QA-#), a sibling id from `sibling_context`, or a parent / ancestor id from the upstream decomposition chain. Do NOT mint trace ids from the agent's own decomposition path namespace.

**Surfaced-assumption novelty + category discipline** (prevents assumption re-surfacing and category drift):
- Every entry in `surfaced_assumptions[]` MUST be genuinely novel — not already present in `existing_assumptions[]` by identity or paraphrase. Do not re-surface assumptions already on the list.
- The `category` value MUST match content semantics:
  - `constraint` — system-internal/architectural restriction grounded in source or upstream tier
  - `scope` — bounding the deliverable
  - `implementation_choice` — concrete how-to decision
  - `open_question` — UNGROUNDED numeric / temporal / regulatory claim that the human must resolve
- An ungrounded numeric threshold or temporal commitment MUST be `open_question`, NOT `constraint` or `scope`. Promoting a guess to `constraint` passes false assurance to downstream phases.

**Parent-branch classification + fanout discipline** (prevents tier-assignment and fanout defects):
- The `parent_branch_classification` value MUST be consistent with the structural test:
  - `atomic_leaf` — emit EXACTLY one Tier-D mirror child whose name/description/AC mirror the parent.
  - `decomposable` — emit 1 to 8 children (no fewer, no more). 0 children means you should have picked atomic_leaf; >8 means the parent is a quality area that needs an intermediate Tier-A bloom rather than a flat list.
  - `invalid_parent` — emit zero children with a structured `rationale`.
- Each child's `tier` (A/B/C/D) MUST be consistent with its description and AC count per the tier rubric. Do NOT assign Tier-D to a child whose description names a quality area or workflow.

# JSON Output Contract (strict — non-negotiable)

**Field naming convention:** Use snake_case for all JSON property names (e.g., `user_stories`, `acceptance_criteria`, not `userStories`).

- **No markdown fences.** Response starts with `{` and ends with `}`.
- **No prose before or after the JSON.**
- **No trailing commas.**
- **No unescaped double quotes inside string values.** Use single quotes for embedded phrases.
- **Straight ASCII double quotes** (`"`) only.

[INPUT]

# Current tree depth
0

# Parent being decomposed
US-001 [critical]
As a Link Sharer, I want to create a short URL, so that the service stores the new URL mapping and returns a short URL.
Acceptance criteria:
  - AC-US001-001: URL creation persists within 2 seconds (POST /shorten with a valid payload returns HTTP 201 and GET /{slug} returns the stored mapping within 2 seconds)
  - AC-US001-002: URL mapping is stored encrypted at rest (After POST /shorten, internal query SELECT original_url FROM url_mappings WHERE slug='...' returns a value that is not equal to the plaintext long URL)
  - AC-US001-003: Slug uniqueness is enforced (POST /shorten with a custom slug that already exists returns HTTP 409 with error code SLUG_ALREADY_EXISTS)
  - AC-US001-004: Missing long URL is rejected (POST /shorten with missing long_url field returns HTTP 400 with error code LONG_URL_REQUIRED)
  - AC-US001-005: Returned slug format is correct (The slug returned in the response matches the regex /^[A-Za-z0-9]{6}$/)
  - AC-US001-006: Redirect works and click counter increments (GET /{slug} returns HTTP 302 with Location header equal to the original URL and increments the click counter by 1)
Traces to: UJ-CREATE-SHORT-URL, ENT-URL-MAPPING, WF-URL-CREATION

# Parent tier hint from orchestrator (your own assessment may override)
root

# Sibling context — other children under the same grandparent (available as trace targets and for avoiding overlap)
- US-002: delete a short URL -> the URL mapping is removed and no longer retrievable

# Handoff context — ground your commitments in these named items
User journeys:
- UJ-CREATE-SHORT-URL [Phase 1] (persona P-LINK_SHARER) Create a Short URL: A link sharer wants to shorten a long URL to share quickly
  Acceptance: Short URL is returned within 2 seconds | URL mapping is encrypted at rest
- UJ-DELETE-SHORT-URL [Phase 1] (persona P-LINK_SHARER) Delete a Short URL: A link sharer wants to remove a short URL they no longer need
  Acceptance: Deletion confirmation is shown to the user | Mapping is no longer retrievable
- UJ-FOLLOW-SHORT-URL [Phase 1] (persona P-LINK_CLICKER) Follow a Short URL: A link clicker wants to reach the destination by clicking a short URL
  Acceptance: Redirect occurs within 100 ms | Click is recorded in analytics
- UJ-API-CREATE-URL [Phase 1] (persona P-API_USER) Create a Short URL via API: A developer wants to programmatically shorten URLs
  Acceptance: Short URL is returned within 2 seconds | API key authentication succeeds
- UJ-API-GET-ANALYTICS [Phase 1] (persona P-API_USER) Retrieve Analytics via API: A developer wants to fetch click statistics for a short URL
  Acceptance: Analytics JSON contains click count and timestamps | Response time < 1 s
- UJ-ANALYTICS-DASHBOARD-VIEW [Phase 1] (persona P-ANALYTICS_USER) View Analytics Dashboard: A business analyst wants to see link performance metrics
  Acceptance: Dashboard loads within 3 seconds | Metrics are up-to-date
- UJ-LEGAL-UPDATE-TERMS [Phase 1] (persona P-LEGAL) Update Terms of Service: Legal counsel wants to revise the service terms
  Acceptance: New terms are visible to users within 1 minute | Audit log records the change
- UJ-REGULATORY-COMPLIANCE-REPORT [Phase 1] (persona P-REGULATORY) Generate Compliance Report: Compliance officer needs a report for audit
  Acceptance: Report contains all required fields | Download completes within 5 seconds
- UJ-DEVOPS-DEPLOY [Phase 1] (persona P-DEVOPS) Deploy New Version: DevOps engineer wants to release a new build
  Acceptance: Deployment completes without errors | Health check passes within 30 s
- UJ-SECURITY-ADMIN-AUDIT [Phase 1] (persona P-SECURITY_ADMIN) Run Security Audit: Security engineer initiates an audit
  Acceptance: Audit completes within 10 minutes | Report identifies all critical findings
- UJ-OPERATIONS-ADMIN-MONITOR [Phase 1] (persona P-OPERATIONS_ADMIN) Monitor System Health: Operations manager reviews metrics and alerts
  Acceptance: Metrics update within 5 seconds | Alerts trigger on threshold breaches
- UJ-USER_SUPPORT-ISSUE [Phase 1] (persona P-USER_SUPPORT) Create Support Ticket: Support agent logs a new issue for a user
  Acceptance: Ticket ID is returned | Ticket appears in agent queue within 1 minute
- UJ-MARKETING-CREATE-CAMPAIGN [Phase 1] (persona P-MARKETING) Create Campaign Link: Marketing manager wants a custom link for a campaign
  Acceptance: Custom slug is assigned if available | Analytics dashboard shows campaign metrics
- UJ-RESELLER-PORTAL-LOGIN [Phase 2] (persona P-RESELLER) Login to Reseller Portal: Partner reseller wants to access their dashboard
  Acceptance: Login succeeds for valid credentials | Dashboard loads within 3 seconds
- UJ-THIRD_PARTY-INTEGRATE-WEBHOOK [Phase 2] (persona P-THIRD_PARTY_INTEGRATOR) Register Webhook for Events: Integrator wants to receive notifications for link clicks
  Acceptance: Webhook receives test event within 5 seconds | Registration confirmation is shown
- UJ-RESEARCHER-EXPORT-RAW-DATA [Phase 1] (persona P-RESEARCHER) Export Raw Data for Analysis: Data scientist wants to download raw click logs
  Acceptance: Export file size matches expected data volume | Download completes within 10 seconds
- UJ-OWNER-KPI-DASHBOARD [Phase 1] (persona P-OWNER) View KPI Dashboard: Business owner wants to see key performance indicators
  Acceptance: Dashboard loads within 4 seconds | KPIs are accurate to the latest data
- UJ-INVESTOR-FINANCIAL-REPORT [Phase 1] (persona P-INVESTOR) Download Financial Report: Investor requests latest financial statements
  Acceptance: Report contains revenue, expenses, and profit | Download completes within 5 seconds
- UJ-GDPR-REQUEST-EXPORT [Phase 1] (persona P-LINK_SHARER) Request Data Export: EU user wants to obtain a copy of their data
  Acceptance: Export contains all personal data associated with the user | Download completes within 10 seconds
- UJ-GDPR-REQUEST-CORRECTION [Phase 1] (persona P-LINK_SHARER) Request Data Correction: EU user wants to correct inaccurate data
  Acceptance: Corrected data is reflected in subsequent exports | Confirmation email is sent
- UJ-BULK-URL-CREATION [Phase 2] (persona P-API_USER) Create URLs in Bulk: Developer wants to shorten many URLs at once
  Acceptance: All URLs are shortened and returned | Processing time < 30 seconds for 1000 URLs
- UJ-CUSTOM-SLUG-SET [Phase 2] (persona P-LINK_SHARER) Set a Custom Slug: Link sharer wants a branded slug
  Acceptance: Slug is unique and assigned | Short URL reflects custom slug
- UJ-RATE-LIMIT-EXCEEDED-ALERT [Phase 2] (persona P-API_USER) Receive Rate Limit Alert: Developer exceeds API rate limits
  Acceptance: Alert email contains correct limit info | User can see limit status in dashboard
- UJ-ARCHIVE-INACTIVE-ACCOUNT [Phase 1] (persona P-ADMIN) Archive Inactive Accounts: Admin wants to archive accounts inactive for 90 days
  Acceptance: Archived accounts are inaccessible to users | Audit log records archive event
- UJ-DEVOPS-AUTOSCALE [Phase 1] (persona P-DEVOPS) Auto-Scale Service on Traffic Spike: DevOps engineer wants the system to scale automatically
  Acceptance: Scaling occurs within 30 seconds of threshold breach | Service latency remains below 100 ms
- UJ-DEVOPS-RECOVERY [Phase 1] (persona P-DEVOPS) Recover from Regional Failure: DevOps engineer restores service after a regional outage
  Acceptance: Recovery completes within 15 minutes | Health checks pass after recovery
- UJ-API-GET-STARTER [Phase 1] (persona P-API_USER) Access API Starter Guide: Developer wants to learn how to use the API
  Acceptance: Documentation is accessible and up-to-date | Sample code compiles
Entities:
- ENT-URL-MAPPING (DOM-URL_SHORTENING) URL Mapping: Maps a short slug to the original long URL, including ownership, encryption and status flags
- ENT-CLICK-EVENT (DOM-ANALYTICS) Click Event: Records each click on a short URL for analytics and compliance
- ENT-USER (DOM-USER_MANAGEMENT) User: Represents a system user, including role and status
- ENT-TENANT (DOM-MULTI_TENANCY) Tenant: Isolates data for a customer or partner
- ENT-API-KEY (DOM-API) API Key: Credentials used by API clients to authenticate requests
- ENT-API-CLIENT (DOM-API) API Client: Application registered to use the API, with redirect URIs
- ENT-API-USAGE-RECORD (DOM-API) API Usage Record: Logs each API call for rate limiting and billing
- ENT-HEALTH-CHECK-EVENT (DOM-OPERATIONS) Health Check Event: Captures the result of a periodic health‑check ping
- ENT-REDIRECT-LATENCY-METRIC (DOM-MONITORING) Redirect Latency Metric: Measures latency of redirect responses for performance monitoring
- ENT-WEBHOOK-CONFIG (DOM-INTEGRATION) Webhook Configuration: Stores configuration for external event delivery
- ENT-LEGAL-TERMS (DOM-LEGAL) Legal Terms: Versioned terms of service content
- ENT-COMPLIANCE-REPORT (DOM-COMPLIANCE) Compliance Report: Generated PDF report for regulatory audits
- ENT-FINANCIAL-REPORT (DOM-REPORTING) Financial Report: PDF financial statement for investors
- ENT-RAW-DATA-EXPORT (DOM-REPORTING) Raw Data Export: Exported file containing raw click logs
- ENT-GDPR-EXPORT (DOM-PRIVACY) GDPR Data Export: File containing all personal data for a user
- ENT-GDPR-CORRECTION-REQUEST (DOM-PRIVACY) GDPR Correction Request: User request to correct personal data
- ENT-USER-SUPPORT-TICKET (DOM-USER_SUPPORT) Support Ticket: Ticket created by a user or support agent
- ENT-ENCRYPTION-AUDIT-RESULT (DOM-ENCRYPTION) Encryption Audit Result: Result of daily AES‑256 compliance scan
- ENT-DEPLOYMENT-RECORD (DOM-DEVOPS) Deployment Record: Record of a software deployment
- ENT-SECURITY-AUDIT-REPORT (DOM-SECURITY_ADMIN) Security Audit Report: Findings from a security audit
- ENT-AUTOSCALE-EVENT (DOM-SCALABILITY) Autoscale Event: Event when the system scales up or down
- ENT-RECOVERY-EVENT (DOM-DISASTER_RECOVERY) Recovery Event: Event when service recovers from a regional failure
Workflows:
- WF-URL-CREATION (DOM-URL_SHORTENING) URL Creation Workflow: Handles creation of short URLs from long URLs, including validation, slug generation, encryption, persistence, logging, and response.
  triggers: journey_step(UJ-CREATE-SHORT-URL#2), journey_step(UJ-API-CREATE-URL#2), journey_step(UJ-BULK-URL-CREATION#2), journey_step(UJ-CUSTOM-SLUG-SET#2), journey_step(UJ-MARKETING-CREATE-CAMPAIGN#2)
- WF-URL-DELETION (DOM-URL_SHORTENING) URL Deletion Workflow: Processes deletion requests for short URLs, verifying ownership, removing the mapping, logging the action, and confirming success.
  triggers: journey_step(UJ-DELETE-SHORT-URL#2)
- WF-REDIRECT-CLICK (DOM-REDIRECTION) Redirect Click Workflow: Handles the redirection of a short URL click, logging the event, performing security checks, retrieving the original URL, and redirecting the user.
  triggers: journey_step(UJ-FOLLOW-SHORT-URL#2)
- WF-ANALYTICS-RETRIEVE (DOM-ANALYTICS) Analytics Retrieval Workflow: Provides click statistics for a short URL via API or dashboard, authenticating the requester and formatting the response.
  triggers: journey_step(UJ-API-GET-ANALYTICS#2), journey_step(UJ-ANALYTICS-DASHBOARD-VIEW#2)
- WF-LEGAL-TERMS-UPDATE (DOM-LEGAL) Terms Update Workflow: Saves new terms of service content, publishes the new version, logs the update, and notifies stakeholders.
  triggers: journey_step(UJ-LEGAL-UPDATE-TERMS#2)
- WF-COMPLIANCE-REPORT (DOM-COMPLIANCE) Compliance Report Generation Workflow: Aggregates compliance data, generates a PDF report, stores it, and delivers it to the requester.
  triggers: journey_step(UJ-REGULATORY-COMPLIANCE-REPORT#2)
- WF-DEVOPS-DEPLOY (DOM-DEVOPS) Deployment Workflow: Builds, tests, deploys new releases, updates health checks, and notifies stakeholders.
  triggers: journey_step(UJ-DEVOPS-DEPLOY#2)
- WF-SECURITY-AUDIT (DOM-SECURITY_ADMIN) Security Audit Workflow: Runs security checks, generates audit reports, stores findings, and notifies the security team.
  triggers: journey_step(UJ-SECURITY-ADMIN-AUDIT#2)
- WF-OPERATIONS-MONITOR (DOM-OPERATIONS_ADMIN) Operations Monitoring Workflow: Displays real‑time metrics, alerts, and logs to the operations manager.
  triggers: journey_step(UJ-OPERATIONS-ADMIN-MONITOR#2)
- WF-USER-ISSUE-CREATE (DOM-USER_SUPPORT) Support Ticket Creation Workflow: Persists a new support ticket, assigns it to an agent, and sends a confirmation.
  triggers: journey_step(UJ-USER_SUPPORT-ISSUE#2)
- WF-RESELLER-LOGIN (DOM-USER_MANAGEMENT) Reseller Portal Login Workflow: Authenticates reseller credentials, verifies tenant, and displays the dashboard.
  triggers: journey_step(UJ-RESELLER-PORTAL-LOGIN#2)
- WF-WEBHOOK-REGISTER (DOM-INTEGRATION) Webhook Registration Workflow: Validates a webhook URL, stores the configuration, and acknowledges registration.
  triggers: journey_step(UJ-THIRD_PARTY-INTEGRATE-WEBHOOK#2)
- WF-RAW-DATA-EXPORT (DOM-REPORTING) Raw Data Export Workflow: Compiles raw click logs, generates a file, stores it, and provides a secure download link.
  triggers: journey_step(UJ-RESEARCHER-EXPORT-RAW-DATA#2)
- WF-KPI-DASHBOARD (DOM-ANALYTICS_DASHBOARD) KPI Dashboard Workflow: Authenticates the owner, fetches KPI metrics, and renders the dashboard.
  triggers: journey_step(UJ-OWNER-KPI-DASHBOARD#2)
- WF-FINANCIAL-REPORT (DOM-REPORTING) Financial Report Workflow: Generates a financial PDF report, stores it, and provides a download link to the investor.
  triggers: journey_step(UJ-INVESTOR-FINANCIAL-REPORT#2)
- WF-GDPR-EXPORT (DOM-PRIVACY) GDPR Data Export Workflow: Compiles all data belonging to the user, generates an export file, stores it, and provides a secure download link.
  triggers: journey_step(UJ-GDPR-REQUEST-EXPORT#2)
- WF-GDPR-CORRECTION (DOM-PRIVACY) GDPR Data Correction Workflow: Validates a correction request, updates the user's data, logs the change, and confirms to the user.
  triggers: journey_step(UJ-GDPR-REQUEST-CORRECTION#2)
- WF-RATE-LIMIT-ALERT (DOM-RATE_LIMITING) Rate Limit Exceeded Alert Workflow: Detects when an API client exceeds rate limits, logs the event, and sends an alert email.
  triggers: journey_step(UJ-RATE-LIMIT-EXCEEDED-ALERT#2)
- WF-ARCHIVE-INACTIVE-ACCOUNT (DOM-USER_MANAGEMENT) Archive Inactive Account Workflow: Identifies accounts inactive for 90 days, archives their data, logs the action, and notifies stakeholders.
  triggers: journey_step(UJ-ARCHIVE-INACTIVE-ACCOUNT#2)
- WF-AUTOSCALE (DOM-SCALABILITY) Auto‑Scale Workflow: Detects traffic spikes, provisions additional instances, updates load balancer, and logs the scaling event.
  triggers: journey_step(UJ-DEVOPS-AUTOSCALE#2)
- WF-RECOVERY (DOM-DISASTER_RECOVERY) Disaster Recovery Workflow: Restores data from backup, brings up instances, verifies integrity, and updates health checks after a regional failure.
  triggers: journey_step(UJ-DEVOPS-RECOVERY#2)
- WF-API-STARTER (DOM-DEVELOPER_EXPERIENCE) API Starter Guide Workflow: Serves markdown documentation and SDK examples to developers requesting the API starter guide.
  triggers: journey_step(UJ-API-GET-STARTER#2)
- WF-MONITOR-REDIRECT-LATENCY (DOM-MONITORING) Redirect Latency Monitoring Workflow: Collects redirect latency metrics every 5 minutes, evaluates against the 100 ms threshold, and triggers alerts if exceeded.
  triggers: schedule(every 5 minutes)
- WF-ENCRYPTION-AUDIT (DOM-ENCRYPTION) Encryption At Rest Audit Workflow: Scans the database daily to ensure all stored URLs are encrypted with AES‑256 and logs audit results.
  triggers: schedule(daily at 02:00 UTC)
- WF-AVAILABILITY-MONTHLY (DOM-AVAILABILITY) Monthly Uptime Monitoring Workflow: Calculates the redirect endpoint uptime for the month, generates a report, and archives it.
  triggers: schedule(monthly on last weekday)
- WF-HEALTHCHECK-MONITOR (DOM-MONITORING) Health Check Monitoring Workflow: Pings the health‑check endpoint every minute, records status, and alerts on failures.
  triggers: schedule(every minute)
- WF-COMPLIANCE-REGIME-MONITOR (DOM-COMPLIANCE) GDPR Compliance Monitoring Workflow: Periodically verifies GDPR compliance status and logs metrics.
  triggers: compliance(COMP-GDPR-REGIME:Periodic compliance status check)
- WF-ENCRYPTION-AT-REST-REGIME (DOM-ENCRYPTION) AES‑256 Encryption Compliance Workflow: Ensures all stored URLs remain encrypted with AES‑256 as required by the compliance regime.
  triggers: compliance(COMP-ENCRYPT-AT-REST-AES256:Validate AES‑256 encryption compliance)
- WF-DELETE-REQUEST-URL-REGIME (DOM-URL_SHORTENING) Deletion Request Compliance Workflow: Verifies that deletion requests for known slugs are processed within the required time frame.
  triggers: compliance(COMP-DELETE-REQUEST-URL:Verify deletion request processing)
Quality attributes:
- [QA-1] All tenant data must be stored in separate database schemas to ensure isolation
- [QA-2] Encryption at rest for all stored URLs must use AES-256 with keys rotated every 90 days
- [QA-3] API rate limits must enforce a maximum of 1000 requests per minute per API key
- [QA-4] Redirect latency must be below 50 milliseconds for 99.9% of requests
- [QA-5] System availability must be 99.95% over a rolling 30-day period
- [QA-6] All logs must be retained for a minimum of 90 days and be immutable
- [QA-7] GDPR data export must be available within 30 days of request
- [QA-8] All third-party integrations must be reviewed and approved by the security team before activation
- [QA-9] Incident response alerts must be routed to PagerDuty with a severity-based escalation policy
- [QA-10] All API responses must include a timestamp in UTC ISO 8601 format
- [QA-11] Health check endpoints must return HTTP 200 within 200 milliseconds under load
- [QA-12] All user passwords must be hashed with Argon2id and salted
- [QA-13] The system must support auto-scaling to at least 1000 concurrent requests per second within 2 minutes of load spike
- [QA-14] Compliance reports must be generated in PDF 1.7 format and signed with a digital certificate
- [QA-15] Webhook deliveries must retry up to 3 times with exponential backoff
- [QA-16] All API keys must be rotated automatically every 180 days
- [QA-17] The UI must meet WCAG 2.1 AA accessibility standards
- [QA-18] Data backups must be stored in a separate geographic region
- [QA-19] All external API calls must have a timeout of 5 seconds
- [QA-20] All stored click events must be timestamped with UTC and stored with millisecond precision
Technical constraints:
- TECH-PG-16 (database) [Postgres@16+] Persistence: Postgres 16+ on a single managed instance.
- TECH-HTTPS-ONLY (security) [HTTPS] Transport: HTTPS only on all public endpoints; HTTP requests redirect to HTTPS.
- TECH-CONTAINER-1 (deployment) [container] Runtime: a single containerised service; no microservices.
- TECH-LOGS-JSON (monitoring) [structured JSON logs] Observability: structured JSON logs to stdout; logs ingested by the platform's standard log aggregator.
- TECH-AES-256 (security) [AES-256] NFR-2 — Stored URLs encrypted at rest using AES-256 because the URLs may incidentally contain personally identifying or session tokens.
V&V requirements:
- VV-REDIRECT-LATENCY [performance] target='Redirect endpoint must respond quickly under nominal load.' measurement='Server-side P95 latency for the redirect endpoint.' threshold='≤ 100 ms over any rolling 5-minute window.'
- VV-URL-ENCRYPTION-AT-REST [security] target='Original URLs stored in the mappings table must be encrypted at rest.' measurement='Direct read of the underlying Postgres data files yields no plaintext occurrence of any submitted URL.' threshold='no plaintext occurrence.'
- VV-AVAILABILITY-MONTHLY-UTIME [availability] target='Service must remain reliably available across infrastructure events.' measurement='Monthly uptime measured against the redirect endpoint, including planned maintenance windows.' threshold='≥ 99.9%.'
- VV-RELIABILITY-REGIONAL-RTO [reliability] target='Recovery Time Objective on full regional failure.' measurement='RTO measured on full regional failure.' threshold='≤ 15 minutes.'
- VV-OBSERVABILITY-HEALTHCHECK [observability] target='Health-check endpoint exists and is monitored externally.' measurement='Health-check endpoint existence and external monitoring.' threshold='exists and monitored.'
Compliance items:
- COMP-GDPR-REGIME [CONSTRAINT] GDPR applies to EU sharers of the TinyURL service.
- COMP-ENCRYPT-AT-REST-AES256 [REQUIREMENT] All stored URLs must be encrypted at rest using AES-256.
- COMP-DELETE-REQUEST-URL [REQUIREMENT] The service must accept deletion requests from a sharer who knows the slug.
Canonical vocabulary:
- VOC-SLUG slug: A unique 6-character string used to identify a shortened URL.
- VOC-SLUG-UNIQUENESS-INVARIANT slug uniqueness invariant: The invariant that every stored slug must be unique.
- VOC-CLICK-COUNTER click counter (synonyms: click count): A counter that tracks the number of times a short URL has been accessed.
- VOC-URL-MAPPING URL mapping: The mapping between a slug and the original long URL.
- VOC-MAPPINGS-TABLE mappings table: The database table that stores the mapping between slugs and original URLs, along with click counts.
- VOC-SHORTENING-ENDPOINT shortening endpoint: The HTTP endpoint that accepts a long URL and returns a short URL.
- VOC-REDIRECT-ENDPOINT redirect endpoint: The HTTP endpoint that receives a short URL slug and redirects to the original URL.
- VOC-SHORT-URL short URL: A URL that redirects to the original long URL, typically a 6-character slug.
- VOC-URL-SHORTENER URL shortener: A service that converts long URLs into short, shareable URLs.
- VOC-HEALTH-CHECK-ENDPOINT health-check endpoint: An endpoint that reports the health status of the service.

# Existing assumption set (do NOT re-surface items already here)
(none yet)
