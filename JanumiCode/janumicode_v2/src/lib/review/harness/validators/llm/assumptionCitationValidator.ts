/**
 * LLM validator: assumption_citation_validator. Factory-built.
 */
import { makeLLMValidator } from './llmValidatorRunner';

export const invokeAssumptionCitationValidator = makeLLMValidator({
  validatorId: 'assumption_citation_validator',
});
