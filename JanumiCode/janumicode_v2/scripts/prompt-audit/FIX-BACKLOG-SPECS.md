# FIX-BACKLOG — Implementation-Ready Fix Specs (open items)

Generated by the design Workflow (wf_b56d5de0) 2026-07-02 — 10 design agents read the actual code + live data. Companion to FIX-BACKLOG.md (statuses) and HARNESS-REVIVAL-PLAN.md. Verify line numbers before editing (they drift).

## Execution plan

**Order:** PA-9 (do the template-only half first) — id-namespace unification: align the injected catalog ids to the exact canonical ids the schema/example demands (TECH-BUN-1->TECH-BUN, ent-*->DM-*, and add ENT-/WF-/VOC- to the FR traces_to roster at fr template L158). Pure prompt-template + injected-id alignment, S/low-risk, and the single biggest unblocker (gates PA-7/PA-10/PA-5/PA-15 joins). Split off the leaf/root US/AC portion to co-land with PA-3. → PA-14 — compliance_context deterministic bridge in phase8.ts:302: replace the hardcoded 'No compliance regimes' with a read of the Phase-1.0d compliance_retention_discovery artifact (drop the dangling rule when genuinely empty). Isolated to one phase, S, deterministic producer bridge, no deps. → PA-6 — component_saturation domain-context scoping in phase4_2a.ts: inject the parent's own software domain + a thin sibling-domain index instead of all 18 domains + 104 SRs. S, low-risk, no deps, uses the established per-target scoping + full-summary-fallback pattern. → PA-11 (finish) — close the remaining data_model_saturation branch literal (atomic_value menu vs atomic_leaf fanout) + the nfr skeleton-without-thresholds mismatch; NFR category enum is already canonical in the template. Small, unblocks PA-12; guard with the existing file-reading enum-consistency test pattern. → PA-12 — strip the ```json fence from the in-prompt output example in fr_saturation (L117) and task_skeleton templates so it stops contradicting the 'No markdown fences' rule; confirm the normalizer still strips fences. Template-only, land alongside PA-11 to avoid double-churning the fr template. → PA-10 — inject each node's actual inherited active_constraints array into the parent_task/parent_entity blocks (phase6_1a.ts, phase6.ts component narrowing, phase5). Deterministic bridge, S; do right after PA-9 so the surfaced TECH-* ids join the registry. → PA-7 — parent-slot keying: current code already single-sources the target id (PA-1/PA-2 propagated component_id), so this is a verify-and-lock item — add the unit/harness assert 'queued node id == injected parent block id == meta.label id' now that PA-9 has unified the id forms. → PA-5 — fr/nfr_saturation handoff-corpus compaction: compact each handoff item to id + one-line label and dedup repeated decision_trace rows (drop the multi-line Acceptance/description bodies that are 62% of the prompt). Deterministic, no item loss; S but medium-risk (grounding-adjacent) so validate live. Optionally follow with the byte-identical-block hoist (provider cache) as a separate L item. → PA-3 — task_skeleton AC-inventory + tech-specs per-component scoping: inject only THIS component's candidate leaf AC-* ids via the orchestrator-owned component->leaf-AC map (tree-walk lineage.canonicalize for root->leaf binding, NEVER regex) + a compact id-only index for the rest; scope technical_specs_summary to the component's SR/data-model/API slice. Co-land PA-9's leaf/root US/AC unification here. Coverage-critical (a wrong binding drops ACs), M. → PA-13 — duplicate catalog injection: at root collapse sibling_context==depth-0 into one block and inject governing constraints once (in-template/top-slot vs DMR-file), reference thereafter. M/medium-risk; batch with PA-10 since both edit the phase6_1a parent-block/catalog renderers (overlaps the done PA-1 territory). → PA-15 — monolithic test_case_skeleton (P7.1) -> per-component chunking + orchestrator-owned AC-coverage reconciliation loop, mirroring the P6.1 pattern. L, coverage-critical (fixes ~28% single-pass AC coverage); must follow PA-3 (reuse its per-component chunking + component->leaf-AC map) and PA-9 (unified AC id namespace for the coverage loop).

**Quick wins (S/low-risk):** PA-9 (template-alignment half) — S, low-risk, prompt-template + injected-id alignment; highest unblocking value.; PA-14 — S, low-risk deterministic bridge confined to phase8.ts (read the Phase-1.0d compliance artifact).; PA-6 — S, low-risk domain-context scoping in phase4_2a.ts, no deps.; PA-11 finish + PA-12 — S template-only pair (data_model branch literal + nfr thresholds, then strip the JSON fence); land together.; PA-10 — S deterministic bridge (surface node active_constraints), immediately after PA-9.; PA-7 — S verify-and-lock: add the 'queued id == parent block id == label id' assertion (mechanism already single-sourced).

**Coverage-critical (correctness, not bloat):** PA-9 — broken id joins mean emitted active_constraints/entity refs don't resolve against the registry (orphaned/dropped), not just bloat; also unblocks the other coverage fixes.; PA-3 — root->leaf AC lineage binding: a wrong binding silently drops acceptance criteria from a component's task skeleton (789/804 unused today masks real mis-selection).; PA-15 — monolithic test_case_skeleton yields ~28% AC coverage per pass; chunking + coverage-reconciliation loop is the difference between covered and dropped test coverage.; PA-10 — without the node's inherited active_constraints the model reconstructs narrowing from instructional examples -> wrong/omitted TECH-* constraints on children (correctness, not economy).; PA-14 — hardcoded-empty compliance_context drops the real compliance signal into Phase-8 evaluation design (correctness, though small blast radius).

**Already-handled / refuted:** PA-7 — status already-handled: the label/parent-block are materialized from the same record and current code single-sources the target id (PA-1 anchored the parent block, PA-2 propagated suite/component_id into root component_ids). Remaining work is only a regression assertion (queued node id == injected parent block id == meta.label id), best added after PA-9 unifies id forms. det_bridge=false.; PA-11 (NFR category slice) — the nfr_saturation template already renders the canonical 5-value AssumptionCategory enum (domain_regime|constraint|compliance|scope|open_question) at template L96/L156 and uses atomic_leaf consistently; only the data_model_saturation branch literal and the nfr skeleton-without-thresholds mismatch remain open under PA-11.; PA-1/PA-2/PA-4/PA-8 — DONE (scoping + serializer fixes landed and, for PA-8/PA-11 fr-nfr, live-validated); they establish the *ById-scoping + full-summary-fallback and summaryFormat serializer patterns the open items should follow.

**Dependency notes:** "PA-9<->PA-3 is a genuine two-part entanglement, not a deadlock: PA-9's TECH-BUN-1->TECH-BUN / ent-*->DM- / FR traces_to-roster alignment is pure-template with no PA-3 dependency and must lead; PA-9's leaf-vs-root US/AC unification shares the lineage.canonicalize tree-walk that PA-3 needs, so co-land that half inside PA-3. Once PA-9 lands, it gates PA-7 (label==parent-id assert needs one id form), PA-10 (surfaced TECH-* must join the registry), PA-5 (soft: compaction preserves whatever ids exist so it is not blocked, but the FR traces_to roster gap at L158 is PA-9's fix), and PA-15 (coverage loop keys on the unified AC namespace). PA-12 depends on PA-11: both edit the fr_saturation template, so strip the ```json fence while finishing PA-11's remaining data_model branch literal / nfr-threshold items to avoid churning the template twice. PA-13 overlaps the already-done PA-1 and co-locates with PA-10 in phase6_1a's parent-block/catalog renderers — batch PA-10 + PA-13 (and reuse PA-1's compact id-only roster) so the same renderers aren't rewritten twice. PA-15 depends on both PA-3 (reuse its per-component chunking + orchestrator-owned component->leaf-AC map) and PA-9 (AC id namespace), so it must be last. Throughout, reuse the established patterns: per-target *ById scoping with a full-summary fallback, the shared summaryFormat.ts serializers, deterministic producer-side bridges (orchestrator owns the deterministic axis), and file-reading enum/template consistency tests; structural id work stays a lineage tree-walk, never regex."

**Summary:** "Front-load PA-9 (split so its pure-template id-namespace alignment leads and its leaf/root AC-unification half co-lands with PA-3): it is a single S/low-risk change that fixes broken registry joins and unblocks five other items. Then sweep the isolated S/low-risk bridges (PA-14, PA-6), finish the in-progress PA-11 to release the template-only PA-12, and take the post-PA-9 quick wins (PA-10, the PA-7 verify-and-lock, PA-5 compaction). Land the two coverage-critical decomposition fixes last — PA-3's root->leaf AC scoping via a lineage tree-walk, then PA-15 mirroring its per-component chunking + coverage-reconciliation loop — batching PA-13's dedup into the shared phase6_1a renderers and reusing the established scoping/serializer/deterministic-bridge patterns throughout."

---

## Per-item specs

### PA-9 — id-namespace unification (TECH-BUN-1→TECH-BUN, ent-*→DM-*) via prompt-template alignment to the minted/injected canonical ids
- **status:** confirmed · **risk:** low · **effort:** S · **deterministic-bridge:** false · **deps:** PA-3 · **live-validation:** true · **confidence:** 0.9
- **approach:** Template-consistency fix (the PA-11 established pattern: "fix the prompt not the matcher"). Two id namespaces where the saturation prompts teach an example id FORM that differs from what the orchestrator actually injects into the same prompt and mints as the registry, so the model's emitted join-keys (active_constraints, target_entity_id) miss the registry.

(A) TECH-*: 5 templates hardcode `-1`-suffixed example ids (TECH-BUN-1/TECH-POSTGRES-1/TECH-DBOS-1/TECH-SVELTEKIT-1) in the active_constraints prose, the Required-output example, and surfaced-assumption citations. But the discovery phase mints slug ids `TECH-<UPPER-SLUG>` (NOT a running number; suffix -2/-3 only on collision — technical_constraints_discovery.product.system.md:99), and formatTechnicalConstraints injects the REAL id `t.id` (e.g. TECH-BUN) into the same prompt (phase6_1a.ts:365-370, phase4_2a.ts:352, phase5_1a.ts:277). Fix: rewrite the example ids to the canonical slug form (drop the `-1`), e.g. TECH-BUN-1→TECH-BUN, TECH-POSTGRES-1→TECH-POSTGRES, TECH-DBOS-1→TECH-DBOS, TECH-SVELTEKIT-1→TECH-SVELTEKIT, in: task_decomposition.system.md (88,187,193); component_decomposition.product.system.md (102,158,164); data_model_decomposition.system.md (118,124); test_decomposition.system.md (175,181); implementation_task_decomposition.system.md (189).

(B) Entity: data_model_decomposition.system.md example uses `ent-*` (id `ent-work-order-state` L104; target_entity_id `ent-work-order` L116) but entities are minted `DM-<comp>-<name>` (dataModelIdMinter.ts:28-29 via phase5.ts:199) and the injected sibling/ancestor/depth-0 blocks render the real DM-* ids (phase5_1a.ts:447,463); target_entity_id is join-validated against that DM-* set (traces_to_id_validity, template:139). Fix: rewrite the entity `id` and `target_entity_id` example placeholders to the DM-* form (e.g. DM-work-order-state / DM-work-order).

NO runtime bridge: resolveAgainstOracle (idResolver.ts:76) demonstrably fails this drift (idComparisonKey('TECH-BUN-1')='bun1' vs 'bun', similarity 0.75 < 0.90 → null) AND a suffix-stripping bridge would be semantically WRONG because -2/-3 are legitimate distinct collision-disambiguated ids. The orchestrator already deterministically owns the canonical axis (minter + injected menu); the only correct move is aligning the LLM-facing example to it. The leaf-vs-root US/AC third sub-item is a lineage.canonicalize tree-walk problem already owned/deferred by PA-2/PA-3 — out of PA-9 scope.
- **files/functions:**
  - `prompts/phases/phase_06_implementation_planning/task_saturation/task_decomposition.system.md` — **active_constraints prose (L88) + Required-output example active_constraints (L187) + surfaced_assumptions citations (L193)**: Replace TECH-BUN-1→TECH-BUN, TECH-POSTGRES-1→TECH-POSTGRES, TECH-DBOS-1→TECH-DBOS, TECH-SVELTEKIT-1→TECH-SVELTEKIT so example ids match the injected active_constraints menu (real minted TECH-<SLUG> ids).
  - `prompts/phases/phase_04_architecture/component_saturation/component_decomposition.product.system.md` — **active_constraints prose (L102) + example active_constraints (L158) + citations (L164)**: Same TECH-*-1 → TECH-* canonical-slug rewrite (component_saturation injects real TECH ids the same way via phase4_2a formatTechnicalConstraints).
  - `prompts/phases/phase_05_technical_specification/data_model_saturation/data_model_decomposition.system.md` — **child entity example id (L104) + relationships[].target_entity_id (L116) + active_constraints (L118) + citations (L124)**: ent-work-order-state→DM-work-order-state and ent-work-order→DM-work-order (match minted DM-<comp>-<name> registry + injected ancestor/depth-0 DM-* blocks); TECH-POSTGRES-1→TECH-POSTGRES.
  - `prompts/phases/phase_07_test_planning/test_case_saturation/test_decomposition.system.md` — **example active_constraints (L175) + citations (L181)**: TECH-BUN-1→TECH-BUN, TECH-POSTGRES-1→TECH-POSTGRES.
  - `prompts/phases/phase_06_implementation_planning/implementation_task_decomposition.system.md` — **Required-output traces_to example (L189)**: TECH-POSTGRES-1→TECH-POSTGRES for consistency with the minted TECH slug namespace.
  - `src/test/unit/orchestrator/phases/idNamespaceConsistency.test.ts` — **NEW file-reading regression test (PA-11 pattern)**: Read the 5 templates; assert the `-1`-suffixed TECH placeholders are ABSENT and canonical TECH-BUN/TECH-POSTGRES present; assert data_model template has no `"id": "ent-` or `"target_entity_id": "ent-` and uses DM- entity ids; import mintEntityId from dataModelIdMinter and assert it returns a DM- prefixed id (single-source guard pinning the example prefix).
- **unit-test plan:** New Vitest src/test/unit/orchestrator/phases/idNamespaceConsistency.test.ts, modeled on dataModelSaturationCategoryConsistency.test.ts (fs.readFileSync from repoRoot). Assertions: (1) For each of the 5 saturation/skeleton templates, the exact strings TECH-BUN-1 / TECH-POSTGRES-1 / TECH-DBOS-1 / TECH-SVELTEKIT-1 do NOT appear, and the canonical TECH-BUN / TECH-POSTGRES do appear in the active_constraints/citations/traces_to examples. (2) A regex-free structural check that every `\"active_constraints\": [...]` and citation TECH placeholder matches the discovery slug shape (TECH- followed by an UPPER-SLUG that is not a bare running number) — reuse the discovery rule `^TECH-[A-Z0-9_-]+$` and additionally assert no token ends in `-1`. (3) data_model_decomposition.system.md contains no `\"id\": \"ent-` and no `\"target_entity_id\": \"ent-`; entity id examples start with `DM-`. (4) Single-source guard: import { mintEntityId } from phase5/dataModelIdMinter and assert mintEntityId('comp-x','Y').startsWith('DM-'), so the example prefix is pinned to the minter. All 6 assertions fail on today's templates and pass after the edits; no live LLM needed. (Existing 39x phase unit tests + tsc + eslint must stay green.)

### PA-14 — compliance_context_summary hardcoded empty in Phase 8 evaluation_design — real deterministic bridge from the Phase-1.0d compliance artifact is missing
- **status:** confirmed · **risk:** low · **effort:** S · **deterministic-bridge:** true · **deps:** none · **live-validation:** false · **confidence:** 0.9
- **approach:** CONFIRMED against current code. `Phase8Handler.runEvaluationDesign` (phase8.ts, current line 302) passes the literal `compliance_context_summary: 'No compliance regimes'` into `templateLoader.render`, unconditionally. The upstream signal genuinely exists and is never read: Phase 1.0d writes an `artifact_produced` record with `content.kind === 'compliance_retention_discovery'` and `content.complianceExtractedItems: ExtractedItem[]` (phase1.ts:880-889; item shape = {id (COMP-*), type: REQUIREMENT|DECISION|CONSTRAINT|OPEN_QUESTION, text} per records.ts:2241). phase8's `extractPriorPhaseContext(allArtifacts)` has NO case for `compliance_retention_discovery` (phaseContext.ts switch, lines 106-247), so the items are dropped, and `execute()` never touches the artifact. The evaluation_design template both consumes the slot in CONTEXT (`Compliance: {{compliance_context_summary}}`, line 110) and predicates a rule on it (`Compliance-related NFRs from compliance_context must have evaluation criteria`, line 61) — so with the hardcoded literal the model can never mint compliance evaluation criteria even when regimes exist. The regression fixture proves the bug bakes in: phase08_evaluation_design__tinyurl-001.fixture.json has `"compliance_context_summary": "No compliance regimes"` while the SAME run's FR summary cites `COMP-PII-SANCTION` deletion obligations — compliance content flows upstream but is erased at the eval prompt.

FIX (deterministic bridge — orchestrator owns the deterministic axis of WHICH compliance items exist; LLM owns the judgment of how to evaluate them; mirrors the existing frSummary/nfrSummary/testPlanSummary reads and the summaryFormat.ts / computeEvalCoverage helper pattern):
1) Add exported pure helper `formatComplianceContextSummary(items: unknown): string` in phase8.ts. Coerce to array; keep items with a string `text`. When present: emit compact one-line-per-item block `- ${id ?? 'COMP-?'} [${type ?? 'ITEM'}]: ${text}` (id-preserving, like the other summarizers). When empty/undefined: return a self-describing NEUTRAL sentinel that also neutralizes the dangling rule, e.g. `'No compliance or data-retention regimes were identified upstream (Phase 1.0d). Emit no compliance-specific evaluation criteria — their absence is expected here, not a coverage gap.'` (replaces the misleading bare 'No compliance regimes').
2) In `execute()`, after computing testPlanSummary, find the current-version compliance record and build the summary: iterate `allArtifacts`, pick the record with `is_current_version && content.kind === 'compliance_retention_discovery'` (filter is_current_version — getRecordsByType returns all versions; downstreamGatekeeper.ts:152 filters the same way), read `content.complianceExtractedItems`, then `const complianceSummary = formatComplianceContextSummary(items)`. Optionally push that record id into `dmr81Seeds`/`derivedFromIds` for provenance.
3) Thread `complianceSummary` into `runEvaluationDesign(...)` as a new parameter and use it at the render call in place of the `'No compliance regimes'` literal.
4) OPTIONAL low-risk template hygiene (addresses 'drop the dangling rule'): reword evaluation_design.system.md line 61 to be conditional — `If compliance_context lists any regimes, every compliance-related NFR MUST have an evaluation criterion; if it states none were identified, emit none.` Keep the slot. Not required once the empty sentinel is self-neutralizing; recommend only if pairing with the PA-11-style file-reading guard test.
- **files/functions:**
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase8.ts` — **formatComplianceContextSummary (NEW exported pure helper)**: Add exported function taking the raw complianceExtractedItems (unknown), returning a compact id-preserving 'COMP-* [TYPE]: text' block when items exist, or a self-describing neutral sentinel (not the bare 'No compliance regimes') when empty. Pure + exported for unit test, mirroring computeEvalCoverage / summaryFormat.ts.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase8.ts` — **Phase8Handler.execute**: After testPlanSummary, locate the current-version artifact_produced record with content.kind==='compliance_retention_discovery' (filter is_current_version), read content.complianceExtractedItems, and build complianceSummary = formatComplianceContextSummary(items). Optionally add its record id to dmr81Seeds/derivedFromIds. Pass complianceSummary into runEvaluationDesign.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase8.ts` — **Phase8Handler.runEvaluationDesign**: Add a complianceSummary: string parameter; replace the hardcoded `compliance_context_summary: 'No compliance regimes'` (current line 302) in the templateLoader.render(...) call with that parameter.
  - `JanumiCode/janumicode_v2/prompts/phases/phase_08_evaluation_planning/evaluation_design.system.md` — **line 61 rule (OPTIONAL)**: Optional hygiene: make the 'Compliance-related NFRs from compliance_context must have evaluation criteria' rule conditional so it does not dangle when the context reports no regimes. Only if paired with a file-reading guard test; otherwise the self-neutralizing empty sentinel handles it.
- **unit-test plan:** New Vitest file src/test/unit/orchestrator/phases/phase8ComplianceContext.test.ts (peer of phase8EvalCoverage.test.ts), importing formatComplianceContextSummary from '../../../../lib/orchestrator/phases/phase8'. Cases: (1) items present — pass [{id:'COMP-1',type:'CONSTRAINT',text:'GDPR right-to-erasure within 30d'},{id:'COMP-2',type:'REQUIREMENT',text:'PCI-DSS cardholder data'}] and assert the returned string contains 'COMP-1', 'COMP-2', both text fragments, and does NOT contain the old literal 'No compliance regimes'; (2) empty array and undefined — assert the returned string is the neutral self-describing sentinel, contains 'expected' / 'not a coverage gap' wording (so the dangling rule is neutralized) and is NOT the bare 'No compliance regimes'; (3) malformed items (missing id or non-string text) are tolerated — items without text are skipped, no throw, no '[object Object]'/'undefined' leakage (reuse the PA-8 anti-[object Object] assertion style). If the optional template rewording is taken, add a file-reading guard (PA-11 pattern, like frNfrSaturationCategoryConsistency.test.ts) asserting evaluation_design.system.md still contains the {{compliance_context_summary}} slot and phrases the compliance rule conditionally. Also update/regenerate the regression fixture phase08_evaluation_design__tinyurl-001.fixture.json whose baked '\"compliance_context_summary\": \"No compliance regimes\"' will now become the real COMP-* summary (harness step 4).

### PA-6 — component_saturation domain-context scoping (P4.2a): scope domain_context to the parent's own software domain + thin sibling-domain index
- **status:** confirmed · **risk:** low · **effort:** S · **deterministic-bridge:** true · **deps:** none · **live-validation:** false · **confidence:** 0.9
- **approach:** Deterministic producer-side scoping bridge, mirroring the DONE PA-4 pattern (optional *ById map on the loop input + map.get(key) ?? fullSummary fallback). The orchestrator owns the deterministic axis (which single-domain block each node sees, keyed by the node's own domain_id); the LLM owns judgment (how to decompose). No regex, no lineage tree-walk needed — domain_id is a direct exact key, not an id reduction.

CONFIRMED root cause (current code, line numbers verified): phase4.ts:298-301 builds domainsSummary = ALL software domains joined, each line "id: name (reqs: system_requirement_ids...)  Terms: term:def; ...". The per-domain (reqs: ...) transitively dumps every SR id across all ~18 domains (the backlog's 18 domains + 104 SR; there is NO separate SR block — the SRs ride inside domainsSummary). phase4.ts:699-707 passes that one full string as input.domainsSummary. phase4_2a.ts:583 renders domain_context: input.domainsSummary for EVERY single-component call, ignoring entry.component.domain_id. Template slot (component_decomposition.product.system.md:224-225) says the software domain THIS PARENT belongs to (Phase 4.1 output) — singular. So it is both bloat/scoping (PA-4-class) AND template-vs-reality mismatch (PA-11-class). sibling_context (phase4_2a.ts:578-582, siblingsByParent.get(entry.parentNodeId)) is ALREADY parent-scoped — explicitly OUT of PA-6 scope; only domain_context changes.

THE FIX (3 edits): (1) phase4_2a.ts — extend ComponentSaturationInput with two optional, back-compat fields: domainContextById (domain_id to that ONE domain's full block) and domainIndex (thin id:name-only roster of all domains). Add a small EXPORTED pure helper formatScopedDomainContext(domainId, input): returns domainContextById[domainId] plus appended thin domainIndex under an 'Other software domains (index only)' header when present, when the parent's domain resolves; falls back to the full domainsSummary when domainId is null/undefined/unresolved (established safety net). At the render site (~583) replace domain_context: input.domainsSummary with domain_context: formatScopedDomainContext(entry.component.domain_id, input). (2) phase4.ts — where domainsSummary is built (298-301), also build domainContextById (reuse the exact same single-domain line format, keyed by d.id) and domainIndex (id + name only per domain, one per line). Pass both into the runComponentSaturationLoop input object at 699-707. Keep domainsSummary as the fallback field. (3) No template change required — slot semantics now MATCH reality.

Back-compat: existing phase4_2aSaturation.test.ts calls pass only domainsSummary (no domainContextById) so the helper falls through to input.domainsSummary — identical current behavior. No record-shape or DB change.
- **files/functions:**
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase4_2a.ts` — **ComponentSaturationInput**: Add two optional back-compat fields: domainContextById (Record string to string; domain_id to that domain's full block) and domainIndex (string; thin id:name-only roster). Mirrors PA-4 componentSummaryById.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase4_2a.ts` — **formatScopedDomainContext (new exported pure helper)**: New exported function taking (domainId, input with domainContextById/domainIndex/domainsSummary). Returns the parent's own single-domain block from domainContextById[domainId] plus an appended thin domainIndex under an 'Other software domains (index only)' header when both present; falls back to domainsSummary when domainId is null/undefined or not in the map.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase4_2a.ts` — **runComponentSaturationLoop (variables render site ~line 583)**: Replace domain_context: input.domainsSummary with domain_context: formatScopedDomainContext(entry.component.domain_id, input).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase4.ts` — **Phase4Handler.execute (build domainsSummary ~298-301)**: Alongside domainsSummary, build domainContextById keyed by d.id using the SAME single-domain line format, and domainIndex as one 'id: name' line per domain.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase4.ts` — **Phase4Handler.execute (runComponentSaturationLoop call ~699-707)**: Pass domainContextById and domainIndex into the ComponentSaturationInput object (keep domainsSummary as fallback).
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/phase4_2aDomainScope.test.ts` — **new test file (PA-6 regression)**: New Vitest file mirroring phase5_1aDataModelScope.test.ts: seed two roots in different domains, drive runComponentSaturationLoop, assert via mock.getCallLog() prompts; plus direct unit tests of formatScopedDomainContext.
- **unit-test plan:** New file src/test/unit/orchestrator/phases/phase4_2aDomainScope.test.ts (Vitest), structured like phase5_1aDataModelScope.test.ts and reusing phase4_2aSaturation.test.ts helpers (seedRootNode, tinyHandoff, MockLLMProvider, configureMock).

Loop-level regression test: two root DecompositionComponents compA (id comp-a, domain_id domain-a) and compB (id comp-b, domain_id domain-b). Mock fixture returns an atomic Tier-D mirror decomposition so the loop terminates in one pass. Call runComponentSaturationLoop with domainsSummary = FULL-DOMAIN-CATALOG, domainContextById = {domain-a: SCOPED-DOMAIN-A-ONLY, domain-b: SCOPED-DOMAIN-B-ONLY}, domainIndex = a two-line id:name roster. Take prompts = mock.getCallLog().map(c => c.options.prompt). Find the prompt containing SCOPED-DOMAIN-A-ONLY. Assert it contains SCOPED-DOMAIN-A-ONLY; does NOT contain SCOPED-DOMAIN-B-ONLY (sibling full block excluded); does NOT contain FULL-DOMAIN-CATALOG (no full-catalog fallback fired); DOES contain the thin index line for domain-b (id:name only is allowed).

Fallback test: single root with domain_id null; same inputs. Assert the emitted prompt contains FULL-DOMAIN-CATALOG (safety-net fallback preserved).

Direct pure-helper unit tests for formatScopedDomainContext: (a) resolvable id + index present -> own block + index header, no full summary; (b) null/undefined id -> full summary; (c) id absent from map -> full summary; (d) index absent -> own block only.

Also confirm existing phase4_2aSaturation.test.ts still passes green (back-compat via fallback, since it passes only domainsSummary). Run vitest on both files + full phases suite; tsc + eslint clean.

### PA-12 — JSON-fence self-contradiction in fr_saturation + task_skeleton templates: strip the ```json fence from the in-prompt output example
- **status:** confirmed · **risk:** low · **effort:** S · **deterministic-bridge:** false · **deps:** PA-11 · **live-validation:** false · **confidence:** 0.9
- **approach:** Template-only consistency fix (same class as the DONE PA-11), guarded by a file-reading unit test. In each of the two named templates, the labeled full-output example is wrapped in a ```json ... ``` markdown fence, and a few lines later the "JSON Output Contract" block orders "No markdown fences. Response starts with {". The model pattern-matches the ```json wrapper and emits fenced output (audit C4). FIX: delete the ```json opening line and the closing ``` line around the full-output example, leaving the raw JSON object under its heading, and prepend a one-line lead-in reinforcing "emit as raw JSON, first char is {, no surrounding markdown code fences". Keep the (correct) "No markdown fences" JSON Output Contract block unchanged so instruction and example now agree. Do NOT touch the other, legitimately-illustrative plain ``` blocks (parent_branch_classification enum, Completion-Criteria Shape, Path Discipline GOOD/BAD) — they illustrate sub-shapes, not the whole response, and are not the mimicry trigger; scoping the fix to the single ```json full-object example is the minimal change. The second half of the backlog note ("ensure normalizer strips fences") is ALREADY-HANDLED: extractJsonObject (jsonRecovery.ts:34-37) strips ```json/``` wrappers on every parse path (ollama.ts:235/407, orchestratorEngine.ts:1906, llmCaller.ts:902) — no code change needed there. The template fix still matters because a fenced response fires jsonOutputDisciplineCheck's markdown_fence_wrapper at HIGH (short-circuits the LLM validator chain) and can trigger json_repair latency. RECOMMENDED same-pass extension (identical mechanical edit): apply to the sibling decomposition templates that share the exact contradiction — nfr_saturation, data_model_saturation, task_saturation (1 ```json each) and test_case_saturation (2) — and cover them all in the one test.
- **files/functions:**
  - `prompts/phases/phase_02_requirements/fr_saturation/functional_requirements_decomposition.product.system.md` — **# Required output (strict schema) section (currently lines ~115-144; file is already M in the working tree from PA-11, so anchor on the heading not the line number)**: Remove the ```json opening-fence line and the closing ``` line surrounding the example object. Replace the blank line after the heading with a lead-in such as: 'Emit your ENTIRE response as a single raw JSON object of exactly this shape — start at `{`, end at `}`, with NO surrounding markdown code fences:' Leave the JSON object body verbatim; the following `# Hard rules` heading already bounds it. Leave the `# JSON Output Contract` 'No markdown fences' block unchanged.
  - `prompts/phases/phase_06_implementation_planning/implementation_task_decomposition.system.md` — **## Complete example (two tasks — all field shapes shown) section (currently lines ~132-193; anchor on the heading)**: Remove the ```json opening-fence line (line ~134) and the closing ``` line (line ~193) around the two-task example. Prepend a lead-in: 'The example below shows the exact output shape — emit it as raw JSON with NO surrounding markdown code fences; your response's first character is `{`.' Leave the JSON body verbatim; the following `---` rule bounds it. Leave the `# JSON Output Contract` 'No markdown fences' block (line ~210-218) unchanged.
  - `src/test/unit/orchestrator/phases/jsonFenceContractConsistency.test.ts` — **NEW file — describe('PA-12 — decomposition templates: no self-contradicting JSON fence')**: Mirror frNfrSaturationCategoryConsistency.test.ts (fs.readFileSync from repoRoot = resolve(__dirname,'..','..','..','..','..')). For each template in the decomposition family, assert body.toContain('No markdown fences') AND body NOT to contain '```json' (the ```json output-example fence is gone), and a positive check that the example content survived (fr: toContain('"parent_branch_classification"'); task_skel: toContain('"tasks"')). Start with the two named templates; add nfr_saturation/data_model_saturation/task_saturation/test_case_saturation if the extension edits land in the same pass.
  - `src/lib/llm/jsonRecovery.ts` — **extractJsonObject**: NO CHANGE (already-handled). Documented here as the reason the backlog's 'ensure normalizer strips fences' half needs no work: lines 34-37 already strip ```json/``` wrappers, with an outermost-brace fallback, on all parse paths.
- **unit-test plan:** New Vitest file src/test/unit/orchestrator/phases/jsonFenceContractConsistency.test.ts, modeled on the existing frNfrSaturationCategoryConsistency.test.ts (same fs.readFileSync + repoRoot resolution pattern). For fr_saturation and task_skeleton templates: (1) expect(body).toContain('No markdown fences') — the correct instruction is still present; (2) expect(body).not.toContain('```json') — the self-contradicting output-example fence is stripped (each file has exactly one ```json today, so this is a crisp non-flaky invariant); (3) positive survival check that the example body was not accidentally deleted: fr expect(body).toContain('\"parent_branch_classification\"'); task_skel expect(body).toContain('\"tasks\"'). This pins the invariant and makes the C4 contradiction non-recurring. Run: npx vitest run src/test/unit/orchestrator/phases/jsonFenceContractConsistency.test.ts (plus the full phases suite to confirm no regression: ~391+ tests green). No new normalizer test needed — jsonRecovery fence-stripping is already covered by existing recovery tests.

### PA-10 — parent_task/parent_entity blocks carry no active_constraints (TECH-* narrowing not surfaced at node level)
- **status:** partial · **risk:** low · **effort:** S · **deterministic-bridge:** true · **deps:** PA-9 · **live-validation:** false · **confidence:** 0.9
- **approach:** Two-facet item. Facet A (narrow the GOVERNING CONSTRAINTS menu to the node's inherited set) is ALREADY HANDLED in committed code: phase6_1a.ts:553-556 and phase5_1a.ts:424-427 filter input.technicalConstraints by entry.activeConstraints into activeConstraintsForPrompt, rendered at {{active_constraints}}. Facet B — the literal PA-10 ask, "inject the node's actual inherited active_constraints array into its block" — is OPEN and is the fix: add an `Active constraints:` line to the two node-block renderers so the node's own inherited TECH-* id list is visible in the parent block (currently only the derivable global menu shows it). This is a deterministic bridge: the orchestrator owns the 'which TECH-* ids this node inherited' axis (persisted on node.task/entity.active_constraints, and already the exact source the narrowed menu derives from), and surfaces it verbatim; the LLM keeps ownership of per-child narrowing judgment. The line reads the id array with an empty-fallback string; no template variable change, no schema change, no control-flow change. Roots (seeded with the full set) show the full id list — acceptable, it is the true starting inherited set and gives children an id list to copy; deeper nodes show the genuinely-narrowed subset. Per-component ROOT narrowing (phase6.ts:1502 / phase5.ts:258 seed the full constraintIds set; phase4.ts:658 seeds components full too) is deferred: there is no reliably-narrowed per-component source yet, so it is a larger upstream-dependent change, not part of this minimal fix.
- **files/functions:**
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6_1a.ts` — **formatRootTaskForPrompt**: In the returned string array (currently Task id..Dependencies..Traces to, lines ~349-362) add one line rendering the node's own inherited constraints, e.g. after `Dependencies: ${deps}`: compute `const constraints = (t.active_constraints ?? []).join(', ') || '(none — inherit all governing constraints)';` and insert `\`Active constraints: ${constraints}\`,` into the array. Type already supports it (DecompositionTask.active_constraints?: string[]).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase5_1a.ts` — **formatEntityForPrompt**: Mirror the task fix: compute `const constraints = (e.active_constraints ?? []).join(', ') || '(none — inherit all governing constraints)';` and add `\`Active constraints: ${constraints}\`,` to the returned array (lines ~263-272), e.g. after the Relationships block. DecompositionEntity.active_constraints?: string[] supports it.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/phase6_1aSaturation.test.ts` — **new it('PA-10 — parent_task block carries node inherited active_constraints ids')**: Extend tinyTask (or override) so the seeded root task carries active_constraints:['TECH-BUN-1','TECH-POSTGRES-1']; pass technicalConstraints=[TECH-BUN-1,TECH-POSTGRES-1,TECH-SMTP-1] (distractor); atomic fixture → 1 call. Assert captured prompt (mock.getCallLog()) parent block contains 'Active constraints: TECH-BUN-1, TECH-POSTGRES-1', and (Facet-A regression) the GOVERNING CONSTRAINTS text does NOT include the TECH-SMTP-1 constraint text. Optional 2nd case: empty active_constraints → parent block shows '(none — inherit all governing constraints)'.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/phase5_1aDataModelScope.test.ts` — **new it('PA-10 — parent_entity block carries node inherited active_constraints ids')**: Analogous entity case reusing tinyEntity/seedRootNode: seed root entity active_constraints:['TECH-POSTGRES-1'], technicalConstraints=[TECH-POSTGRES-1,TECH-SMTP-1]; assert parent_entity block contains 'Active constraints: TECH-POSTGRES-1' and GOVERNING CONSTRAINTS excludes TECH-SMTP-1 text.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **normalizeRootTaskShape (line 1502) / phase5.ts entity seeding (line 258)**: NO CHANGE this pass — documents where roots are seeded with the full constraint id set. Per-component root narrowing deferred (no narrowed per-component source exists; phase4.ts:658 seeds components full too).
- **unit-test plan:** Vitest, extend the two existing loop-driven prompt-capture tests (same harness: real OrchestratorEngine + MockLLMProvider, seed root node, run loop, inspect mock.getCallLog().options.prompt). (1) phase6_1aSaturation.test.ts: seed root task with active_constraints:['TECH-BUN-1','TECH-POSTGRES-1']; technicalConstraints=[BUN,POSTGRES,SMTP]; atomic fixture; assert prompt contains parent-block line 'Active constraints: TECH-BUN-1, TECH-POSTGRES-1' AND that GOVERNING CONSTRAINTS narrowed (no TECH-SMTP-1 text) — this also regression-guards Facet A. (2) phase5_1aDataModelScope.test.ts: analogous entity case (active_constraints:['TECH-POSTGRES-1'], distractor TECH-SMTP-1; assert parent_entity block + narrowed menu). (3) Optional empty case: active_constraints:[] → parent block shows '(none — inherit all governing constraints)', GOVERNING CONSTRAINTS shows full set. Run `npx vitest run src/test/unit/orchestrator/phases/phase6_1aSaturation.test.ts src/test/unit/orchestrator/phases/phase5_1aDataModelScope.test.ts` plus full phase suite + tsc + eslint.

### PA-7 — parent-slot keying (label ≠ injected parent) — data_model_saturation / task_saturation / task_skeleton
- **status:** already-handled · **risk:** low · **effort:** S · **deterministic-bridge:** false · **deps:** PA-9 · **live-validation:** false · **confidence:** 0.85
- **approach:** The claimed "genuine builder/keying bug" (parent block materialized from a different node than the label) does NOT exist in current code — it is REFUTED. In all three cited sub_phases the target block and the trace label are single-sourced from the same in-memory record, so they cannot diverge:
  • data_model_saturation (phase5_1a.ts runDataModelSaturationLoop): variables.parent_entity = formatEntityForPrompt(entry.entity) [L450]; traceContext.label uses entry.displayKey [L493] — same `entry`. Template renders `# Parent entity being decomposed {{parent_entity}}` first in INPUT [template L183-184].
  • task_saturation (phase6_1a.ts runTaskSaturationLoop): variables.parent_task = formatRootTaskForPrompt(entry.task) [L572]; label uses entry.displayKey [L614] — same `entry`. Template `# Parent task being decomposed {{parent_task}}` [L227].
  • task_skeleton (phase6.ts runTaskDecomposition): component_model_summary = renderComponentBlockForTask(component) [L692/696]; label = `Phase 6.1 — Task Decomposition (${cid})`, cid = String(component.id) [L714] — same loop `component`, no fallback.
The foreign ids the audit flagged as the "injected parent" come only from REFERENCE slots that legitimately enumerate other nodes: sibling_context, depth_zero_entities/depth_zero_tasks, the all-component technical_specs_summary roll-up / DMR detail_file_content, and the component_context full-summary fallback (renderComponentBlockForTask emits `[Backlog] comp-*`, phase6.ts L1102/1109). The audit's B3 parent-id extraction heuristic mis-attributes one of these to the parent block. Two benign (non-matching) divergences exist: the collision-suffix display key (`ent-x#1a2b` in label vs raw `ent-x` in block — same base id), and the component_context fallback that injects the full multi-component summary when entry.*.component_id fails to resolve in componentSummaryById (id drift) — but that corrupts only the supporting context slot, never parent_entity/parent_task, and is owned by PA-4/PA-9, not a parent-slot keying bug.
RECOMMENDED DELIVERABLE (the only actionable piece — the guard the PA-7 note itself proposes): add a Vitest regression lock that asserts, for each of the three sub_phases, the rendered prompt's parent/target block id equals the label's target id and that distractor foreign ids appear only under reference headers, not in the parent block. Optional durability hardening: extract a single shared label builder decompositionTraceLabel(subPhase, passNumber, entry.displayKey, entry.depth, entry.tierHint) into summaryFormat.ts and use it at both label sites so a future refactor cannot decouple label from block. No behavioral/producer change is required; do NOT add any regex id resolution.
- **files/functions:**
  - `src/lib/orchestrator/phases/phase5_1a.ts` — **runDataModelSaturationLoop (parent_entity var L450 + traceContext.label L493)**: No change needed — parent_entity and label already single-sourced from entry.entity/entry.displayKey. Anchor for the regression test.
  - `src/lib/orchestrator/phases/phase6_1a.ts` — **runTaskSaturationLoop (parent_task var L572 + traceContext.label L614)**: No change needed — parent_task and label already single-sourced from entry.task/entry.displayKey. Anchor for the regression test.
  - `src/lib/orchestrator/phases/phase6.ts` — **runTaskDecomposition (component_model_summary L692/696 + label L714)**: No change needed — component_model_summary and label(cid) both from the same loop `component`. Anchor for the regression test.
  - `src/test/unit/orchestrator/phases/parentSlotKeying.test.ts` — **describe('PA-7 parent-slot keying')**: ADD new test file: 3 tests (data_model / task / task_skeleton) that seed a target node among distractor siblings+depth-0 nodes with different ids, run the loop with a mock returning atomic (terminating) children, then read mock.getCallLog() and assert (a) the parent-block header line is followed by the target id, (b) the label target id (strip any #suffix) == the parent-block id, (c) each distractor foreign id does NOT appear in the parent-block region (only under reference headers).
  - `src/lib/orchestrator/phases/summaryFormat.ts` — **decompositionTraceLabel (optional new helper)**: OPTIONAL hardening: add a single shared label builder taking (subPhaseId, passNumber, displayKey, depth, tierHint); wire it into the label sites in phase5_1a.ts and phase6_1a.ts so label and parent block provably share one id source. Skip if keeping the change minimal.
- **unit-test plan:** Vitest, src/test/unit/orchestrator/phases/parentSlotKeying.test.ts, following the phase6_1aSaturation.test.ts / phase7_1aSaturation.test.ts harness (createTestDatabase + OrchestratorEngine + MockLLMProvider, engine.setAutoApproveDecisions(true), capture prompts via mock.getCallLog().map(c=>c.options)). Three cases:
1. task_saturation: seed root node task-target-root (component comp-a) plus distractor roots task-decoy-root (comp-b) and task-other-root (comp-c). Mock returns a decomposable→all-Tier-D response so each root makes exactly one call then terminates. Find the call whose options.traceContext.label contains 'task-target-root'. Assert: the prompt segment after '# Parent task being decomposed' up to '# Sibling context' contains 'Task id: task-target-root' and NOT 'task-decoy-root'/'task-other-root'; and the label's target id equals the parent-block Task id.
2. data_model_saturation: same shape with runDataModelSaturationLoop — root entities ent-target (comp-a), ent-decoy (comp-a) [forces a sibling roster], ent-foreign (comp-b). Assert the '# Parent entity being decomposed' block carries 'Entity id: ent-target' and the label id matches; ent-decoy appears only under '# Sibling context'/'# Depth-0 entities', never in the parent block.
3. task_skeleton: drive runTaskDecomposition (or its renderComponentBlockForTask + label construction) over 2 leaf components; assert each per-component call's component_model_summary block id == its label cid, and the other component's id is absent from that call's component_model_summary block.
Add a mutation-guard assertion (label id === parent-block id) so a future refactor that rewires the label to a different entry field fails the test. All assertions use base-id comparison (strip trailing '#xxxx' collision suffix). Expect ~391→394 phase tests green; tsc + eslint clean.

### PA-5 — fr/nfr_saturation handoff-corpus compaction (id + one-line label; drop verbose bodies)
- **status:** confirmed · **risk:** medium · **effort:** S · **deterministic-bridge:** true · **deps:** PA-9 (soft/non-blocking): the FR template's traces_to id list at line 158 enumerates UJ-*/VV-*/TECH-*/COMP-*/QA-# but the handoff also carries ENT-*/WF-*/VOC-* (bloom validator recognizes ENT-/WF-/VOC-). Compaction preserves whatever ids exist so it is not blocked, but the id-namespace/roster mismatch is PA-9's concern., Optional follow-up (separate item, not a blocker): the 'hoist byte-identical block' lever (provider prompt-cache or template-reorder to a stable cacheable prefix) is an L-effort, provider-dependent change; compaction here is the provider-agnostic, unit-testable minimal fix. · **live-validation:** true · **confidence:** 0.86
- **approach:** Adopt the backlog's Lever 1 (compaction), NOT per-node scoping (fuzzy, deferred) and NOT the hoist/prompt-cache lever (provider-dependent, template is not a clean prefix). Rewrite ONLY `formatHandoffForDecomposition` (phase2.ts:2818) so it renders one compact row per handoff item = `id + one-line label`, dropping the multi-line acceptance/description/trigger/definition bodies that are the ~22.7K bulk. This is a deterministic producer-side bridge: the orchestrator owns the byte-budget/compaction axis; the LLM still owns judgment over the named items. Because the template only asks the model to (a) ground commitments in these NAMED items and (b) resolve `traces_to[]` against these ids (UJ-*/ENT-*/WF-*/QA-#/TECH-*/VV-*/COMP-*/VOC-*), keeping id+name/title/category preserves both the fabrication-guard roster (QA-# comment at phase2.ts:2823-2831) and trace-id resolution, while cutting ~18-20K of the 22.7K per call across ~54 identical calls.\n\nConcrete change to `formatHandoffForDecomposition`: do NOT call the shared verbose `formatJourneys/formatEntities/formatWorkflows/formatVocabulary/formatTechnicalConstraints/formatVVRequirements/formatExtractedItems` (they must stay verbose — they are passed via the `format:` object into runFrBloomThreePass/runNfrBloomThreePass at phase2.ts:444,693 and used on the traces-SCOPED subset in frBloomThreePass.ts:148-152, where the bodies are intended grounding). Instead render inline compact rows using only fields already proven present in those formatters: journeys `- {j.id} {j.title}`; entities `- {e.id} {e.name}`; workflows `- {w.id} {w.name}`; quality attributes keep existing `- [QA-{i+1}] {q}` (q is a short attribute string; pass through a single-line/trim helper); technical constraints `- {t.id} ({t.category})[ {t.technology}@{t.version}]` (drop t.text); V&V `- {v.id} [{v.category}] {oneLine(v.target)}` (drop measurement/threshold); compliance `- {i.id} [{i.type}]` (drop i.text); vocabulary `- {t.id} {t.term}` (drop synonyms/definition). Preserve the section headings and the `(empty handoff)` sentinel. Add a tiny local `oneLine(s)`=first-line+length-cap helper for the free-text QA/target fields. Add `export` to `formatHandoffForDecomposition` for direct unit testing (mirrors nestChildStoryId/assignReleaseToRoot export style). Note the hoist lever separately as an optional L follow-up.
- **files/functions:**
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase2.ts` — **formatHandoffForDecomposition (line ~2818; caller at line 1166, injected at line 1435 as handoff_context)**: Rewrite body to emit compact id+one-line-label rows per category (journeys/entities/workflows/QA/tech-constraints/V&V/compliance/vocab), dropping verbose scenario/acceptance/description/triggers/definition bodies. Do NOT reuse the shared verbose formatJourneys/formatEntities/formatWorkflows/formatVocabulary/formatTechnicalConstraints/formatVVRequirements/formatExtractedItems (they stay verbose for the scoped bloom passes). Add a small local oneLine() trim helper for free-text QA/target fields. Add `export` keyword so the compaction can be unit-tested directly. Keep section headings and the '(empty handoff)' sentinel.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/phase2HandoffCompaction.test.ts` — **new Vitest file (import { formatHandoffForDecomposition } from phase2.ts)**: Add regression test asserting compaction + no-item-loss (see unit_test_plan).
- **unit-test plan:** New file src/test/unit/orchestrator/phases/phase2HandoffCompaction.test.ts, importing the now-exported formatHandoffForDecomposition. Build a synthetic ProductDescriptionHandoffContent with 1-2 items per category, each carrying a deliberately LONG unique sentinel body (e.g. journey.scenario='SCENARIO_BODY_XYZ' + acceptanceCriteria=['ACC_BODY_XYZ'], entity.description='ENT_DESC_XYZ', workflow.description='WF_DESC_XYZ' + a trigger, vocab.definition='VOC_DEF_XYZ' + synonyms, technicalConstraints.text='TECH_TEXT_XYZ', vvRequirements.measurement='VV_MEAS_XYZ', complianceExtractedItems.text='COMP_TEXT_XYZ'). Assertions: (1) every id/roster token present — UJ-*, ENT-*, WF-*, '[QA-1]', TECH-*, VV-*, COMP-*, VOC-* and each item's title/name/term; (2) NONE of the verbose sentinels present (SCENARIO_BODY_XYZ, ACC_BODY_XYZ, ENT_DESC_XYZ, WF_DESC_XYZ, VOC_DEF_XYZ, TECH_TEXT_XYZ, VV_MEAS_XYZ, COMP_TEXT_XYZ) — proves bodies dropped; (3) no multi-line continuation ('\\n  Acceptance:' / '\\n  triggers:') present — one line per item; (4) empty handoff ({}) → '(empty handoff)'; (5) economy assertion: output length is a small fraction (assert < ~35%) of a verbose baseline computed by re-rendering the same fixture through the still-verbose shared formatters, guarding the byte-cut. OPTIONAL guard test in same file (or a one-liner) asserting formatJourneys(...) on the fixture STILL contains SCENARIO_BODY_XYZ — pins that the shared bloom formatters were not accidentally compacted (prevents a bloom-grounding regression). Follow existing category-consistency test conventions; keep the 39x-green phase suite passing (tsc + eslint clean).

### PA-3 — task_skeleton AC-inventory + tech-specs per-component scoping (root→leaf AC lineage binding)
- **status:** confirmed · **risk:** medium · **effort:** M · **deterministic-bridge:** true · **deps:** PA-9 · **live-validation:** true · **confidence:** 0.8
- **approach:** CONFIRMED against current code (line numbers drifted; mechanism intact). Phase 6.1 was already refactored to PER-COMPONENT chunking (runTaskDecomposition, phase6.ts:643-774), but the two reference catalogs are STILL injected in full into every per-component call: acceptance_criteria_menu = renderAcceptanceCriteriaMenu over ALL leaf ACs (precomputed phase6.ts:178, rendered phase6.ts:695), and technical_specs_summary = the all-component SR+IC+DM+API+error+config blob (built phase6.ts:135-142, rendered phase6.ts:697). That is the measured 46%+26%=72% bloat, and why the model latches onto a foreign component's ACs (C3 13h).

The clean fix the deferral waited on — the root→leaf AC lineage binding — is now available and already used in this exact shape: buildRequirementLineage(...).canonicalize (packetSynthesis/idResolution.ts), a STRUCTURAL leaf→root tree-walk (no regex; hard project rule), already imported by phase4.ts:446 and phase8.ts to canonicalize component traces_to leaf-US ids to roots. Components carry their US traces (root view: traces_to; leaf view alias: satisfies_requirement_ids, set phaseContext.ts:788), and each leaf-AC group carries its owning leaf story. Canonicalizing BOTH sides to the root-US display_key yields a deterministic component→leaf-AC map.

DETERMINISTIC BRIDGE (orchestrator owns the axis, LLM owns judgment):
1. execute(): build reqLineage = buildRequirementLineage([...allArtifacts, ...requirementDecompositionNodes]) (both already in scope: :99 and :176) and thread reqLineage.canonicalize into runTaskDecomposition.
2. componentRootStorySet(component, canonicalize): read traces_to ?? satisfies_requirement_ids ?? [] (dual-key tolerance — leaf view renames it), map each through canonicalize, return the Set of root ids.
3. renderScopedAcceptanceCriteriaMenu(leaves, componentRoots, canonicalize): partition leaves into OWNED (canonicalize(leaf.leafDisplayKey ?? leaf.leafStoryId) in componentRoots) vs OTHER. Render OWNED in full (reuse renderAcceptanceCriteriaMenu grouped format) + a compact id-only appendix of the OTHER AC ids. FALLBACK: OWNED empty (unresolved traces / namespace drift) → return the FULL menu (identical to today → coverage can never regress below baseline). Mirrors the established *ById + full-summary-fallback pattern (PA-1/2/4).
4. Per-component loop (phase6.ts:690-702): replace acceptance_criteria_menu: acceptanceCriteriaMenu with the scoped render for THIS component; the precomputed full menu at :178 becomes the fallback path only.

COVERAGE SAFETY: the model still SEES every AC id (owned=full text, others=id-only) so it can cite any; and the orchestrator-owned reconciliation loop already present (computeUncoveredAcIds over the FULL leafAcIdSet → reconcileUncoveredAcs, phase6.ts:731-758) is the hard backstop that closes any AC a scoped menu omitted. So AC scoping is coverage-safe by construction.

TECH-SPECS (secondary axis, same mechanism, splittable): add techSpecsSummaryById[cid] in execute() (mirroring componentSummaryById:119-123) slicing data_models + api_definitions items by component_id (both carry it — phase5.ts:37/50/66) and system_requirements items by source-id reachability (canonicalize(SR.source_requirement_ids[i]) in componentRoots), keeping the small IC/error/config blocks as-is; fallback to the full techSpecsSummary when the slice is empty. RISK NOTE: tech-spec coverage has NO reconciliation backstop (only a downstream validator that each spec id appears in some task traces_to), so gate the SR-reachability slice behind live validation; the DM/API-by-component_id slice is clean/low-risk. Ship AC scoping + DM/API scoping first, SR-slice second.

ALREADY-HANDLED PREREQUISITE: the template's former "every leaf AC MUST be covered" global mandate is already softened — implementation_task_decomposition.system.md rule 6b now says cover only THIS component's ACs and "Global coverage ... is reconciled by the orchestrator", and the AC-Inventory heading already reads "reference lookup ... NOT a coverage target for this call". No template change required.
- **files/functions:**
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **LeafAcceptanceCriteria (interface) + collectLeafAcceptanceCriteria**: Add optional leafDisplayKey?: string to the interface and capture the leaf node's content.display_key into it (additive; robust canonicalize input so collision-safe display_keys that differ from story.id still resolve; falls back to leafStoryId when absent).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **componentRootStorySet (new exported helper)**: Given a component record + canonicalize fn, read traces_to ?? satisfies_requirement_ids ?? [] (dual-key: leaf view renames traces_to to satisfies_requirement_ids at phaseContext.ts:788), map each id through canonicalize (structural tree-walk, no regex), return Set<string> of root ids.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **renderScopedAcceptanceCriteriaMenu (new exported helper)**: Partition leaves into owned (canonicalize(leafDisplayKey??leafStoryId) in componentRoots) vs other; render owned in full via the existing grouped format + a compact id-only appendix of other AC ids; return the full menu (renderAcceptanceCriteriaMenu) when owned is empty (no-coverage-regression fallback).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **Phase6Handler.runTaskDecomposition**: Add a canonicalize: (id:string)=>string parameter; in the per-component loop (currently rendering acceptance_criteria_menu: acceptanceCriteriaMenu at :695) render the per-component scoped menu via renderScopedAcceptanceCriteriaMenu(leafAcceptanceCriteria, componentRootStorySet(component, canonicalize), canonicalize). Optionally drop the now-fallback-only acceptanceCriteriaMenu param.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **Phase6Handler.execute + import**: Import buildRequirementLineage from './packetSynthesis/idResolution'; build reqLineage = buildRequirementLineage([...allArtifacts, ...requirementDecompositionNodes]) (both already in scope) and pass reqLineage.canonicalize into runTaskDecomposition. Secondary: build techSpecsSummaryById[cid] slicing data_models/api_definitions by component_id and system_requirements by source-id reachability, full-fallback, used in the per-component loop instead of the shared techSpecsSummary — gate the SR slice behind live validation.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/phase6AcScoping.test.ts` — **new test file**: Unit tests for componentRootStorySet + renderScopedAcceptanceCriteriaMenu (see unit_test_plan).
- **unit-test plan:** New Vitest file src/test/unit/orchestrator/phases/phase6AcScoping.test.ts (model after phase6LeafAcBinding.test.ts leafNode helper). Build a requirement_decomposition tree: root {depth:0, node_id:n1, display_key:'US-001', parent:null} + leaf {depth:1, node_id:n2, display_key:'US-001-D', parent:n1, user_story US-001-D with AC-A1/AC-A2}; independent story US-002 leaf {display_key:'US-002', AC-B1}. lineage = buildRequirementLineage(records); leaves = collectLeafAcceptanceCriteria(records).
1. componentRootStorySet: {traces_to:['US-001-D']} -> {'US-001'} (leaf->root tree-walk); leaf-view alias {satisfies_requirement_ids:['US-001']} -> {'US-001'}.
2. renderScopedAcceptanceCriteriaMenu for componentRoots={'US-001'}: contains AC-A1/AC-A2 WITH descriptive text; contains AC-B1 as an id-only token but NOT its description text and NOT the US-002 story text (proves the foreign-component text bloat is cut while the id stays citable).
3. Fallback: componentRoots=empty -> output === renderAcceptanceCriteriaMenu(leaves) (full menu, no regression).
4. Collision robustness: a leaf whose display_key ('US-003x') differs from user_story.id ('US-003') still binds via leafDisplayKey canonicalize when its root is in componentRoots.
5. (If SR/DM tech-spec slice implemented) a data_models item with component_id=X appears in techSpecsSummaryById[X], absent from [Y]; empty slice falls back to full techSpecsSummary. Assert phase tests + tsc + eslint stay green.

### PA-13 — Duplicate catalog injection: governing constraints rendered 2-3× (in-template + top-slot vs DMR-file); sibling==depth-0 and material-findings-repeat already handled
- **status:** partial · **risk:** medium · **effort:** M · **deterministic-bridge:** true · **deps:** PA-1 · **live-validation:** false · **confidence:** 0.88
- **approach:** PA-13 bundles three concerns; two are already handled by prior fixes, one is real and open.

(1) sibling_context == depth_zero at root — ALREADY-HANDLED by PA-1. phase6_1a.ts:566-568 renders depth_zero_tasks as an id-only comma roster and 574-586 scopes root sibling_context to the same component. They no longer duplicate (~33KB gone) and now serve distinct purposes: depth_zero = valid cross-branch dependency-target id namespace; sibling_context = same-parent siblings with names. Guarded by phase6_1aSaturation.test.ts:169-215. The backlog's original "collapse to a single block at root" would REGRESS this distinct-purpose design — do NOT do it.

(3) DMR "Most Material Findings" repeats — ALREADY-HANDLED by the ws-156 dedup in dmrHydration.ts:141-172 (renderedAboveIds built from packet.activeConstraints + dedupedCount footer), guarded by dmrHydration.test.ts:69-85.

(2) Governing constraints injected 2-3× — REAL, OPEN, two sub-defects:
 (2a) In-template duplication: implementation_task_decomposition.system.md (task_skeleton) renders {{active_constraints}} at line 25 (top 'GOVERNING CONSTRAINTS (apply without exception)') AND line 231 ('# Active constraints' in [INPUT]). Both fill with dmr.activeConstraintsText (BINDING + CERTIFIED CONTEXT, several KB on a real spec). This is the only template of 29 with count=2; all others =1. Fix = pure template edit: delete the [INPUT] '# Active constraints\n{{active_constraints}}' occurrence, replace with a one-line pointer to the top block. active_constraints stays a required variable (still used at line 25), so no renderer/code change.
 (2b) Top-slot vs DMR-file: every DMR-consuming P0-8 phase injects active_constraints (= dmr.activeConstraintsText) at the top AND detail_file_content (= dmr.detailFileContent = renderHydratedPacket) which re-renders the same governing statements under '## Governing Constraints (binding...)' + '## Certified Architecture Context'. Fix = deterministic producer-side dedup: add omitGoverningSections to renderHydratedPacket (skip ONLY those two section blocks; keep supersession chains, contradictions, known-conflict-zones, material findings, and keep the constraint-id findings dedup); buildPhaseContextPacket keeps the DISK detail file FULL (P9 reads it) but returns an omit-governing variant as detailFileContent when a new opt-in flag inlineOmitsGoverning is set. phase6.ts task_skeleton call opts in; the P9 executionScheduler.fetchDmrPacketForTask call does NOT (P9's active_constraints is task-scoped id-refs per executionContextBuilder.ts:869, so it must keep governing bodies in detail_file_content — see the explicit slice-151 comment at executionContextBuilder.ts:849-858). Scope the concrete edit to task_skeleton (PA-13's named phase); the identical one-line opt-in generalizes to phases 2/3/4/5/7/8 as a follow-up.
- **files/functions:**
  - `JanumiCode/janumicode_v2/prompts/phases/phase_06_implementation_planning/implementation_task_decomposition.system.md` — **[INPUT] '# Active constraints' slot (lines ~229-231)**: Delete the duplicate '{{active_constraints}}' injection in the [INPUT] section; replace with a one-line reference: '(Governing constraints are stated once at the top under GOVERNING CONSTRAINTS — apply without exception; not repeated here.)'. Keep the single authoritative occurrence at line 24-25. Fixes 2a. Zero code risk (variable still referenced once).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/dmrHydration.ts` — **renderHydratedPacket + HydrateOptions**: Add 'omitGoverningSections?: boolean' to HydrateOptions; gate the two section pushes at lines 93-102 ('## Governing Constraints (binding...)' and '## Certified Architecture Context') behind !opts.omitGoverningSections. Leave supersession/contradiction/known-conflict-zone/material-finding sections AND the renderedAboveIds constraint-id findings dedup (141-172) intact. Fixes 2b (producer capability).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/dmrContext.ts` — **buildPhaseContextPacket + PhaseContextPacketOptions**: Add 'inlineOmitsGoverning?: boolean' (default false). Keep the disk-file hydration full (line 143 unchanged — P9 path reference). When the flag is set, compute a second renderHydratedPacket(packet, resolve, { omitGoverningSections: true }) and return THAT as detailFileContent (line 184) instead of detailFile.content; when unset, return full as today. Deterministic bridge — governing delivered exactly once per prompt.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **PhaseSix.execute — buildPhaseContextPacket({ subPhaseId: 'task_skeleton' }) call (~line 163)**: Pass 'inlineOmitsGoverning: true' (task_skeleton renders active_constraints at the top). Do NOT touch executionScheduler.fetchDmrPacketForTask:1410 (P9 must keep governing in detail_file_content). Optional same-line generalization to phase2 fr/nfr bloom, phase3, phase4, phase5, phase7, phase8.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/dmrHydration.test.ts` — **new it('omitGoverningSections drops governing/certified but keeps findings + dedup')**: With omitGoverningSections:true assert '## Governing Constraints' and '## Certified Architecture Context' headings are ABSENT while supersession chains, contradictions, and a distinct material finding remain, and a constraint-id finding is still deduped.
  - `JanumiCode/janumicode_v2/src/test/unit/orchestrator/phases/taskSkeletonConstraintsSingleInjection.test.ts` — **new file-reading guard (mirrors PA-11 frNfrSaturationCategoryConsistency.test.ts)**: Read implementation_task_decomposition.system.md and assert (body.match(/\{\{active_constraints\}\}/g) ?? []).length === 1, pinning 2a against recurrence.
- **unit-test plan:** Vitest, src/test/unit/orchestrator/phases/. (2a) New taskSkeletonConstraintsSingleInjection.test.ts — file-read the task_skeleton template, assert exactly ONE '{{active_constraints}}' occurrence (and still contains the top 'GOVERNING CONSTRAINTS (apply without exception)' anchor); mirrors the PA-11 file-reading pattern. (2b) Extend dmrHydration.test.ts: (a) with { omitGoverningSections: true } on a packet carrying both binding + certified_context constraints plus a supersession chain, a contradiction, and one distinct material finding — assert '## Governing Constraints' and '## Certified Architecture Context' headings are absent, but 'Supersession Chains', 'Contradictions', and the distinct finding are present; (b) a constraint whose id also appears as a material finding is still deduped (dedup independent of section rendering); (c) regression: without the option (default), both governing headings still render (existing tests at lines 52-96 already cover this, so default behavior is pinned). No dmrContext.ts unit test (engine/DB-coupled) — the pure renderHydratedPacket + template file-read tests are the decisive guards; the dmrContext plumbing is exercised by existing phase-6 integration/regression fixtures.

### PA-15 — Monolithic test_case_skeleton (P7.1) → per-component chunking + orchestrator-owned AC-coverage reconciliation loop (mirror P6.1)
- **status:** confirmed · **risk:** medium · **effort:** L · **deterministic-bridge:** true · **deps:** PA-3, PA-9 · **live-validation:** true · **confidence:** 0.85
- **approach:** CONFIRMED against current code. `Phase7Handler.runTestCaseGeneration` (phase7.ts:406-454) makes ONE `callForRole` rendering the WHOLE plan into `test_case_generation.system.md`: `functional_requirements_summary` (full leaf-FR view w/ all ~303 ACs), `component_id_list` (ALL components), `component_model_summary` (full), and the template demands "every AC has a test case" + "every component listed must appear as a suite.component_id" in one response. This is the exact monolithic-output disease P6.1 had pre-fix: the output size (a suite-per-component + a case-per-AC in one shot) makes local dense models loop/short-cut → ~28% AC coverage (backlog A3/C2). The shortfall then flows through SILENTLY: `runCoverageAnalysis` (phase7.ts:463) computes AC gaps but is visibility-only (feeds `has_unresolved_warnings`, gate never fails on it, phase7.ts:393); `backfillMissingComponentSuites` (phase7.ts:506) closes only COMPONENT gaps and does so with EMPTY stub suites (placeholder TC with `acceptance_criterion_ids: []` → covers ZERO ACs); and 7.1a saturation only refines/fans-out the roots that already exist, it never mints coverage for an AC that got no case at all. So there is no closed loop that guarantees AC coverage.\n\nFIX (mirror the proven P6.1 redesign — "chunk the reasoning small, let deterministic code own 100% via iterate-until-covered; orchestrator owns the deterministic axis, LLM owns judgment"):\n1. Per-component chunking: loop leaf components, ONE bounded `callForRole` each (label `Phase 7.1 — Test Case Generation (<cid>)`), dedup suite/test-case ids, merge — replacing the single monolithic call.\n2. Deterministic per-component AC scoping (P7's advantage over P6): build `componentAcMap: componentId → Set<AC-*>` from `prior.implementationPlan.content.tasks` (each task's `component_id` × `traces_to` AC ids — P6 already produced this binding). Each per-component call sees ONLY its component's ACs (kills the full-catalog firehose AND wrong-component binding). Fall back to the FULL leaf-AC menu for any component absent from the map (never starve).\n3. Orchestrator-owned coverage reconciliation loop (the 100% guarantee): after per-component generation, `computeUncoveredTestAcIds` over emitted cases' `acceptance_criterion_ids` vs the full leaf-AC set → `chunkUncoveredByStory` into bounded batches → one `reconcileUncoveredTestAcs` call per batch (new `test_case_reconciliation` template) → accept only cases covering a still-uncovered AC → repeat until clean or budget spent (env `JANUMICODE_P7_RECON_PASSES` default 2, `JANUMICODE_P7_RECON_BATCH_AC` default 25). Residual logged HONESTLY via `summarizeResidualDivergence` (no fabrication) — same as P6.1.\n4. Reuse the already-exported P6 helpers (`collectLeafAcceptanceCriteria`, `chunkUncoveredByStory`, `renderUncoveredAcsMenu`, `renderComponentMenu`, `renderComponentBlockForTask` [already imported in phase7.ts], `summarizeResidualDivergence`) so this is mostly wiring + a template pair, not new machinery. Keep the existing phase-exit corrections downstream (normalizeTestPlanAcRefs, backfillMissingComponentSuites as component-coverage backstop, gatekeeper) unchanged.
- **files/functions:**
  - `JanumiCode/janumicode_v2/prompts/phases/phase_07_test_planning/test_case_generation.system.md` — **template (sub_phase: test_case_skeleton)**: Rewrite to be COMPONENT-SCOPED: 'generate test suites for exactly ONE component — the one in Component Model Summary below; cover the ACs it implements'. Replace the all-component `component_id_list` coverage mandate + `functional_requirements_summary` firehose with a scoped `acceptance_criteria_menu` variable (this component's ACs) reframed as reference-lookup; state global coverage is closed by the orchestrator, not this call. Mirror implementation_task_decomposition.system.md's single-component framing. (While here: fix the stray `"type": "functional"` example on L41 to the real union unit|integration|end_to_end|property — adjacent PA-11/PA-12-class contradiction that weakens every emitted case.)
  - `JanumiCode/janumicode_v2/prompts/phases/phase_07_test_planning/test_case_reconciliation.system.md` — **NEW template (sub_phase: test_case_reconciliation)**: New file mirroring implementation_task_reconciliation.system.md: required_variables active_constraints, uncovered_acceptance_criteria, component_menu, plus any interface/precondition context; instruct 'emit test suites/cases that cite EVERY uncovered AC id (verbatim) in acceptance_criterion_ids; route each AC to its best-fit component'. Strict-JSON top-level `test_suites`. Same array-safety wording (acceptance_criterion_ids is an array of AC-* strings, never a boolean).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase7.ts` — **Phase7Handler.runTestCaseGeneration**: Rewrite from single monolithic call to: per-component loop (one bounded callForRole each, scoped AC menu from componentAcMap with full-menu fallback, dedup suite_id/test_case_id via a seenIds set, merge) → coverage reconciliation loop (computeUncoveredTestAcIds → chunkUncoveredByStory → reconcileUncoveredTestAcs per batch → credit only cases covering a still-uncovered AC → residual via summarizeResidualDivergence). New signature accepts leafAcceptanceCriteria: LeafAcceptanceCriteria[], leafComponents, componentAcMap, dmr. Directly mirrors runTaskDecomposition (phase6.ts:643-774).
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase7.ts` — **Phase7Handler.reconcileUncoveredTestAcs (NEW private)**: Mirror reconcileUncoveredAcs (phase6.ts:782-836): render test_case_reconciliation template with renderUncoveredAcsMenu(batch, leaves) + renderComponentMenu(components); label `Phase 7.1 — Test Coverage Reconciliation (pass P, batch i/N, K ACs)`; parse via parseTestSuites; confirm-parse logging (parse_failed vs empty); return [] on failure.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase7.ts` — **buildComponentAcMap / computeUncoveredTestAcIds / parseTestSuites / caseCoversAny / renderScopedAcMenu (NEW module helpers, exported)**: buildComponentAcMap(tasks): componentId → Set<AC-*> from implementationPlan tasks' component_id × traces_to (AC-prefixed only, array-safe). computeUncoveredTestAcIds(suites, leafAcIdSet): leaf ACs cited by no test_case.acceptance_criterion_ids. parseTestSuites(parsed): tolerant envelope ({test_suites}, {test_plan:{test_suites}}, {test_plan:[...]}) reusing pickItemsArray. caseCoversAny(tc, idSet): tc.acceptance_criterion_ids ∩ idSet. renderScopedAcMenu(cid, componentAcMap, leaves): only this component's ACs grouped by story, fall back to full renderAcceptanceCriteriaMenu when empty.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase7.ts` — **Phase7Handler.execute (call site ~line 91-152)**: Reuse the already-fetched `decompositionNodes` to call collectLeafAcceptanceCriteria(decompositionNodes) (import from phase6) for the LeafAcceptanceCriteria structure; build componentAcMap from prior.implementationPlan?.content.tasks; pass both + componentIds into the new runTestCaseGeneration. No change to the downstream normalizeTestPlanAcRefs / backfillMissingComponentSuites / gatekeeper / 7.1a seeding.
  - `JanumiCode/janumicode_v2/src/lib/orchestrator/phases/phase6.ts` — **chunkUncoveredByStory, renderUncoveredAcsMenu, renderComponentMenu, summarizeResidualDivergence, collectLeafAcceptanceCriteria (already exported)**: No change — import and reuse in phase7.ts. Confirms the mirror is mostly wiring.
- **unit-test plan:** New Vitest file src/test/unit/orchestrator/phases/phase7TestCaseChunking.test.ts (model after phase6LeafAcBinding.test.ts + phase7_1aSaturation.test.ts). Pure-helper tests (no LLM): (1) buildComponentAcMap — tasks {component_id, traces_to} → map; drops non-AC ids; array-safe on missing/boolean traces_to. (2) computeUncoveredTestAcIds — suites whose cases cite a SUBSET of the leaf-AC set → returns exactly the uncovered remainder; dedups; ignores unknown ids. (3) parseTestSuites — tolerates {test_suites:[...]}, {test_plan:{test_suites:[...]}}, {test_plan:[{...}]}, and null→[]. (4) renderScopedAcMenu — for a mapped component renders ONLY that component's ACs and NOT other components' ACs; for an unmapped component falls back to the full menu. (5) caseCoversAny — true iff acceptance_criterion_ids ∩ idSet. Integration tests with MockLLMProvider + real OrchestratorEngine (as in phase7_1aSaturation.test.ts): seed decompositionNodes (leaf FR stories w/ ACs) + an implementation_plan artifact (tasks binding components→ACs) + 3 components; mock returns per-component suites covering only a SUBSET → assert (a) exactly one generation call PER component and each rendered prompt contains only that component's ACs (no single prompt carries all components' ACs — the anti-monolith regression assertion), (b) reconciliation fires for the orphan ACs and merged plan reaches 100% leaf-AC coverage, (c) suite_id/test_case_id dedup across chunks, (d) when recon budget is exhausted the residual is logged (summarizeResidualDivergence) with NO fabricated case, (e) component absent from componentAcMap falls back to the full AC menu (no starvation). Assert defect cannot recur: no monolithic all-component/all-AC prompt is ever rendered.

