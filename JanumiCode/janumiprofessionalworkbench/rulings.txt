====================================================================================================
LENS step-mandatoriness-rph   verdict=GESTURES_ONLY   overturned=True
-- SUMMARY --
Nothing in the corpus makes a plan STEP mandatory — but the corpus is not blankly silent on mandatoriness, and the sponsor is not choosing in a vacuum. Four separate ratified places define what "mandatory" means and how it is carried, in the corpus's own idiom: `ObligationObject.strength: 'MANDATORY'|'CONDITIONAL'|'ADVISORY'` (RPH-DOC-002 §10.1), `ConstraintObject.strength: 'MANDATORY'|'PREFERRED'|'ADVISORY'` (§11.1), PWU-Type "permitted, mandatory, and conditional child-type rules" backed by min/max cardinality + an applicability predicate (Guide §11.7.2, canon DOC-002 §2, authorable in RPH-DOC-010 §14), and `ApplicabilityOutcome = REQUIRED|RECOMMENDED|OPTIONAL|…` (RPH-DOC-004 §5.2). Every one of them attaches to a PROFESSIONAL object. None attaches to an ExecutionStep, and the architecture explains why: `ExecutionStep` is the one execution shape that is NOT a Professional Work Object — it does not `extend ObjectEnvelope`, `EXECUTION_STEP` is absent from §4's twenty-member `ProfessionalWorkObjectType` union (which does contain `EXECUTION_PLAN` and `RUNTIME_BINDING`), and `execution_steps` is the one execution table whose id does not reference `professional_work_objects`. Obligations hang off the PWU (`obligationIds`, §7.1) and §20.2 forbids a plan from touching them; the Guide states twice that an activity lacking a professional boundary "remains an Execution Step/Attempt."

So the four derivation routes named in the brief are each closed, and closed for a stated reason, not for want of searching. (a) From an Obligation allocated to the step — no such allocation exists: `ObligationAllocation` allocates to child WORK UNITS (§13.1), and steps carry no obligation reference in any of the three step shapes the corpus declares. (b) From a Condition — `Condition`, the declared type of `preconditions`/`postconditions`, is never defined anywhere in the corpus; the carrier is itself undefined. (c) From `stepType` — the nine values are node kinds (MODEL_INVOCATION … PARALLEL_GROUP); no document assigns any of them a weight, and RPH-DOC-010, the PWA/Undertaking authoring surface, contains the word "step" ZERO times. (d) From the PWU's obligations — §20.2 severs exactly this link.

Two further facts change the shape of the escalation. First, the §21.1 bullet immediately above the rule (RPH-EXE-005) and the one immediately below (RPH-EXE-006) both have conformance invariants in RPH-DOC-008; "skip" returns ZERO in that document, and ZERO in RPH-DOC-007. §21.1's mandatory-skip bullet is the only one of the six with neither a conformance invariant nor a wire command — s
-- WHAT THE SPONSOR MUST DECIDE --
Not "is there a gap" — there is, and REG-F-105's one-sentence framing of it is accurate. What the widened search changes is the menu.

ROUTE A — put the fact on the step. Add an optionality field to `ExecutionStep` and ratify a writer. The corpus supplies the vocabulary to copy verbatim (`strength: 'MANDATORY'|'CONDITIONAL'|'ADVISORY'`, §10.1) rather than a fresh boolean, which would also survive the `mandatory:false` bypass better than a two-valued flag. COST, and it is real: it makes the step the first non-Professional-Work-Object to carry a governance-bearing fact. §4's union would still exclude it, it would still not extend `ObjectEnvelope`, and `execution_steps` would still not reference `professional_work_objects` — so the field could never be version-bound to a subject the way ASR-12 requires of everything else that gates an authorization. The repository already walked into this once and recorded it (HARMONIZATION-LOG: "minted a version for a non-object to make a check pass — the ceremony trap").

ROUTE B — put the fact where the corpus already puts mandatoriness, and derive. Make a step mandatory iff it discharges a MANDATORY Obligation of its PWU — which needs one new ratified relation (a step→obligation reference, the mirror of §13.1's `ObligationAllocation` to child work units) and no new vocabulary at all. This is the only route where the authorizing act already exists: §10.2's "A waived mandatory obligation requires an authorized waiver" is the same act §21.1 demands, already ratified, already having a decision type. It also makes the WHY true — "laundered de-s
-- REFUTER --
The report's HEADLINE holds and I strengthened it: nothing in the corpus makes a plan STEP mandatory. My control pair proves it is a fact about the corpus, not the search — same regex shape, same files (docs/Recursive Professional Harness/ + docs/Constitution Discussion/ + the Guide): `mandator.{0,100}obligation|obligation.{0,100}mandator` returns 25 hits; `mandator.{0,100}\bstep|step.{0,100}mandator` returns exactly ONE, and it is the rule itself (RPH-DOC-002 L1308).

I set overturned=true because I found ratified corpus material the report missed that (a) falsifies three of its load-bearing sub-claims and (b) changes the sponsor's menu — specifically on the one axis the brief told me to attack, "whether the concept is expressed as a RELATIONSHIP rather than a field."

1. THE STEP-TO-OBLIGATION RELATIONSHIP EXISTS IN RATIFIED CORPUS PROSE. The report's finding #4 asserts obligations attach "to the PWU, never to a step," and its Route B says deriving step mandatoriness from an obligation "needs one new ratified relation." RPH-DOC-006 §29.5 states the relation, and states its CARDINALITY: an execution step is an operational action used to satisfy ONE obligation. Composed with §10.1's already-ratified `strength: 'MANDATORY'`, both premises of Route B are ratified today. The report searched for a FIELD (`ObligationAllocation`, `obligationIds`) and concluded the concept was absent — the REG-F-069 failure mode exactly.

2. CANON DID DROP IT, WHICH THE REPORT EXPLICITLY FORECLOSED. Its finding #10 says the asymmetry "is in canon, not an artifact of distillation loss — which forecloses the 'canon just dropped it' hypothesis." Canon's DEC-1 (JPWB-DOC-003 L211) carries the PWU half of DOC-006's decision rule and drops the step half — while canon's OWN extract (docs/canon/_extrac
-- REFUTER FOUND --
(1) THE STEP→OBLIGATION RELATION, WITH CARDINALITY — the report denied this exists in any form.
"Use child PWU Instances for distinct professional obligations.
Use execution steps for operational actions used to satisfy one obligation."
docs/Recursive Professional Harness/Janumi Professional Workbench Field Service Management SaaS Reference Undertaking.md:1638 and :1640. Heading chain read from the file, not inferred: "## 29.5 Execution-plan and PWU Instance boundaries may blur" (L1632) → "### Recommended decision rule" (L1636). This file is RPH-DOC-006 per docs/Recursive Professional Harness/JAN-ROADMAP-001-v2/detailed-roadmaps/W0/evidence/document-manifest-and-source-authority.md:16 ("| `RPH-DOC-006` | Field Service Management SaaS Reference Undertaking | Reference implementation fixture |").

(2) CANON'S OWN EXTRACT CAPTURED IT; CANON'S DEC-1 DROPPED THE STEP HALF.
"- \"Use child PWU Instances for distinct professional obligations. Use execution steps for operational actions used to satisfy one obligation.\" (RPH-DOC-006.md L1639-1641) — The decision rule for the PWU-vs-execution-step boundary blur (friction 29.5)."
docs/canon/_extracts/extract-doc006.md:22
versus canon proper, which keeps only the PWU half:
"**DEC-1 · The PWU boundary test.** Do not create a PWU for prompt rendering, retrieval, an API call, formatting, a database write, a retry, or a UI click. Those are exe

====================================================================================================
LENS step-mandatoriness-elsewhere   verdict=GESTURES_ONLY   overturned=True
-- SUMMARY --
The corpora outside `docs/Recursive Professional Harness/` do NOT define what makes an execution-plan step mandatory — but they are far from silent, and the escalation should not be framed as "the corpus never considered this." It was considered, at least four times, in the two highest-prior places, and each time it was DELIBERATELY DEFERRED with the reason recorded.

Four things the sponsor does not currently have in REG-F-105's framing:

1. **It is a recorded deferral, not an oversight.** `JAN-EXECPLAN-DR-002 §15` refused to author a per-step `required` flag because it is `Source-TBD`; `DR-003 §15` lists "a step-level `mandatory`/`ordinal` contract field" in its Deferrals; `JAN-EXECPLAN-DS-003 §6` surfaced it as a "grammar-independent fact"; and `JAN-EXECREM-design-working-papers` §F2 Open questions classifies it in terms: *"it is a §21.1 governance question, not an interpreter question."* The sponsor is being asked to CLOSE a deferral the programs opened on purpose, not to invent from nothing.

2. **The repository already operates a de-facto answer: every step is mandatory.** `DR-002 §15` discloses that §20.1's *"all required steps reach terminal success"* is implemented as `every(SUCCEEDED||SKIPPED)` — i.e. no step may simply not run. Combined with `mandatory ?? true`, the operative semantics today are "all steps mandatory; the only exit is a governed skip." So the ruling "there is no per-step optionality" requires DELETING a field, not adding one — strictly the cheaper act, and REG-F-105 does not say so.

3. **A corpus-sanctioned non-waiver route to retiring a step already exists, and it is not optionality.** `JAN-EXECPLAN-DS-004 §6 D5` defines step retirement by GRAPH NON-LIVENESS (all in-edges NEUTRALIZED → `PruneExecutionStep`), and explicitly keeps it out of the waiver plane: *"a not-taken conditional arm is excluded by the plan's own declared logic, not skipped by a controller."* "Optional step" may be the wrong primitive; "unreachable step" is already designed. Caveat: the T3C-ii review found this route exploitable (a single contract-legal edge converts every mandatory step into a waiver-free skip), so leaning on it requires hardening.

4. **Two worked analogues exist in-repository that the sponsor can adopt rather than draft.** `JAN-CSAA-010-RSK-004/005` answers the identical question shape for a different object: mandatory-ness *"SHALL be derived from exact applicable profiles and affected concern rather than tool availability or convenience"*, and discretionary work *"MAY be omitted only when its nonapplicability or lower evidentiary value 
-- WHAT THE SPONSOR MUST DECIDE --
Not one question but three, in this order — and the first may dissolve the other two.

Q1 (cheapest, and the one REG-F-105 omits): **Is there any such thing as an optional execution-plan step?** The repository already answers NO in the completion rule — `JAN-EXECPLAN-DR-002 §15` implements §20.1's "all required steps" as `every(SUCCEEDED||SKIPPED)`, so no step may simply not run. If the sponsor ratifies that reading, the act is to DELETE `mandatory` from `SkipExecutionStepPayloadSchema` (every skip then requires an authorization), amend STA-8's wording to drop the qualifier "mandatory", and pin REG-F-105's admission test in its inverted form. No contract field is authored, no new meaning is minted, and the caller-assertion hole closes by removal. This option is not on REG-F-105's ballot and is strictly the smallest.

Q2, only if Q1 is NO (optionality is real): **who supplies the fact, and from what?** Two candidates, and they are not equally supported.
  (a) The plan author asserts it at proposal. This is the escalated defect moved one command earlier — the party who benefits still supplies the input — unless it is bound by an authorized plan approval. The corpus offers no derivation basis for it.
  (b) It is DERIVED from the obligation the step serves. Only this one has scaffolding, in two halves the corpus never joins: RPH-DOC-006's boundary rule (via `canon/_extracts/extract-doc006.md:22`) — an execution step is "operational actions used to satisfy one obligation" — and the ratified `ObligationStrength ∈ {MANDATORY, CONDITIONAL, ADVISORY}` bound to `ObligationObject.stre
-- REFUTER --
THE LENS VERDICT SURVIVES — do not over-read `overturned`. I could not find any text, anywhere, that says which execution-plan steps are mandatory or how to determine it. GESTURES_ONLY stands as the answer to the question asked. What is overturned is (i) the report's characterisation of the corpus as merely gestural on the ADJACENT question — Guide §8.4 RULES OUT the caller-assertion mechanism outright, which is answer (d), not (c) — and (ii) two of the report's own load-bearing claims, one of which is its self-declared "DECISIVE" finding.

REFUTATION 1 — THE REPORT'S FINDING #3 RESTS ON A QUOTE THAT IS NOT IN THE CORPUS. The report tells the sponsor: "`DR-002 §15` discloses that §20.1's *'all required steps reach terminal success'*…" and calls it "DECISIVE for the cheapest ruling", building its whole Q1 on it. I read the heading. RPH-DOC-002 "## 20.1 Execution Plan Object" (…Canonical Domain Model…md:1219) is a TypeScript interface and nothing else; "## 20.2 Execution Plan invariants" (:1248) has eight bullets and none is a completion condition; there is no ExecutionPlan state-machine section anywhere in the file (§27-§30 are Command Model, Concurrency, Invalidation, Legacy Lens). The phrase's actual origin is a REPOSITORY VOCAB FILE, byte-exact:

packages/rph-domain/vocab/m11-execution.json:37 — `{ "value": "COMPLETED", "meaning": "All required steps reached terminal success; plan finished.", "sourceRef": "§20.1; Contract §15" }`

and the same file admits three lines later, at :43 — `"note": "No dedicated ExecutionPlan transition table exists in the docs; the machine is the status enum (§20.1) + execution commands (§34.3) + execution events (§26.4) + the PWU lifecycle rows that gate on plan state (§8.1)."` The EXECPLAN documents cite it as `m11:37,50` (DR-002:49; DS-0
-- REFUTER FOUND --
FOUR pieces of text the report missed, each byte-exact with the heading read directly above it.

[1] The corpus RULES OUT (option d, not option c) the exact mechanism REG-F-105 complains about — in a document the report searched but at a section it never opened. Heading: "### 8.4 De minimis assurance floor and control planning" — e:/Projects/hestami-ai/JanumiCode/janumiprofessionalworkbench/docs/Janumi Canonical Implementation Context - Coding Agent Guide.md:835. Three sentences under it:

:837 — "Risk proportionality governs assurance above a mandatory floor; it does not make the floor optional."
:844 — "A result is nonmaterial only when a versioned rule establishes lossless semantic equivalence; the producer cannot exempt its own output, and ambiguity resolves to material."
:867 — "A planner may optimize optional controls but cannot weaken mandatory applicability or independence."

The report's whole file list for this document is "full heading map, §9.1, §9.2, §11.7.2, §16 do-not-guess register (all 25 items), §17 source map." §8.4 is not on it. :867 names the PLANNER — the exact role that authors an execution plan — and says it may optimise OPTIONAL controls but may not weaken MANDATORY applicability. :844 states the general form of the defect ("the producer cannot exempt its own output") and its fail-closed default ("ambiguity resolves to material") in one sentence. This i

====================================================================================================
LENS decision-subjects   verdict=CORPUS_DEFINES_IT   overturned=True
-- SUMMARY --
The corpus is NOT silent on either half, and on one limb of REG-F-106 it actively RULES OUT the option the entry offers.

(1) MAY A DECISION EXIST WITHOUT SUBJECTS — DEFINED, and defined precisely. Ratified canon (DOC-003 §8.7 ASR-15) does not forbid the record; it names what such a record IS: "An approval whose actor, subject, subject version, type, and time cannot be identified is not authority — it is provenance at best, and re-decision is required." A subjectless Decision is a legal artifact and a null authority. REG-F-106's own sentence ("a subjectless one is not an authorization in the operative sense at all") is a correct reading of canon, and the disclosure it shipped is the corpus-prescribed treatment, not an invention.

(2) THE "THREE EXEMPT TYPES" PREMISE IS REFUTED — this is the most consequential finding. REG-F-106 offers keeping a propose form "scoped to the three types no gate reads" (ESCALATION, RESHAPE, REVOKE). No corpus text exempts any decisionType from naming a subject, and four independent texts cut against it: DOC-002 §3 defines Decision as authority exercised over "approval, rejection, waiver, escalation, reshaping, replanning, risk acceptance, or promotion" — escalation and reshaping are named exercises of authority, not lesser records; ASR-15's SCOPE clause says it "governs governance acts" and folds escalation and proposal into the governed channel rather than outside it; RPH-DOC-004 §13 requires an escalation package to include a "subject"; and ASR-15 plus RPH-GOV-007 make revocation trigger impact analysis, which has no referent without a subject. Decisive structural control: in BOTH ratified DecisionObject contracts the author marked `effectiveAt?` optional in the same code block while leaving `subjectObjectIds: string[]` unmarked. The corpus knew how to make a Decision field optional and did not do it to this one. That option should not reach the sponsor as live.

(3) WHERE AN AUTHORIZATION IS AUTHORED — the corpus HAS a view, and it is two-sided, which changes REG-F-106's binary. A general "decision center" is not merely envisaged, it is named by that exact phrase in a RATIFIED artifact and blessed: SPEC-001 §3.2 INV-02's NON-EXAMPLE calls "a Decision Center listing governance acts across the whole workbench" legitimate and conformant, and CONFIRMS `/decisions` is exactly such a surface. RPH-DOC-010 §4.7 goes further — the Decision Center is "Used to exercise authority over versions, waivers, escalations, and acceptance", i.e. authoring, not merely listing. So "remove the propose form" has NO corpus warrant, and "delete th
-- WHAT THE SPONSOR MUST DECIDE --
REG-F-106's escalation should be RE-FRAMED before it reaches the sponsor, because one of its two options is refuted and a third is corpus-described.

WHAT IS NO LONGER OPEN (do not ask):
1. Whether ESCALATION / RESHAPE / REVOKE are exempt from naming a subject. They are not. DOC-002:133 names escalation and reshaping as exercises of authority; ASR-15's SCOPE governs escalation; RPH-DOC-004 §13 requires a subject in an escalation package; revocation triggers impact analysis, which needs a referent; and both DecisionObject contracts leave subjectObjectIds unmarked while marking effectiveAt optional in the same block. REG-F-106's option (b) — "keep a propose form scoped to the three types no gate reads" — is asking the sponsor to ratify something the corpus already refuses. Its premise, that those three types are gate-free, is a fact about the repository's gates, and REG-F-102 is the standing proof that a gate census is not a corpus census.
2. Whether /decisions may be deleted. No: SPEC-001 §3.2 blesses a Decision Center by name as legitimate and conformant; FORK-10's standing rule forbids removing a top-level Surface until it is ruled; and RPH-DOC-010 §4.7 gives the Decision Center an authority-exercising job, not a listing job.

WHAT THE SPONSOR MUST ACTUALLY DECIDE:
Q1 (the real REG-F-106). Should the /decisions propose form be (i) REMOVED, leaving a register that lists/approves/grants/denies; or (ii) REPAIRED — kept as the Governance context's authoring surface but made to name a subject, selecting from the objects the Decision Center already lists? The corpus supports (ii
-- REFUTER --
The report's VERDICT (CORPUS_DEFINES_IT) survives — but four of its load-bearing supports are wrong or missable, and I found on-point text in three documents it never opened. Two of my finds refute its own "decisive" control.

**1. ITS DECISIVE STRUCTURAL CONTROL IS DEFEATED — twice.** The report rests limb (2) on: "in BOTH ratified DecisionObject contracts the author marked `effectiveAt?` optional in the same code block while leaving `subjectObjectIds: string[]` unmarked. The corpus knew how to make a Decision field optional and did not do it to this one."
 (a) CPCO §5.16 lists `effectiveAt` among a Decision's **Required properties** and lists **no subject at all** — neither as a required property nor in its "A material Decision SHALL identify" relationship list. The two documents disagree about `effectiveAt`, so the `?` marker is not the doctrinal ledger the report treats it as.
 (b) More basic: `subjectObjectIds: string[]` **does not forbid `[]`**. Non-optionality requires the KEY to be present, not the array to be non-empty. `subjectObjectIds: []` fully satisfies that contract. The report's control cannot bear the weight of "no decisionType is exempt from naming a subject."

**2. BUT THE CORPUS STATES THE ACTUAL RULE, and the report never found it.** It inferred structurally where ratified canon states it outright. DOC-003 OBJ-1 does two things the report's cited AX-6 does not: it names **empty arrays** specifically (AX-6's list — "null fields, missing values, attachment, proximity, ordering, storage location, or UI placement" — does not), and its SCOPE clause supplies the requiredness test: "requiredness is justified only where absence would cause semantic ambiguity, never by implementer convenience." Its source is Contract Package §4.2. So the right argument is: `
-- REFUTER FOUND --
**[1] RATIFIED CANON — the empty-array clause and the requiredness test the report never found.**
File: docs/canon/JPWB-DOC-003 Semantic Model and Invariant Catalog.md, lines 105 and 107.
Heading read from the file: "## 3. Core objects and minimum rules" (line 75). Nearest preceding headings verified: "## 2. The five-layer semantic model" (53), "## 3. Core objects and minimum rules" (75) — no intervening heading before 105.
L105: "**OBJ-1 · Semantic state is always explicit.** No semantic state may be inferred from null values, empty arrays, missing rows, absent output, legacy phase order, event ordering alone, UI position, or agent prose. Illegal or incomplete states are represented explicitly. An omitted optional field means the field is not part of that message — not unknown, false, empty, or inapplicable."
L107: "**SCOPE:** governs semantic state wherever it is read or persisted. **NON-EXAMPLE:** this does not require every optional wire field to become a status enum; requiredness is justified only where absence would cause semantic ambiguity, never by implementer convenience."

**[2] The source rule OBJ-1 distils.**
File: docs/Recursive Professional Harness/Janumi Professional Workbench Recursive Professional Harness - Command, Event, Schema Contract Package.md, lines 302-306.
Heading read from the file: "## 4.2 Required fields" (line 302), within "# 4. Schema Standard" (l

====================================================================================================
LENS already-ruled   verdict=RULES_IT_OUT   overturned=True
-- SUMMARY --
Both escalations should be re-drafted before they reach the sponsor, and neither should go as an open binary — because the corpus rules out one horn of each.\n\nQ1 (mandatory execution step). REG-F-105's narrow claim SURVIVES: nothing in the corpus attaches optionality to an ExecutionStep. But three things around it do not survive. (1) The corpus is not silent on the CONCEPT — it defines mandatoriness five times, always the same way: `Obligation.strength: MANDATORY|CONDITIONAL|ADVISORY` and `Constraint.strength: MANDATORY|PREFERRED|ADVISORY`, each carried beside an `authority: AuthorityReference` (CDM §10.1/§11.1); `enforcementLevel: mandatory|strong_preference|preference|advisory` (CPCO §5.10 — the second spelling REG-F-105's field-name search could not see); a literal `mandatory: Boolean` on CompletionCondition authored in the PWU framing contract (JSDL / PWU Aggregate §12.1); and `ApplicabilityOutcome REQUIRED|RECOMMENDED|OPTIONAL|…` computed from a rule on the policy. In every instance the strength is a declared property of the governed object set by an authority — never a claim by the actor performing the act. So 'who sets it' is not open; only 'which of these five shapes' is. (2) The corpus RULES OUT the second horn. Guide §8.4: 'the producer cannot exempt its own output, and ambiguity resolves to material', plus 'Risk proportionality … does not make the floor optional' and 'No PWA profile, low-risk classification, planner optimization, or local agent instruction may suppress this Reasoning Review floor'. `p.mandatory ?? true` read from the skipper's payload is exactly a producer exempting its own output. Ratifying the caller-asserted reading would overturn that sentence, not merely narrow STA-8. (3) THE QUESTION IS NOT NEW. JAN-EXECPLAN-DR-003:41 recorded on 2026-07-24 that `mandatory` 'has no source on the step (no contract field)', chose caller-asserted-with-fail-closed as a disclosed reading, and DEFERRED 'a step-level `mandatory`/`ordinal` contract field' as a named 3C-ii item (line 225). Under CON-000 B1 that document holds HYPOTHESIS authority in its program's scope. This is a request to activate a deferral, not a fresh contract question.\n\nAlso NEW and unrecorded: a SECOND site depends on step-requiredness. The plan-completion predicate 'all required steps reach terminal success', cited as ratified §20.1 in `m11-execution.json:37` and in JAN-EXECPLAN-DR-002/DS-002, appears NOWHERE in the corpus — CDM §20.1 is the interface block and contains no completion sentence. Whatever is ruled governs two consumers, one of which currently rests on a 
-- WHAT THE SPONSOR MUST DECIDE --
Not the two questions as drafted. Re-put them as follows.\n\nQ1 — ONE question, not a binary. 'ExecutionStep gains a strength/optionality property in the shape the corpus already uses four times (a declared value carried with an authority, set at plan proposal or derived from the PWU's obligations); the caller-asserted reading is not available because Guide §8.4 forbids a producer exempting its own output. Which carrier: (a) `strength: MANDATORY|CONDITIONAL|ADVISORY` on the step, mirroring Obligation/Constraint; (b) derived — a step is mandatory iff it discharges a MANDATORY obligation or invokes a policy whose ApplicabilityOutcome is REQUIRED; or (c) a `mandatory: Boolean` on the step mirroring CompletionCondition, set in the framing/proposal act?' Attach three riders: this activates the already-recorded 3C-ii deferral in JAN-EXECPLAN-DR-003:225 rather than opening a new one; the answer also governs the plan-completion predicate, whose 'all required steps reach terminal success / §20.1' citation does not exist in the corpus and needs its own disposition; and §21.1's mandatory-skip invariant is the only one of its six with no RPH-EXE conformance test, so the ratification should mint one.\n\nQ2 — a narrower question and a cheaper prior one. Narrower: 'The Decision Center stays a register (list, approve, grant, deny) and the propose action is retired from it, because a consequential control must name its object and every decisionType — including ESCALATION, RESHAPE and REVOKE — is defined over subjects. Confirm, or state the exception.' Do NOT offer the 'scope it to three typ
-- REFUTER --
I attacked all five vectors. Q1's narrow absence SURVIVED my attempt to break it; Q2 did not — I found definitional text the report never quoted, and it changes the shape of the answer.

WHAT I OVERTURNED (Q2). The report reaches the right verdict by the wrong route, and the route matters because a sponsor can refuse an assembled argument. Its case that the second horn ("keep a propose form scoped to ESCALATION/RESHAPE/REVOKE") is refused rests on (a) the Guide §11.5 interaction grammar, and (b) an assembly of per-type invariants gathered from three separate documents — CDM §23.2 for WAIVER and REVOKE, JEM §42.2/§42.3 for ESCALATION. For RESHAPE it offers no citation at all, only the bare assertion "RESHAPE reshapes a named PWU". That is the one limb a sponsor could push back on, and it is the limb the report could not source.

It did not need to assemble anything. `subjectObjectIds: string[]` is a REQUIRED, non-optional field of the ratified `DecisionObject` — in the Canonical Domain Model (§23.1) AND independently in the Command/Event/Schema Contract Package (§22) — sitting three lines below the nine-value `decisionType` enum it governs. The Contract Package adds a second required field, `subjectSemanticVersions`. And `'PROPOSED'` is one of the four statuses of that same interface. So the corpus does not merely rule out a subjectless AUTHORIZATION; it defines no shape for a subjectless DECISION in any status, of any type. RESHAPE is covered by the same line as the other eight. This is category (a) DEFINES, not (b) GESTURES — a stronger answer than the report's, and it collapses seventeen findings' worth of argument into one contract block.

This matters against REG-F-106's own words. That entry says "**ASR-15 binds a decision to exact subjects and versions**; a subjec
-- REFUTER FOUND --
TWO ratified corpus documents make the decision's subject a REQUIRED field of the DecisionObject itself — not an invariant to be assembled per decisionType.

(1) "  subjectObjectIds: string[];"
— docs/Recursive Professional Harness/Janumi Professional Workbench Recursive Professional Harness - Canonical Domain Model, Invariant Catalog, State Machines, and Event Contract.md:1373, inside the block `interface DecisionObject extends ObjectEnvelope {` (line 1365), under heading "## 23.1 Decision Object" (line 1356), inside "# 23. Decisions and Governance" (line 1354). The same interface declares at 1385-1389:
"  status:
    | 'PROPOSED'
    | 'EFFECTIVE'
    | 'REVOKED'
    | 'SUPERSEDED';"

(2) "  subjectObjectIds: string[];
  subjectSemanticVersions: Record<string, number>;"
— docs/Recursive Professional Harness/Janumi Professional Workbench Recursive Professional Harness - Command, Event, Schema Contract Package.md:1632-1633, inside `interface DecisionObject extends ObjectEnvelope {` (line 1618), under heading "# 22. Decision Contract" (line 1615). Its `status:` (line 1645) again opens with `| 'PROPOSED'` (line 1646).

Neither field carries `?`. Both are required, for all nine `decisionType` values enumerated three lines above them (`APPROVAL | REJECTION | WAIVER | ESCALATION | RESHAPE | REPLAN | PROMOTE_BASELINE | ABANDON | REVOKE`, CDM:1358-1367 / Contract:1621-1630), and `PROP
