// C-0b LEDGER DATA — the disposition of every declared arrow guard, one row per distinct text.
//
// GENERATED FROM A CLASSIFICATION PASS AND THEN ADVERSARIALLY REFUTED, not hand-written and not inferred. Five
// agents read the handler behind every arrow; three skeptics then attacked ONLY the ENFORCED claims, because a
// false ENFORCED is the one direction that puts a hole in the ledger while making it read as covered. **Two of
// the sixteen ENFORCED claims were overturned** and are recorded below as such, with the reasoning, rather than
// quietly rewritten.
//
// ⚠ EDITING A ROW IS A CLAIM ABOUT THE CODE. `UNENFORCED` is the safe default and understates the system;
// `ENFORCED` asserts that a production write path REFUSES when the condition is unmet, and the test requires it
// to name the refusing line. Do not promote a row to ENFORCED without driving the refusal.
import type { GuardRow } from './guard-enforcement-ledger.js';

export const GUARD_LEDGER: Readonly<Record<string, GuardRow>> = {
	["a claim must have a subject (§15.2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Claim.status OPEN->UNDER_ASSESSMENT, performed by `recordClaimAssessment` (packages/rph-application/src/handlers/assurance.ts:737). Its only guard is scoped away from this arrow: `guard: (_state, hctx) => { if (target !== 'SUPPORTED') return null;` (assurance.ts:777-778) — RPH-EVD-002, about evidence, not subjects. Nothing loads or counts `subjectObjectIds` on this path. Nor is a subject required at birth: `AssertClaimPayloadSchema.subjectObjectIds` is `z.array(z.string())` with no `.min(1)` (packages/rph-contracts/src/messages.ts:324) and `ClaimObjectSchema` likewise (packages/rph-contracts/src/objects.ts:499). RAN THE ENGINE: AssertClaim with `subjectObjectIds: []` -> ACCEPTED; RecordClaimAssessment `targetStatus: 'UNDER_ASSESSMENT'` -> ACCEPTED; stored claim reads status UNDER_ASSESSMENT with subjects []. A subjectless claim crosses the arrow unimpeded."
	},
	["a critical assumption must be explicitly accepted by authority before dependent irreversible work; ACCEPTED is NOT equivalent to VERIFIED (§12.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Both arrows (Assumption.status DISCLOSED->ACCEPTED and UNDER_VERIFICATION->ACCEPTED) have no command. The full command registry (packages/rph-application/src/handlers/registry.ts:118-229) contains exactly four Assumption commands — DetectAssumption, DiscloseAssumption, ExpireAssumption, FalsifyAssumption (registry.ts:166-172) — and no AcceptAssumption or VerifyAssumption. Grepped every `'ACCEPTED'` literal in packages/rph-application/src/handlers/*.ts non-test files: the only Assumption occurrences are as SOURCE states in `fromStates(...)` at assurance.ts:1004 (FalsifyAssumption) and :1055 (ExpireAssumption) — i.e. the state is read as a legal origin but nothing ever writes it. Matches verif/arrow-command-census.baseline.json:4 and :8."
	},
	["a PWU may have only ONE active plan at a time; an active plan references exactly one PWU (§20.2)"]: {
		disposition: "ENFORCED",
		evidence: "packages/rph-application/src/handlers/execution.ts:484 — `return reject(` inside `activateExecutionPlan`'s `guard`, entered at :483 `if (!check.ok) {` where `check = canActivatePlan({ planStatus, otherActivePlanExists })` (:482). The sibling-plan fact is RESOLVED from the store by `otherActivePlanExistsForPwu` (execution.ts:404-420), which folds all PLAN aggregates and returns true when another plan on the same `workUnitId` is ACTIVE; the kernel refuses at packages/rph-domain/src/execution.ts:49-54 with errorCode RPH_ACTIVE_PLAN_CONFLICT. Handler-invoked, not a dead predicate. The second clause ('an active plan references exactly one PWU') is structural rather than checked — the plan aggregate carries a single scalar `workUnitId` (read at execution.ts:479), so a plan cannot name two. Ran execution-plan-activation-guard.test.ts at HEAD: green, including :135 'rejects activating a SECOND plan while the PWU already has an ACTIVE one'.",
		enforcingSite: "packages/rph-application/src/handlers/execution.ts:484",
		enforcingAnchor: "Cannot activate plan ${command.targetAggregateId}"
	},
	["a recomposed result requires an explicit assessment (§14.1)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow RecompositionContract.status COMPOSABLE->SATISFIED. `completeRecomposition` declares `targetStates: ['COMPOSABLE', 'CONFLICTED', 'INSUFFICIENT']` (packages/rph-application/src/handlers/decomposition.ts:673) from `precondition: fromStates('EVALUATING')` (:680), and its own comment says so verbatim at decomposition.ts:678: \"This is the EVALUATING -> outcome hop (NOT the machine's COMPOSABLE -> SATISFIED arrow its `completeRecomposition` trigger label names)\". Enumerated every `target:` in decomposition.ts (lines 107-109, 326, 456, 527, 674): no site targets SATISFIED on this machine, and the registry has only ProposeRecomposition/BeginRecomposition/CompleteRecomposition (registry.ts:212-214). Matches the `RecompositionContract.status  COMPOSABLE -> SATISFIED` row in verif/arrow-command-census.baseline.json. CITED BY ROW TEXT, NOT BY LINE, per guard-enforcement-ledger.ts's `enforcingAnchor` doctrine: the baseline is GENERATED and re-pinned whenever coverage moves, and this citation had already drifted — it read \":194\"; re-measured 2026-08-13 that row is at :131."
	},
	["a skipped MANDATORY step requires an authorized plan revision or waiver (§21.1)"]: {
		disposition: "UNENFORCED",
		evidence: "REFUTED BY THE SKEPTIC PASS, having first been classified ENFORCED. The refusal at execution.ts:1582 is real and its mutants do redden — but it only runs when the caller SUPPLIES waiverOrRevisionId, and `mandatory` comes from the COMMAND PAYLOAD (`p.mandatory ?? true`), not from the step. So the actor the rule constrains disables it with one boolean: dispatch `SkipExecutionStep { mandatory: false }` and no authorization is ever sought. 15 of the repository's own fixtures already use that payload shape. The worst kind of false ENFORCED — refusal present, tests green, mutants reddening, rule escapable. ⚠ AND THE REASON IS THE CORPUS, NOT THE WIRING (REG-F-105, 2026-08-10): `ExecutionStep` HAS NO MANDATORY FIELD, and neither does RPH-DOC-002's `interface ExecutionStep` (L1264-1299) — twelve fields, none of them optionality. Searched: `mandatory:` as a FIELD appears nowhere in the RPH corpus; positive control `preconditions: Condition[]` in the same document with the same grep shape returns 1. The repository transcribed the object faithfully. So the corpus STATES the rule (L1308; canon STA-8) and defines NO FACT that could enforce it, leaving the skipper's own payload as the only source. THIS ROW CANNOT MOVE TO ENFORCED BY ANY REPOSITORY CHANGE — it needs a ratified field, which is escalated rather than invented. Pinned meanwhile by execrem-wp12-skip-authorization.test.ts's ADMISSION case, which shows the SAME step refused then skipped on the caller's own boolean and reddens the day an optionality field is read."
	},
	["a step cannot run until preconditions are satisfied (§21.1)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow ExecutionStep.stepState NOT_READY->READY. `advanceStep` is the sole writer of stepState and is driven entirely by STEP_COMMAND_SPECS; enumerated all nine specs (packages/rph-domain/src/step-command-spec.ts:190-380) — targets are RUNNING, SUCCEEDED, FAILED, QUEUED, SKIPPED, CANCELLED, SKIPPED, WAITING, RUNNING. NOTHING targets READY. The source state is also unoccupiable: ProposeExecutionPlan refuses any authored step not at rest in QUEUED (packages/rph-domain/src/plan-proposal.ts:99-105), whose own message reads \"(NOT_READY/READY) declares a step no command can advance\", and the header at plan-proposal.ts:13-14 states \"No command drives a step OUT of NOT_READY\". NOT A DEAD RULE ELSEWHERE, stated so the ledger is not misread: the same §21.1 substance IS enforced on a DIFFERENT arrow — `stepMayBecomeReady` (packages/rph-domain/src/execution.ts:499) is called by `inputReadinessRefusal` at packages/rph-application/src/handlers/execution.ts:793, gating REQUIRES_PRESENT_INPUTS commands into RUNNING. It is this arrow that cannot fire. Matches verif/arrow-command-census.baseline.json:46."
	},
	["a validator execution failure was observed (§34.1)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow ValidatorRegistryEntry.status ACTIVE->DEGRADED, performed by `markValidatorDegraded` = `statusChange('DEGRADED', 'ValidatorDegraded', ['ACTIVE'])` (packages/rph-application/src/handlers/validator-registry.ts:91). The shared factory (validator-registry.ts:69-88) supplies only `precondition: fromStates(...from)` and an event payload; it reads no assessment, no event log, nothing. `reason` is a bare `z.string()` (packages/rph-contracts/src/messages.ts:725-728). The module says so itself at validator-registry.ts:24-27: \"No handler here degrades a validator as a side effect of an assessment; a caller does it deliberately.\" RAN THE ENGINE: RegisterValidator then MarkValidatorDegraded with `reason: 'x'` -> ACCEPTED, entry status DEGRADED, and the ONLY two events in the whole store were ValidatorRegistered and ValidatorDegraded — no assessment, no VALIDATOR_FAILED, no failure of any kind existed to observe."
	},
	["a violated obligation must affect assurance disposition (§10.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Both arrows (Obligation.status ACTIVE->VIOLATED and ALLOCATED->VIOLATED) have no command, and neither source state is occupiable. The registry contains exactly ONE Obligation command, `AssertObligation` (packages/rph-application/src/handlers/registry.ts:204), and it only mints the object at `status: 'PROPOSED'` (packages/rph-application/src/handlers/obligation-constraint.ts:34, :48). obligation-constraint.ts has no advanceStatus site at all — it exports assertObligation and assertConstraint only. So PROPOSED->ACTIVE is itself unperformable, which is why ACTIVE and ALLOCATED are never occupied. verif/arrow-command-census.baseline.json lists all ten Obligation.status arrows as uncovered and the machine in its orphan list (cited without ordinals: the generated baseline is re-pinned as coverage moves, and :83-92/:211 had already drifted to :77-86/:147 by 2026-08-13). Secondary note: the text states a downstream CONSEQUENCE ('must affect assurance disposition') rather than a precondition on the arrow, so even a built arrow would not naturally carry a refusal here."
	},
	["a waived mandatory obligation requires an authorized waiver (§10.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Both arrows (Obligation.status ACTIVE->WAIVED and ALLOCATED->WAIVED) have no command. Same search as the VIOLATED row: the registry's only Obligation command is `AssertObligation` (packages/rph-application/src/handlers/registry.ts:204), which mints at PROPOSED (packages/rph-application/src/handlers/obligation-constraint.ts:34) and is the whole of that module; no advanceStatus site names Obligation.status anywhere in packages/rph-application/src/handlers. The waiver machinery that does exist (RequestWaiver/GrantWaiver/DenyWaiver, registry.ts:194-196) writes Decision.status, never an Obligation. Matches the `Obligation.status  ACTIVE -> WAIVED` and `Obligation.status  ALLOCATED -> WAIVED` rows in verif/arrow-command-census.baseline.json, and Obligation.status is in that file's orphan list. CITED BY ROW TEXT, NOT BY LINE, per guard-enforcement-ledger.ts's `enforcingAnchor` doctrine (\"a line number rots on the next edit and rots SILENTLY\"): the baseline is GENERATED and re-pinned whenever coverage moves, and this citation had already drifted — it read \":87 and :91\" with the orphan at \":211\"; re-measured 2026-08-13 those rows are at :81 and :85 and the orphan entry at :147."
	},
	["Active execution plan approved"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState READY->PLANNED. PLANNED is NOT in PWU_SEMANTIC_LIFECYCLE_COMMANDS (packages/rph-application/src/handlers/pwu.ts:328-335), so the sole writer of this arrow is changePwuState (pwu.ts:1057). Its entire refusal chain was read line by line — rejectUnbackedDisposition (pwu.ts:678), rejectUnbackedBaselining (pwu.ts:721), rejectUnbackedExecutionSuccess (pwu.ts:773), rejectUnauthorizedAbandonment (pwu.ts:977), rejectIllegalWorkLifecycleMove (pwu.ts:1022), rejectArrowOwnedBySemanticCommand (pwu.ts:934), rejectUnauthorizedRejection (pwu.ts:854) — and NONE has a branch keyed on newState==='PLANNED'. WORK_LIFECYCLE_CROSS_AXIS_GUARDS (packages/rph-domain/src/pwuGuards.ts:19-23) declares only three pairs and READY->PLANNED is not one. RAN THE ENGINE: a PWU driven PROPOSED->SHAPING->READY through the owning commands, then ChangePwuState READY->PLANNED via Engine.dispatch with ZERO ExecutionPlan objects in the store => ACCEPTED, workLifecycleState committed to PLANNED (repeated from a seeded READY with executionState held at NOT_PLANNED: also ACCEPTED). The only production site that loads an EXECUTION_PLAN on the PWU write path is rejectUnbackedExecutionSuccess, which fires on executionState==='SUCCEEDED' and never on this arrow."
	},
	["all mandatory criteria MET; no open BLOCKING/CRITICAL finding; required independence verified (§8.4, INV-8); every satisfied claim references admissible evidence (INV-3); required evidence admissible (§10.3, §15.9)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow AssuranceAssessment.state ASSESSING->SATISFIED via `completeAssuranceAssessment` (packages/rph-application/src/handlers/assurance.ts:1763). RAN THE ENGINE and the conjunction is DEMONSTRABLY satisfiable-when-false: seeded a policy with `independenceRequirement: 'DIFFERENT_AGENT'`, requested + began an assessment, completed with a validatorResult recommending SATISFIED and NO `producer` -> ACCEPTED, assessmentState SATISFIED, and `independenceResult` ABSENT from the AssuranceAssessmentCompleted event. The independence limb runs only under `if (independenceRequirement && independenceRequirement !== 'NONE' && producer && evaluator)` (assurance.ts:1922) and the handler's own comment at :1805-1811 states the assessment then \"proceeds WITHOUT a check\". The 'all mandatory criteria MET' limb is unevaluated — criterion results are dropped at the boundary, disclosed at assurance.ts:1787-1789 — and my run passed `claimResults: []`. The 'every satisfied claim references admissible evidence (INV-3)' limb is not read here either: nothing in the handler loads the assessment's `claimIds`. TWO limbs ARE conditionally gated and I name them so the ledger is not read as 'nothing at all': Gate A refuses at assurance.ts:2033 when required evidence is unsubmitted, and Gate C refuses at assurance.ts:1750 when the policy declares `forbiddenOpenSeverities` for this disposition (skipped entirely when it does not). Partial and policy-contingent gating of a five-limb conjunction is not enforcement of the text."
	},
	["all required children satisfied/conditionally-satisfied/waived/superseded via authorized decision (§14.1)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow RecompositionContract.status DRAFT->READY. Doubly unreachable. (1) The source state is never occupied: `proposeRecomposition` births the contract in READY, declared explicitly at packages/rph-application/src/handlers/decomposition.ts:508 `births: [{ machine: 'RecompositionContract.status', statusField: 'status', values: ['READY'] }]`, with the comment at :507 \"REG-F-071: the machine declares `initialState: 'DRAFT'` and nothing writes it. Born READY.\" (2) No command targets READY on this machine: enumerated every `target:` in decomposition.ts (lines 107-109, 326, 456, 527, 674) — EVALUATING and the three outcome states only — and the registry has just ProposeRecomposition/BeginRecomposition/CompleteRecomposition (registry.ts:212-214). Matches the `RecompositionContract.status  DRAFT -> READY` row in verif/arrow-command-census.baseline.json (cited by row text; :197 had drifted to :134 by 2026-08-13)."
	},
	["all required evidence present and admissible per §6.2 (identity stable, provenance present, in-scope, not invalidated, sufficiently current, relevant)"]: {
		disposition: "UNENFORCED",
		evidence: "NOTE THE CENSUS IS WRONG HERE — verif/arrow-command-census.baseline.json:15 lists this arrow as uncovered because it only reads `advanceStatus` sites, but AssuranceAssessment.state EVIDENCE_PENDING->READY IS performed, by `submitEvidenceForAssessment` via a direct `commitState`: `const advancesToReady = assessmentState === 'EVIDENCE_PENDING' && stillOutstanding.length === 0;` (packages/rph-application/src/handlers/assurance.ts:1452) then `...(advancesToReady ? { assessmentState: 'READY', ... })` (:1457). There is NO refusing line — an unmet condition simply records the receipt and does not move — and the ADMISSIBILITY half of the guard is never evaluated at all: the handler checks that `satisfiesRequirementId` is policy-declared (:1408) and not a duplicate (:1418), and never loads the Evidence object or reads its status. RAN THE ENGINE: policy with one requiredEvidence ER1 (requiredForDispositions ALL) -> assessment lands EVIDENCE_PENDING -> SubmitEvidenceForAssessment with `evidenceId: 'evd_this_object_does_not_exist'` -> ACCEPTED, assessmentState READY, and that evidence id resolves to no object in the store. Every §6.2 sub-limb (identity stable, provenance present, in-scope, not invalidated, sufficiently current, relevant) was satisfied by an id naming nothing."
	},
	["an authoritative baseline is immutable — changes create a SUCCESSOR baseline; supersession preserves traceability (§24.2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Baseline.status AUTHORITATIVE->SUPERSEDED via `supersedeBaseline` (packages/rph-application/src/handlers/governance.ts:998). Its ONLY check is `precondition: fromStates('AUTHORITATIVE')` (governance.ts:1006). `supersedingBaselineId` is `z.string()` (packages/rph-contracts/src/messages.ts:511-513), is never resolved to an existing Baseline, and — per the handler's own note at governance.ts:1013 — \"has no object field to hold it (`BaselineObjectSchema` declares none)\", so the trace exists only on the event. ENGINE EVIDENCE, shipped and green at HEAD: packages/rph-application/src/handlers/dwp04-precondition-coverage.test.ts:284 defines `supersede = (supersedingBaselineId = 'base_v2')` and :311-318 asserts the arrow ACCEPTED — 'base_v2' is created nowhere in that fixture. Ran dwp04-precondition-coverage.test.ts: 18 tests green. The repository's own register already records the disposition: packages/rph-domain/src/enforcement-register.ts (RPH-BAS-007 `why`) reads \"THREE POST-CONDITIONS of a SUCCESSFUL supersession … so no dispatch can be refused for violating them\" and \"THE TRACE IS THE FINDING, and it was OBSERVED\". Not NOT_MECHANICALLY_CHECKABLE: 'the named successor exists' is trivially decidable; it simply is not decided."
	},
	["approval requires authority; an agent may recommend but cannot exercise authority unless delegated (§23.2)"]: {
		disposition: "ENFORCED",
		evidence: "Arrow: Decision.status PROPOSED -> EFFECTIVE, performed by `makeDecisionEffective` (governance.ts:274), the shared advanceStatus literal behind BOTH approveDecision (governance.ts:363) and grantWaiver (governance.ts:627). governance.ts:313 derives `const authorityHeld = authority?.actorType === 'HUMAN';` from the RECORDED authority and hands it to `authorizeDecisionEffective`; packages/rph-domain/src/governance.ts:56 `if (!d.authorityHeld)` returns not-ok; governance.ts:323 REFUSES with 'RPH_AUTHORITY_INSUFFICIENT'. The 'unless delegated' half is closed at the other end — proposeDecision REFUSES at governance.ts:192 when the declared `authority` is not `command.issuedBy`, saying 'no delegation record exists to carry one actor's authority for another'. DRIVEN THROUGH THE REAL BUS: an AGENT credential proposed a decision on its own authority (ACCEPTED, status PROPOSED), then issued ApproveDecision -> UNAUTHORIZED / RPH_AUTHORITY_INSUFFICIENT 'actor lacks sufficient authority to make this decision effective'; the decision stayed PROPOSED. Pinned suite: decision-authority-provenance.test.ts:151 ('an AGENT cannot reach an EFFECTIVE decision by either route').",
		enforcingSite: "packages/rph-application/src/handlers/governance.ts:323",
		enforcingAnchor: "cannot become effective: ${check.reason ??"
	},
	["assurance observations must REMAIN VISIBLE after remediation — the observation is not deleted (§18.1)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: AssuranceObservation.disposition OPEN -> REMEDIATED. `recordAssuranceObservation` (packages/rph-application/src/handlers/assurance.ts:2138) is the ONLY observation command and it CREATES with `disposition: 'OPEN'` (assurance.ts:2162); no handler writes the field again. PROBED THE REGISTRY AT RUNTIME: `Object.keys(HANDLERS).filter(k => /Observation|Remediat/i.test(k))` === ['RecordAssuranceObservation'] — one command, and it is a mint. `AssuranceObservation.disposition` is in the C-0 census `orphans` list (no advanceStatus site names it) and all five OPEN-> arrows sit in verif/arrow-command-census.baseline.json `uncovered`. The only generic setter in the system, ChangePwuState, is PWU-only (generic-setter-scope.test.ts). SEARCHED: grep for the literal REMEDIATED across packages/ and apps/ excluding dist and gen — outside enums.ts:562 and transitions.data.ts it appears ONLY in packages/rph-projections/src/work-projection.ts:13,112 (a read-model treating REMEDIATED as visible-but-not-blocking) and one governance.ts:58 comment. Nothing WRITES it, so the guard can never fire."
	},
	["Authorized actor"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrows Intent.intentStatus RAW|UNDER_DISCOVERY|PROVISIONAL -> WITHDRAWN. No command can perform them. Six INTENT commands are registered (packages/rph-application/src/handlers/registry.ts:113-118: CaptureIntent, BeginIntentDiscovery, ProvisionIntent, FormalizeIntent, ApproveIntent, ReviseIntent) and none targets WITHDRAWN; I printed Object.keys(HANDLERS) at runtime and confirmed no WithdrawIntent/SupersedeIntent in the 95 registered types. Grepped every write of `intentStatus` across packages/*/src excluding tests: all of them are in intent.ts (lines 61, 73, 149, 183, 229, 256, 311) and the only targets are RAW/UNDER_DISCOVERY/PROVISIONAL/FORMALIZED/APPROVED/REVISED. There is no generic Intent setter (ChangePwuState is PWU-only, pwu.ts:1057). The repository states the same conclusion independently at packages/rph-domain/src/enforcement-register.ts:1473-1479 (RPH-INT-007): \"no dispatch sequence can put an Intent into SUPERSEDED: six INTENT commands are registered, none targets it, `SupersedeIntent` and `WithdrawIntent` occur nowhere in the repository, intent.ts is the only production writer of the aggregate, and the engine has no generic aggregate-mutation command. SUPERSEDED and WITHDRAWN are declared terminal states of the ratified machine and both are unreachable.\" verif/arrow-command-census.baseline.json also lists all three arrows in `uncovered`."
	},
	["Authorized decision (Decision.decisionType=ABANDON)"]: {
		disposition: "ENFORCED",
		evidence: "All 17 arrows -> ABANDONED. ABANDONED is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS, so changePwuState is the only writer, and rejectUnauthorizedAbandonment (pwu.ts:977) is source-state-independent — it keys on `p.newState !== 'ABANDONED'` (pwu.ts:987) alone, so one disposition genuinely covers all 17. RELOCATED 2026-08-09 by JAN-PWUWP W-1 (REG-D-029) — a RELOCATED delta row, not ENFORCED_NEW and not UNREACHABLE_BY_DELETION: the guard did not weaken, it acquired the semantically named command PER-3 requires. The refusing line is now pwu.ts:678 inside `abandonPwu`, and `changePwuState` refuses the arrow outright via PWU_SEMANTIC_LIFECYCLE_COMMANDS rather than adjudicating it. Re-driven through the bus: AbandonPwu naming a dangling id, a non-DECISION, an APPROVAL, a PROPOSED decision, another PWU's decision, and an unpinned subject are each REJECTED; the ownership refusal is asserted by its own control. THE PRIOR TEXT, kept because the site moved and the reasoning did not: the refusing line WAS pwu.ts:1003 (`return reject(command, 'RPH_INVARIANT_VIOLATION', ...` — \"would abandon PWU ... with no authorized decision to back it\"), called from the handler at pwu.ts:1110. The per-limb checks live in packages/rph-application/src/handlers/abandon-authorization.ts:88 (decisionType !== 'ABANDON' refused), :96 (status !== 'EFFECTIVE' refused) and :111 (subjectSemanticVersions bind). RAN THE ENGINE: ChangePwuState BLOCKED->ABANDONED with supportingObjectIds:[] => REJECTED RPH_INVARIANT_VIOLATION; citing a non-DECISION object (the Intent) => REJECTED the same way.",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:678",
		enforcingAnchor: "AbandonPwu ${id}: ${verdict.reason}"
	},
	["Authorized decision exists"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus FORMALIZED->APPROVED, performed by approveIntent (packages/rph-application/src/handlers/intent.ts:235-285). The handler NEVER LOADS `p.decisionId`: its precondition is fromStates('FORMALIZED','REVISED') (intent.ts:246) and its precheck checks exactly two things — INT-004 non-empty desiredOutcomes (intent.ts:261-267) and approvedSemanticVersion === current semanticVersion (intent.ts:275-281). `decisionId` is only echoed into the IntentApproved event payload (intent.ts:254). Nothing verifies the decision EXISTS, is a DECISION, is EFFECTIVE, or names this intent. RAN THE ENGINE: after formalizing with one desired outcome, ApproveIntent with `decisionId: 'dec_ghost_no_such_object'` => ACCEPTED, intentStatus committed to APPROVED. (Contrast the PWU side, where abandon-authorization.ts:88-111 does all four of those checks — so the pattern exists in the codebase and is simply not applied here.)"
	},
	["Authorized promotion decision"]: {
		disposition: "ENFORCED",
		evidence: "RELOCATED 2026-08-09 by JAN-PWUWP W-4.5 — a second RELOCATED delta on this row, and the anchor check added hours earlier caught the move on its first real test (\"anchor absent from pwu.ts\"). rejectUnbackedBaselining moved out of the generic setter into `baselinePwu`, unchanged in substance: the design had proposed RETIRING it as a \"strengthening\" and the review showed that was wrong, because it checks a cross-aggregate Governance fact no propagating plane covers. ORIGINAL EVIDENCE: Arrows SATISFIED->BASELINED and RECOMPOSED->BASELINED. BASELINED is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS, so changePwuState is the only writer, and rejectUnbackedBaselining (pwu.ts:721) keys on `p.newState !== 'BASELINED'` (pwu.ts:728) alone, so both arrows are covered identically. THE REFUSING LINE IS pwu.ts:746 (`return reject(command, 'RPH_EVIDENCE_MISSING', ...` — \"would baseline PWU ... with no promoted baseline to back it\"), called from the handler at pwu.ts:1108. It requires a cited BASELINE whose status is AUTHORITATIVE and whose itemObjectVersions include this PWU (pwu.ts:741-743). That grounds the DECISION transitively and the chain was checked: the sole route to Baseline.status=AUTHORITATIVE is promoteBaseline (packages/rph-application/src/handlers/governance.ts:841), whose guard loads p.promotionDecisionId and passes it to canPromoteBaseline requiring an EFFECTIVE decision that NAMES this baseline (governance.ts:854-908, REG-F-073), plus a decisionAuthorizesVersions currency bind (governance.ts:925+). RAN THE ENGINE: SATISFIED->BASELINED with supportingObjectIds:[] => REJECTED RPH_EVIDENCE_MISSING; RECOMPOSED->BASELINED with supportingObjectIds:[] => REJECTED RPH_EVIDENCE_MISSING.",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:702",
		enforcingAnchor: "is not an AUTHORITATIVE BASELINE whose itemObjectVersions"
	},
	["authorized reinstatement"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: ValidatorRegistryEntry.status DISABLED -> ACTIVE, performed by `enableValidator = statusChange('ACTIVE','ValidatorEnabled',['DISABLED'])` (packages/rph-application/src/handlers/validator-registry.ts:101). The shared `statusChange` factory (validator-registry.ts:69-89) passes advanceStatus only a `precondition: fromStates(...)` — NO `guard`, no read of `command.issuedBy`, no Decision lookup, no authority of any kind. EnableValidatorPayloadSchema (packages/rph-contracts/src/messages.ts:740) carries `{validatorId, reason}` only, and `reason` merely rides the event. DRIVEN THROUGH THE REAL BUS: a HUMAN registered a validator, then an AGENT credential issued DisableValidator and EnableValidator — both ACCEPTED; status went ACTIVE -> DISABLED -> ACTIVE. SEARCHED: grep 'EnableValidator' across packages/ and apps/ (excluding dist/node_modules) returns registry.ts:182, the handler, its tests, and one refusal message in assurance.ts:1894 — no authorization site exists anywhere."
	},
	["authorized withdrawal"]: {
		disposition: "UNENFORCED",
		evidence: "Arrows: ValidatorRegistryEntry.status ACTIVE -> DISABLED and DEGRADED -> DISABLED, both performed by the single `disableValidator = statusChange('DISABLED','ValidatorDisabled',['ACTIVE','DEGRADED'])` (packages/rph-application/src/handlers/validator-registry.ts:98) — so one disposition genuinely covers both. Same factory as reinstatement: validator-registry.ts:69-89 supplies a `precondition` and no `guard`; nothing evaluates who may withdraw a validator. DRIVEN: an AGENT credential issued DisableValidator against a validator registered by a HUMAN -> ACCEPTED, entry DISABLED. This is not cosmetic: assurance.ts:1889 refuses any CompleteAssuranceAssessment carrying a DISABLED validator's result, so an unauthorized withdrawal is a live lever for blocking assurance completions."
	},
	["available admissible evidence insufficient to support or reject the claim; evidence deficit (§10.1, §10.3); also on evidence access failure (§34.3)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: AssuranceAssessment.state ASSESSING -> INCONCLUSIVE. The disposition is the VALIDATOR'S WORD taken verbatim: completeAssuranceAssessment ends at packages/rph-application/src/handlers/assurance.ts:2056 with `advanceStatus(... target: disposition ...)`, where `disposition` comes from `validatorResult.dispositionRecommendation` and `parseCompletion` checks only enum membership and that every subject carries a semantic version. Gate A says so in terms at assurance.ts:2005-2008: 'Negative dispositions (REJECTED / ESCALATED / INCONCLUSIVE) are deliberately NOT gated'. No code computes an evidence deficit, compares admissible evidence against the claim, or handles §34.3 evidence-access failure. DRIVEN: policy created and activated, assessment requested and begun to ASSESSING, then CompleteAssuranceAssessment with dispositionRecommendation 'INCONCLUSIVE', evidenceConsideredIds [], residualUncertainty [], observations [] -> ACCEPTED, assessmentState = INCONCLUSIVE, with no deficit established by anything."
	},
	["Change rationale and impact analysis initiated"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus APPROVED->REVISED, performed by reviseIntent (packages/rph-application/src/handlers/intent.ts:288-314). Its only precondition is fromStates('APPROVED') (intent.ts:295). ReviseIntentPayloadSchema (packages/rph-contracts/src/messages.ts:382-385) is `{ changeRationale: z.string(), impactAnalysisId: z.string().optional() }` — the rationale has no min-length and the impact analysis is OPTIONAL and is never loaded or resolved anywhere; both fields are only echoed into the event payload (intent.ts:307-312). So neither conjunct of the guard is checked: an empty rationale passes, and no impact analysis need exist. RAN THE ENGINE: ReviseIntent with `{ changeRationale: '' }` and no impactAnalysisId => ACCEPTED, intentStatus committed to REVISED and semanticVersion bumped."
	},
	["claims supportable only if explicit conditions remain true or required follow-up occurs; typically an open MATERIAL finding (§10.1, §10.3)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: AssuranceAssessment.state ASSESSING -> CONDITIONALLY_SATISFIED. Two gates DO fire on this destination but neither checks this condition: Gate A (assurance.ts:2009) gates on the policy's `requiredEvidence`, and Gate C `rejectForeclosedDisposition` (assurance.ts:2050) on `forbiddenOpenSeverities`. Nothing requires that explicit conditions or required follow-up be stated — and the persisted ASSURANCE_ASSESSMENT has no field for them: probed object keys after a CONDITIONALLY_SATISFIED completion were [...,'evidenceConsideredIds','rejectedEvidence','observationIds','assessmentState','residualUncertainty','recommendedControlActions','startedAt','completedAt','evaluator'] — no conditions. Gate A is additionally vacuous in practice: REG-F-022 / verif/policy-evidence-requirement-census.test.ts pin that NO production path declares requiredEvidence on any policy. DRIVEN: CONDITIONALLY_SATISFIED with residualUncertainty [], observations [], no conditions -> ACCEPTED. 'typically an open MATERIAL finding' is a hedge, not a requirement, and no code requires one."
	},
	["Conditional disposition exists"]: {
		disposition: "ENFORCED",
		evidence: "Arrow UNDER_ASSURANCE->CONDITIONALLY_SATISFIED. Predicate at packages/rph-domain/src/pwuGuards.ts:22 — `'UNDER_ASSURANCE->CONDITIONALLY_SATISFIED': (a) => a.assuranceState === 'CONDITIONALLY_SATISFIED'` — reached from changePwuState (the only writer; the target is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS) via canAdvanceWorkLifecycle at pwu.ts:1039, refusal at pwu.ts:1041. The conditional disposition it demands cannot itself be asserted: rejectUnbackedDisposition (pwu.ts:678, refusal at pwu.ts:697) requires a cited ASSURANCE_ASSESSMENT whose assessmentState is CONDITIONALLY_SATISFIED and whose subjectObjectIds include this PWU. RAN THE ENGINE, both routes: (a) seeded UNDER_ASSURANCE, ChangePwuState -> CONDITIONALLY_SATISFIED with assuranceState=UNASSESSED => REJECTED \"cross-axis guard failed for UNDER_ASSURANCE -> CONDITIONALLY_SATISFIED\"; (b) walked the assurance axis legally to ASSESSING then ASSESSING->CONDITIONALLY_SATISFIED with supportingObjectIds:[] => REJECTED RPH_EVIDENCE_MISSING \"assuranceState=CONDITIONALLY_SATISFIED with nothing to back it\".",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:1041",
		enforcingAnchor: "Cannot advance PWU ${id} ${currentWorkLifecycleState}"
	},
	["contradicting evidence must remain visible (§15.2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrows: Claim.status UNDER_ASSESSMENT|SUPPORTED|CONDITIONALLY_SUPPORTED -> CONTESTED, all via recordClaimAssessment. Its `guard` (assurance.ts:777) returns null for every target except SUPPORTED — the comment at assurance.ts:773-776 states the scoping deliberately. `contradictingEvidenceIds` is OPTIONAL on RecordClaimAssessmentPayloadSchema (packages/rph-contracts/src/messages.ts:333) and is written to the claim only when non-empty (assurance.ts:784-787); nothing requires it, resolves the ids to EVIDENCE objects, or keeps them visible. DRIVEN: claim asserted OPEN -> UNDER_ASSESSMENT -> CONTESTED with the field ABSENT from the payload -> ACCEPTED; claim status CONTESTED, contradictingEvidenceIds = []. ARROWS DIFFER, STRICTEST GIVEN: CONDITIONALLY_SUPPORTED -> CONTESTED is in the C-0 census `dead` list because recordClaimAssessment refuses target CONDITIONALLY_SUPPORTED outright at assurance.ts:741 (no ratified event name), so that source is never occupied; the other two are live and probed."
	},
	["criteria met identified; evidence considered identified (§18.1) — NOT forced by executionState=SUCCEEDED (P1/INV-5)"]: {
		disposition: "UNENFORCED",
		evidence: "⚠ THE NEAR-MISS — a refusing line exists but it refuses a DIFFERENT condition. `rejectUnbackedDisposition` (pwu.ts:678) does REFUSE at pwu.ts:695 with RPH_EVIDENCE_MISSING when assuranceState=SATISFIED is set without citing a SATISFIED ASSURANCE_ASSESSMENT naming this PWU (probed: REJECTED, axis stayed ASSESSING). But it checks only that SOME satisfied assessment is cited. DECISIVE PROBE: I completed an assessment with claimResults [], evidenceConsideredIds [], observations [] -> SATISFIED (object's evidenceConsideredIds = [], observationIds = []), then drove ChangePwuState UNASSESSED -> EVIDENCE_REQUIRED -> READY_FOR_ASSESSMENT -> ASSESSING -> SATISFIED citing it: ACCEPTED, PWU assuranceState = SATISFIED. So the backing assessment need identify NO criteria met and NO evidence considered. SEARCHED: `evidenceConsideredIds` across rph-application/rph-domain/rph-assurance src (non-test) is WRITTEN at assurance.ts:1281 and :2113 and READ nowhere; there is no criteria-met field at all. This is enforcement-by-delegation to a check that does not exist — the shape REG-F-072's critic recorded for PWU SATISFIED -> BASELINED. The second clause is a statement that no code forces the axis from executionState; true, and policed by nothing."
	},
	["CROSS-AXIS guard: assuranceState=SATISFIED. This is the ONLY legal path into workLifecycle SATISFIED (P1/INV-5)."]: {
		disposition: "ENFORCED",
		evidence: "Arrow UNDER_ASSURANCE->SATISFIED. The predicate is packages/rph-domain/src/pwuGuards.ts:21 — `'UNDER_ASSURANCE->SATISFIED': (a) => a.assuranceState === 'SATISFIED'` — and it is NOT an orphan kernel predicate: canAdvanceWorkLifecycle (pwuGuards.ts:155, guard lookup at :159-162) is called from the handler at pwu.ts:1039 inside rejectIllegalWorkLifecycleMove, whose refusal is pwu.ts:1041 ('RPH_ILLEGAL_STATE_TRANSITION', \"cross-axis guard failed for ...\"), reached from changePwuState at pwu.ts:1122. A second independent refusal backs it on the same command: satisfiesP1 (pwuGuards.ts:171) at pwu.ts:1030 blocks a HOLD in SATISFIED without assurance. RAN THE ENGINE: seeded PWU at UNDER_ASSURANCE, ChangePwuState -> SATISFIED with assuranceState=UNASSESSED => REJECTED \"cross-axis guard failed for UNDER_ASSURANCE -> SATISFIED\"; and the evasion route (asserting assuranceState=SATISFIED in the same command) is separately refused — the assurance sub-axis matrix rejects UNASSESSED->SATISFIED, and from a legitimately ASSESSING axis rejectUnbackedDisposition (pwu.ts:697) refuses with RPH_EVIDENCE_MISSING unless a real ASSURANCE_ASSESSMENT is cited.",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:1041",
		enforcingAnchor: "Cannot advance PWU ${id} ${currentWorkLifecycleState}"
	},
	["CROSS-AXIS guard: executionState=SUCCEEDED. Success does NOT auto-satisfy assurance (P1/INV-5)."]: {
		disposition: "ENFORCED",
		evidence: "Arrow EXECUTING->EVIDENCE_PENDING. Predicate at packages/rph-domain/src/pwuGuards.ts:20 — `'EXECUTING->EVIDENCE_PENDING': (a) => a.executionState === 'SUCCEEDED'` — reached from the handler by the same path as the SATISFIED guard: canAdvanceWorkLifecycle (pwuGuards.ts:159-162) called at pwu.ts:1039, refusal at pwu.ts:1041, from changePwuState (pwu.ts:1122). EVIDENCE_PENDING is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS so changePwuState is the only writer. RAN THE ENGINE: seeded a PWU at workLifecycleState=EXECUTING with executionState=NOT_PLANNED, ChangePwuState -> EVIDENCE_PENDING => REJECTED RPH_ILLEGAL_STATE_TRANSITION \"cross-axis guard failed for EXECUTING -> EVIDENCE_PENDING\". The premise itself is separately defended: asserting executionState=SUCCEEDED requires citing an EXECUTION_PLAN for this PWU that planEvidencesExecutionSuccess accepts (rejectUnbackedExecutionSuccess, refusal at pwu.ts:809).",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:1041",
		enforcingAnchor: "Cannot advance PWU ${id} ${currentWorkLifecycleState}"
	},
	["evidence must have provenance and state scope + limitations (§16.2)"]: {
		disposition: "ENFORCED",
		evidence: "Arrow: Evidence.status PROPOSED -> ADMISSIBLE. `admitEvidence`'s advanceStatus `guard` (assurance.ts:527) calls the kernel `evidenceAdmissibility` and at assurance.ts:541-548 returns `verdict.admissible ? null : reject(...)` — the REFUSING line is assurance.ts:543, code 'RPH_VALIDATION_SEMANTIC_FAILED'. All three limbs of this guard text are live in the kernel: packages/rph-assurance/src/assurance-rules.ts:152 `if (!namesAnActor(e.producedBy)) failed.push('PROVENANCE_PRESENT');`, :154 `if (!e.scope) failed.push('SCOPE_STATED');`, :155 `if (e.limitations === undefined) failed.push('LIMITATIONS_RECORDED');`. Each reads the field the rule is about (REG-F-008 replaced two limbs that could never fail). DRIVEN: evidence proposed with `scope: ''` -> AdmitEvidence REJECTED, 'is inadmissible (§8.11) — failed SCOPE_STATED', and the evidence stayed PROPOSED.",
		enforcingSite: "packages/rph-application/src/handlers/assurance.ts:543",
		enforcingAnchor: "is inadmissible"
	},
	["expirationCondition met; expired assumptions cannot continue authorizing work (§12.2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrows: Assumption.status PROPOSED|DISCLOSED|UNDER_VERIFICATION|ACCEPTED -> EXPIRED, all via `expireAssumption` (assurance.ts:1045), which has a `precondition` and NO `guard`. `expirationCondition` is OPTIONAL on ExpireAssumptionPayloadSchema (packages/rph-contracts/src/messages.ts:312) and is merely spread onto the event when supplied (assurance.ts:1059) — nothing checks it is MET, or even present. DRIVEN: DetectAssumption -> DiscloseAssumption -> ExpireAssumption with payload `{}` -> ACCEPTED, status EXPIRED, no expirationCondition on the object. NOT UNDERSTATED: the SECOND clause IS enforced downstream — `canAuthorizeNewWork` (packages/rph-domain/src/decomposition.ts:474-475) refuses EXPIRED/FALSIFIED/SUPERSEDED and is called from approveExecutionPlan's guard via `assumptionsAuthorizeNewWork` (execution.ts:350). But the clause that GATES this arrow is unchecked, so the TEXT may not be cited as a control. ARROWS DIFFER: ACCEPTED->EXPIRED and UNDER_VERIFICATION->EXPIRED are in the C-0 `dead` list (no command reaches those sources); PROPOSED-> and DISCLOSED-> are live."
	},
	["explicit allocation to a child PWU — a child may satisfy a parent obligation only through explicit allocation (§10.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: Obligation.status ACTIVE -> ALLOCATED. `assertObligation` (packages/rph-application/src/handlers/obligation-constraint.ts:22) is the ONLY OBLIGATION command in HANDLERS (registry.ts:210) and it births `status: 'PROPOSED'` (obligation-constraint.ts:34). No handler advances the field: Obligation.status is in the C-0 census `orphans` list, and BOTH `PROPOSED -> ACTIVE` and `ACTIVE -> ALLOCATED` sit in `uncovered`. DRIVEN: AssertObligation ACCEPTED -> status PROPOSED, and no registered command can move it, so ACTIVE is never occupied. ⚠ TRAP DECLINED: decomposition.ts:157 reads `if (status === 'ALLOCATED') objAllocated.push(oid);` — a branch no write path can reach — and P2 obligation conservation DOES check `state.obligationAllocations` at ValidateDecomposition. That is the DecompositionContract's own field, a different object, and has no bearing on this machine's arrow."
	},
	["falsification triggers impact analysis (§12.2); drives PWU.shapeIntegrityState to AT_RISK/VIOLATED (§39 Scenario 2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrows: Assumption.status DISCLOSED|UNDER_VERIFICATION|ACCEPTED|VERIFIED -> FALSIFIED via `falsifyAssumption` (assurance.ts:985). It DOES refuse — but on a different condition (`contradictingEvidenceExists`, assurance.ts:939) — and it only puts the flag `impactAnalysisRequired` on the EVENT (assurance.ts:1035-1041). Its own header at assurance.ts:975 states the disposition: '⚠ THE CASCADE IS DELIBERATELY NOT BUILT'. Nothing drives PWU.shapeIntegrityState from a falsification: that whole machine is a C-0 `orphan` and PRESERVED->AT_RISK / PRESERVED->VIOLATED are both `uncovered`. DRIVEN: a CRITICAL assumption naming the PWU in affectedObjectIds, disclosed, then FalsifyAssumption citing real contradicting EVIDENCE -> ACCEPTED, assumption FALSIFIED, and the PWU read wl=PROPOSED exec=NOT_PLANNED asr=UNASSESSED shape=UNKNOWN both before and after — no axis moved, no impact analysis object created. ARROWS DIFFER: UNDER_VERIFICATION/ACCEPTED/VERIFIED -> FALSIFIED are all in the C-0 `dead` list; only DISCLOSED -> FALSIFIED is live."
	},
	["independence requirements checked before evaluation (§18.1)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: PWU.assuranceState READY_FOR_ASSESSMENT -> ASSESSING, performed by `changePwuState` (pwu.ts:1057). Its only substance guards are the four 'may not be asserted' checks chained at pwu.ts:1106-1114; `rejectUnbackedDisposition` returns early at pwu.ts:686 because ASSESSING is not in ASSESSMENT_BACKED_DISPOSITIONS (pwu.ts:642-647 = SATISFIED, CONDITIONALLY_SATISFIED, REJECTED, ESCALATED), and the other three address baselining, execution success and abandonment. SEARCHED every production caller of `checkIndependence`: assurance.ts:1923 (completeAssuranceAssessment — a different aggregate, and it fires at COMPLETION, not before evaluation) and packages/rph-assurance/src/floor.ts:314. Neither reads the PWU assurance axis. DRIVEN: UNASSESSED -> EVIDENCE_REQUIRED -> READY_FOR_ASSESSMENT -> ASSESSING, every hop ACCEPTED, with no policy resolved, no evaluator, no producer and supportingObjectIds []."
	},
	["Intent exists"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState PROPOSED->SHAPING. The only writer is beginPwuShaping (packages/rph-application/src/handlers/pwu.ts:406) — ChangePwuState cannot perform it, because SHAPING maps to 'BeginPwuShaping' in PWU_SEMANTIC_LIFECYCLE_COMMANDS (pwu.ts:329) and rejectArrowOwnedBySemanticCommand refuses at pwu.ts:945. beginPwuShaping delegates to advancePwuLifecycle (pwu.ts:346), which loads ONLY the PWU (loadOrReject at pwu.ts:360) and calls canAdvanceWorkLifecycle; it never touches state.intentId and never calls ctx.store.loadObject on it. The one place the engine reads the Intent from a PWU is readinessFactsOf (pwu.ts:421-438), and that is called by markPwuReady (SHAPING->READY), a DIFFERENT arrow. I am recording UNENFORCED rather than ENFORCED deliberately: proposePwu DOES refuse a missing intent at pwu.ts:190-196 (\"ProposePwu requires an existing intent ... (PWU-002)\"), which makes the guard's negative case not currently constructible through the command surface — but that line refuses CREATION, not this arrow, and treating a birth-time check as coverage for a later arrow is precisely the vacuous credit this ledger exists to avoid."
	},
	["invalidated evidence cannot support an active claim; expiration triggers reassessment of dependent claims (§16.2)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: Evidence.status ADMISSIBLE -> INVALIDATED via `invalidateEvidence` (assurance.ts:603), which has NO `guard` — it records `affectedClaimIds` on the event and stops. Its own header at assurance.ts:597-600: 'WHAT THIS DOES NOT DO ... It RECORDS the impact on the event. It does not mutate the claims.' DRIVEN END TO END: claim asserted, evidence proposed and ADMITTED, claim driven to SUPPORTED (supportingEvidenceIds = [evd_...]), then InvalidateEvidence -> ACCEPTED; evidence INVALIDATED and the claim is STILL SUPPORTED with the invalidated evidence still listed in supportingEvidenceIds. The forbidden state is created and persists, and no reassessment occurs. NOT UNDERSTATED: a narrower downstream gate exists — PromoteBaseline refuses a promotion resting on invalidated supporting evidence (the CT-10 pull-guard, baseline-invalidated-evidence.test.ts) — but it neither refuses this arrow nor prevents an active claim standing on invalidated evidence."
	},
	["marked inapplicable with rationale (§11.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: Constraint.status ACTIVE -> INAPPLICABLE. `assertConstraint` (packages/rph-application/src/handlers/obligation-constraint.ts:57) is the ONLY CONSTRAINT command in HANDLERS (registry.ts:211) and births `status: 'PROPOSED'` (obligation-constraint.ts:69). Constraint.status is a C-0 census `orphan`; PROPOSED->ACTIVE and ACTIVE->INAPPLICABLE are both `uncovered`. DRIVEN: AssertConstraint ACCEPTED -> status PROPOSED, and nothing can move it, so ACTIVE is never occupied. ⚠ THE TRAP, STATED SO IT IS NOT MISREAD AS ENFORCEMENT: RPH-CNS-003 INAPPLICABLE_WITHOUT_RATIONALE IS genuinely enforced at ValidateDecomposition (decomposition.ts:196-212; decomposition-conservation.test.ts:277,288) — but on DecompositionContract.constraintPropagations[].disposition, a different object and a different enum (ConstraintPropagationSchema, packages/rph-contracts/src/objects.ts:181). It says nothing about this machine's arrow."
	},
	["must record outputs or an explicit no-output result; step success does NOT imply PWU success (§21.1)"]: {
		disposition: "ENFORCED",
		evidence: "Arrow: ExecutionStep.stepState RUNNING -> SUCCEEDED via `completeExecutionStep` (execution.ts:1275). Its `precheck` calls the kernel `validateStepCompletion` and REFUSES at execution.ts:1351; the guard's own case is packages/rph-domain/src/execution.ts:590 `if (!input.hasOutput && !input.explicitNoOutput)` -> RPH_STEP_RESULT_MISSING, mapped to 'RPH_INVARIANT_VIOLATION' at the call site. Load-bearing detail: `explicitNoOutput: noOutput !== undefined` is the CALLER'S ASSERTION read from the payload, not `!hasOutput` — the handler comment records that the earlier form made the kernel's disjunction `b || !b`. RAN THE SUITE: execution-exe006-explicit-result.test.ts, 10/10 green, including 'MISSING: a completion naming nothing and asserting nothing is REFUSED, and moves nothing' (step stays RUNNING, zero ExecutionStepSucceeded appended, no revision bump). Second clause holds structurally: this handler moves only stepState, and on the PWU side satisfiesP1 refuses a SATISFIED hold without assuranceState=SATISFIED (pwu.ts:1034) while rejectUnbackedExecutionSuccess guards executionState=SUCCEEDED.",
		enforcingSite: "packages/rph-application/src/handlers/execution.ts:1351",
		enforcingAnchor: "CompleteExecutionStep blocked (${check.errorCode})"
	},
	["must reference admissible evidence; confidence values must not replace evidence (§15.2)"]: {
		disposition: "ENFORCED",
		evidence: "Arrow: Claim.status UNDER_ASSESSMENT -> SUPPORTED via `recordClaimAssessment` (assurance.ts:737). Its advanceStatus `guard` (assurance.ts:777) returns null unless target is SUPPORTED, then REFUSES at assurance.ts:780 with 'RPH_INVARIANT_VIOLATION' when `claimsWithAdmissibleEvidence(hctx)` does not contain the claim (RPH-EVD-002). The set is folded from COMMITTED state, never the payload, and tests `String(st.status) !== 'ADMISSIBLE'` at assurance.ts:686 — so merely PROPOSED evidence does not count and a caller cannot assert admissibility on the command that consumes it. That is also the second clause: nothing but an ADMITTED Evidence object is accepted, so no confidence value can substitute. DRIVEN: claim -> UNDER_ASSESSMENT -> SUPPORTED with no evidence -> REJECTED, 'Claim … cannot be SUPPORTED: no ADMISSIBLE evidence supports it (RPH-DOC-008 §13 RPH-EVD-002)'; the same command after AdmitEvidence -> ACCEPTED with supportingEvidenceIds populated.",
		enforcingSite: "packages/rph-application/src/handlers/assurance.ts:780",
		enforcingAnchor: "cannot be SUPPORTED: no ADMISSIBLE evidence"
	},
	["no assurance policy applies to this PWU"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: PWU.assuranceState UNASSESSED -> NOT_REQUIRED, performed by `changePwuState` (pwu.ts:1057). NOT_REQUIRED is absent from ASSESSMENT_BACKED_DISPOSITIONS (pwu.ts:642-647), so `rejectUnbackedDisposition` returns undefined at pwu.ts:686, and none of the other three 'may not be asserted' guards touches the assurance axis. The applicability kernel EXISTS and is real — `policyApplicability` / `applicabilityPermitsAssessment` (packages/rph-domain/src/governance.ts:686, :739) — but grep for its production callers returns exactly two, `requestAssuranceAssessment` (assurance.ts:1199-1210) and packages/rph-engine/src/governing-policies.ts:58-69; neither is on this arrow. DRIVEN: on a PWU with consequence HIGH / irreversibility HIGH / securitySensitivity HIGH, ChangePwuState holding workLifecycle at PROPOSED with assuranceState 'NOT_REQUIRED', reasonCode 'CONTROLLER' and supportingObjectIds [] -> ACCEPTED; the axis landed in NOT_REQUIRED, a TERMINAL state, with no applicability determination made, cited or recorded."
	},
	["no material assumption may remain embedded only in model prose (§12.2)"]: {
		disposition: "UNENFORCED",
		evidence: "ARROW SITE: packages/rph-application/src/handlers/assurance.ts:916-927 — `discloseAssumption` is an `advanceStatus` whose ONLY constraint is `precondition: fromStates('PROPOSED')` (:924). It declares NO `guard:` property at all, so nothing about materiality or prose is evaluated. THE MECHANICAL FORM EXISTS AND IS DEAD: `validateAssumptionReification` (packages/rph-domain/src/decomposition.ts:412) implements exactly this rule (its docblock: 'If any material assumption completed still embedded only in prose (reified === false), the assessment result is rejected'). I grepped it across packages/, apps/ and verif/ (excluding /dist/): the only non-test references are its own definition, its own unit test (decomposition.test.ts:442/450), and packages/rph-domain/src/enforcement-register.ts:3058, which records verbatim '...correct, and NOTHING CALLS IT. `validateAssumptionReification` has exactly one non-test reference: its ' and carries `deadPredicate: 'validateAssumptionReification'` at :3068. Same for `requiresReification` (:3038 'has no caller outside its own module'). I ALSO CHECKED THE OTHER DIRECTION: grep `materiality` across packages/*/src (non-test) — the only production consumer is execution.ts:341-350, which builds an AssumptionView for `canAuthorizeNewWork` (a STATUS test, materiality-blind, and on a different arrow). Not NOT_MECHANICALLY_CHECKABLE: the checkable predicate is written and correct; it simply has no caller."
	},
	["Not already BASELINED"]: {
		disposition: "REDUNDANT_WITH_MACHINE",
		evidence: "All 17 arrows -> SUPERSEDED. The guard restates the transition table: BASELINED has ZERO outgoing transitions. Verified at runtime, not by eye — `STATE_MACHINES['PWU.workLifecycleState'].transitions.filter(t => t.from === 'BASELINED')` returns `[]`, `canTransition('PWU.workLifecycleState','BASELINED','SUPERSEDED')` returns `false`, and the machine's own `illegal` list carries only BASELINED->EXECUTING (packages/rph-domain/src/transitions.data.ts:471-476). BASELINED is declared terminal (transitions.data.ts:152). RAN THE ENGINE: seeded a PWU to BASELINED and dispatched SupersedePwu => REJECTED RPH_ILLEGAL_STATE_TRANSITION \"not a legal PWU.workLifecycleState transition: BASELINED -> SUPERSEDED\", which is canTransition speaking (packages/rph-domain/src/pwuGuards.ts:156-158), not any guard reading the text. CONTROL run: the same SupersedePwu against a SATISFIED PWU => ACCEPTED, so the refusal is attributable to the source state and not to the command being broken. No separate 'not already baselined' predicate exists anywhere — grepped the handlers and rph-domain for any BASELINED comparison on the supersede path and found none."
	},
	["Objective and known ambiguities recorded"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus UNDER_DISCOVERY->PROVISIONAL, performed by provisionIntent (packages/rph-application/src/handlers/intent.ts:187-197). Its only precondition is fromStates('UNDER_DISCOVERY') (intent.ts:191); the mutate copies p.ambiguityIds onto the object (intent.ts:195) with no cardinality or resolution check, and there is NO objective field on this command at all — ProvisionIntentPayloadSchema (packages/rph-contracts/src/messages.ts:517-519) is exactly `{ ambiguityIds: z.array(z.string()) }`, so the 'objective' limb has no wire path to check. RAN THE ENGINE: ProvisionIntent with `{ ambiguityIds: [] }` => ACCEPTED, intentStatus committed to PROVISIONAL. (The existing suite already relies on this: execrem-wp12-authority.test.ts:163 and abandonment-authority fixtures dispatch exactly that payload and assert ACCEPTED.)"
	},
	["obligations allocated/retained, mandatory constraints propagated, siblings explicit; independent validation for high-risk work (§13.2)"]: {
		disposition: "UNENFORCED",
		evidence: "PARTIAL — 2 of 4 limbs enforced, so the TEXT is not. ENFORCED HALF: `validateDecomposition` (packages/rph-application/src/handlers/decomposition.ts:318) carries `guard:` at :337-340 running `checkDecompositionConservation`, which calls `validateObligationConservation` + `validateConstraintPropagation` and REFUSES at packages/rph-application/src/handlers/decomposition.ts:262 `return reject(command, 'RPH_INVARIANT_VIOLATION', ...the decomposition does not conserve its parent's obligations/constraints...)`. That covers limb 1 (MISSING_OBLIGATION_ALLOCATION, rph-domain/src/decomposition.ts:82) and limb 2 (SILENT_CONSTRAINT_DROP etc., rph-domain/src/decomposition.ts:200-235). UNENFORCED HALF, and this is a claim about my search: grep -i 'sibling' over packages/rph-domain/src + packages/rph-application/src (non-test) returns no decomposition-sibling rule — every hit is prose in enforcement-register.ts or an unrelated comment; grep 'independentValidation|independent validation' over packages/ (non-dist, non-test) returns EXACTLY ONE hit, packages/rph-domain/src/transitions.data.ts:883 — the guard string itself. grep 'riskProfile' over decomposition.ts: zero. So 'siblings explicit' and 'independent validation for high-risk work' are evaluated nowhere, and a decomposition can reach VALID with both unmet. C-0b may want to split this text."
	},
	["one or more required EvidenceRequirements not yet satisfied"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "THE SOURCE STATE IS NEVER OCCUPIED, measured not read: I ran verif/arrow-command-census.ts's `birthStates()` — AssuranceAssessment.state births = {EVIDENCE_PENDING, READY} only. `requestAssuranceAssessment` CREATES the assessment already in the landing state (packages/rph-application/src/handlers/assurance.ts:1266 `const landingState = evidenceOutstanding ? 'EVIDENCE_PENDING' : 'READY';`, committed at :1284, births declared at :1299), so REQUESTED is never written; and I enumerated STATE_MACHINES['AssuranceAssessment.state'].transitions for `to === 'REQUESTED'` — ZERO in-arrows. (`initialState: 'REQUESTED'` is the REG-F-071 lying-initialState case; the census reads births, not initialState, for exactly this reason.) SECOND, INDEPENDENT REASON: `declaredArrows()` returns NO command for REQUESTED -> EVIDENCE_PENDING, and verif/arrow-command-census.baseline.json lists 'AssuranceAssessment.state REQUESTED -> EVIDENCE_PENDING' under `uncovered`. NOTE FOR C-0b: the condition itself IS computed at assurance.ts:1253-1266 (requiredEvidenceIds non-empty -> EVIDENCE_PENDING) — but as a BIRTH selector, not as this arrow's guard, so it cannot make the arrow fire."
	},
	["open BLOCKING observations prevent promotion unless waived (§24.2)"]: {
		disposition: "UNENFORCED",
		evidence: "MEASURED LIVE (temporary probe through engine.dispatch, since deleted): recorded a real ASSURANCE_OBSERVATION with severity BLOCKING / disposition OPEN against the PWU, then CreateBaseline over that PWU -> ACCEPTED, SubmitBaselineForReview -> ACCEPTED, ApproveBaseline -> ACCEPTED, baseline status APPROVED. CODE: `approveBaseline` (packages/rph-application/src/handlers/governance.ts:770-788) declares NO `guard:` — only `precondition: fromStates('UNDER_REVIEW')` (:780), a `mutate` for approvalDecisionId and an eventPayload. THE RULE IS REAL BUT SITED ON A DIFFERENT ARROW: `findOpenBlockingObservations` inside `canPromoteBaseline` (packages/rph-domain/src/governance.ts:368+), fed by `observationsAgainstBaselineItems` at governance.ts:902 and refusing at governance.ts:910 — that is APPROVED -> AUTHORITATIVE (this batch's IDX 68 arrow), not UNDER_REVIEW -> APPROVED. packages/rph-application/src/handlers/baseline-open-blocking-observation.test.ts:216 pins that separation: after the promotion is refused it asserts `expect(statusOf(BASE)).toBe('APPROVED')` — i.e. the baseline was allowed to become APPROVED with the blocking finding open."
	},
	["open CRITICAL finding (→REJECTED or ESCALATED) OR open BLOCKING finding OR a material claim unsupported/contradicted/violates a blocking criterion (§10.1, §10.3, INV-11)"]: {
		disposition: "UNENFORCED",
		evidence: "MEASURED LIVE (temporary probe through engine.dispatch, since deleted): seeded pol_arch, RequestAssuranceAssessment -> BeginAssuranceAssessment (assessmentState = ASSESSING confirmed), then CompleteAssuranceAssessment with a full §20 validatorResult carrying `disposition: 'REJECTED'` and ZERO observations recorded anywhere in the store -> ACCEPTED, assessmentState = REJECTED, observation-event count = 0. So the arrow fires with none of the three declared limbs true. CODE: the arrow is performed at packages/rph-application/src/handlers/assurance.ts:2056 with `targetStates: AssuranceDispositionRecommendationSchema.options` and `target: disposition` taken straight off the validator's recommendation. Every gate in `completeAssuranceAssessment` runs the OTHER way: Gate A (assurance.ts:2030) is entered only `if (disposition === 'SATISFIED' || disposition === 'CONDITIONALLY_SATISFIED')`, and Gate C `rejectForeclosedDisposition` (:2050) forbids a POSITIVE disposition while a finding is open — the handler's own comment at :2000 says 'Negative dispositions (REJECTED / ESCALATED / INCONCLUSIVE) are deliberately NOT gated'. Nothing requires a finding to exist before REJECTED."
	},
	["Originating expression exists"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus RAW->UNDER_DISCOVERY, performed by beginIntentDiscovery (packages/rph-application/src/handlers/intent.ts:174-184). Its only precondition is fromStates('RAW') (intent.ts:178) and its payload schema is the empty strictObject (packages/rph-contracts/src/messages.ts:515). The handler never reads state.originatingExpression — grepped `originatingExpression` across packages/*/src: the only production references are captureIntent's write (intent.ts:54, :74) and the contracts schema; no reader on any transition. CaptureIntentPayloadSchema (messages.ts:95-100) types it `z.string()` with no min-length, so the empty string satisfies it. RAN THE ENGINE: CaptureIntent with `originatingExpression: ''` => ACCEPTED, then BeginIntentDiscovery => ACCEPTED. So both the field-presence and the field-content readings of the guard go unchecked at this arrow."
	},
	["Outcomes, non-goals, and constraints defined"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus PROVISIONAL->FORMALIZED, performed by formalizeIntent (packages/rph-application/src/handlers/intent.ts:200-232). Its only precondition is fromStates('PROVISIONAL') (intent.ts:205); the mutate writes desiredOutcomes/successConditions/nonGoals/constraintIds/stakeholderIds straight from the payload (intent.ts:207-216) with no emptiness or content check. FormalizeIntentPayloadSchema (packages/rph-contracts/src/messages.ts:102-110) is all bare `z.array(...)` and `z.string()` — empty arrays and the empty string all validate. RAN THE ENGINE: FormalizeIntent with `formalizedObjective: ''`, `desiredOutcomes: []`, `nonGoals: []`, `constraintIds: []` (everything empty) => ACCEPTED, intentStatus committed to FORMALIZED. Note the near-miss that must NOT be counted as coverage: approveIntent DOES enforce non-empty desiredOutcomes (INT-004, intent.ts:261-267), but that refusal is on the FORMALIZED->APPROVED arrow, one hop later, and says nothing about nonGoals or constraints."
	},
	["Parent exists and recomposition is required"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState SATISFIED->RECOMPOSING. RECOMPOSING is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS, so changePwuState is the only writer and none of its seven refusal functions branches on newState==='RECOMPOSING'; pwuGuards.ts:19-23 has no SATISFIED->RECOMPOSING entry. Critically, beginRecomposition (packages/rph-application/src/handlers/decomposition.ts:522) does NOT drive this axis — it advances RecompositionContract.status READY->EVALUATING, and its own comment at decomposition.ts:547-551 says so: \"`workLifecycleState` is DECLARED OPTIONAL and deliberately NOT emitted ... this handler advances the CONTRACT, never the parent PWU.\" Grepped every handler for writes of workLifecycleState: only pwu.ts writes it. RAN THE ENGINE: seeded a PWU (no parentWorkUnitId, and zero RecompositionContract objects in the store) to SATISFIED, ChangePwuState -> RECOMPOSING => ACCEPTED, committed. Neither conjunct — parent existence nor recomposition being required — is read."
	},
	["plan approval required before irreversible execution for high-risk work; approval grants NO runtime privileges (§20.2)"]: {
		disposition: "UNENFORCED",
		evidence: "`approveExecutionPlan` (packages/rph-application/src/handlers/execution.ts:355-383) DOES have a `guard:` (:372-382) — but it enforces a DIFFERENT rule, RPH-ASM-006, refusing at execution.ts:377 with 'it authorizes new work for a PWU whose assumption ... is EXPIRED'. Neither declared limb is evaluated: grep 'riskProfile|irreversib' over the whole of execution.ts returns ZERO hits, so 'high-risk work' is never read at this arrow, and there is no runtime-privilege write to negate (the handler sets `status` and emits `{status:'APPROVED'}` only). The 'approval required before execution' half is enforced on a DIFFERENT machine: packages/rph-application/src/handlers/execution.ts:721 `if (spec.planLiveness === 'REQUIRES_ACTIVE_PLAN' && plan.status !== 'ACTIVE')` gates the ExecutionStep commands, and ACTIVE is reachable only from APPROVED (canActivatePlan, execution.ts:482). That is real, but it is not this arrow, and it is unqualified by risk."
	},
	["plan revision preserves prior attempt history (§20.2)"]: {
		disposition: "UNENFORCED",
		evidence: "ALL FOUR ARROWS ARE ONE HANDLER, so the disposition is uniform: verif/arrow-command-census.ts `declaredArrows()` maps PROPOSED/UNDER_REVIEW/APPROVED/ACTIVE -> SUPERSEDED all to execution.ts:616 (`supersedeExecutionPlan`, packages/rph-application/src/handlers/execution.ts:613-652). Its `guard:` at :632-651 checks exactly two things and refuses on both: the successor plan exists (:635-641) and it belongs to the same PWU (:643-649). It never reads `steps`, attempts, executionAttemptId, or any history. There is no `mutate:` on the advanceStatus, so the base state's `steps` do survive and the event store is append-only — the condition is SATISFIED BY CONSTRUCTION but EVALUATED BY NOTHING, so adding a `mutate` that dropped steps would break the declared guard with no test and no refusal to catch it."
	},
	["privilege expansion requires a NEW authorization event (§22.1)"]: {
		disposition: "UNENFORCED",
		evidence: "REFUTED BY THE SKEPTIC PASS, having first been classified ENFORCED on runtime-binding.ts:204. That line refuses on GRANT CONTAINMENT (granted subset of requested) — which is verbatim the guard of the OTHER arrow, REQUESTED->AUTHORIZED. The row reused a sibling arrow's enforcing site. No value corresponding to \"a new authorization event occurred\" is ever computed, so nothing can refuse on it; the single write path makes the event a CONSEQUENCE, not a checked precondition. Driven: a legitimate expansion is ACCEPTED and emits one RuntimeBindingAuthorized."
	},
	["producer and evaluator share invocation/agent/model/provider/hidden-context/prompt-lineage/authority in violation of policy's IndependenceRequirement (§8.2)"]: {
		disposition: "ENFORCED",
		evidence: "TRIGGER-SHAPED GUARD, and it is genuinely computed on the write path from the STORE: `completeAssuranceAssessment` reads `policyState?.independenceRequirement` off the loaded ASSURANCE_POLICY (packages/rph-application/src/handlers/assurance.ts:1918), calls the kernel `checkIndependence` (packages/rph-assurance/src/assurance-rules.ts:184, which compares invocationId/contextInstanceId/agentId/modelId/providerId/actorType/orgId per the policy's requirement) at assurance.ts:1923-1927, and performs the arrow ONLY inside `if (!verdict.independent)` at assurance.ts:1928 -> advanceStatus to INDEPENDENCE_VIOLATION at :1929. When the condition is FALSE the arrow is not performed at all (control flow falls through to Gates D/A/C and a normal disposition) — a caller cannot assert this state. Live pipeline proof, both directions: packages/rph-application/src/handlers/assurance-independence.test.ts:216 (DIFFERENT_AGENT, producer===evaluator -> INDEPENDENCE_VIOLATION), :263 (the discriminating CONTROL — producer!==evaluator completes normally), :318 (DIFFERENT_MODEL dimension), :365 (both operands recorded). DISCLOSED RESIDUALS, so this is not overstated: (a) the gate is skipped when the policy names no requirement, names NONE, or the caller supplies no producer — a chosen fail-through pinned at :290, so a caller can DODGE the check by omitting `producer`; (b) 'prompt-lineage' has no corresponding Identity field, so that named dimension is not comparable.",
		enforcingSite: "packages/rph-application/src/handlers/assurance.ts:1928",
		enforcingAnchor: "if (!verdict.independent)"
	},
	["Recomposition contract satisfied"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState RECOMPOSING->RECOMPOSED. Same structure as the arrow above: RECOMPOSED is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS, changePwuState is the only writer, no refusal function branches on it, and completeRecomposition (packages/rph-application/src/handlers/decomposition.ts:629) advances only RecompositionContract.status EVALUATING -> COMPOSABLE|CONFLICTED|INSUFFICIENT — its comment at decomposition.ts:692-695 states \"`workLifecycleState` (optional, 'parent PWU RECOMPOSING->RECOMPOSED') is omitted — this handler advances the contract, not the parent PWU.\" RAN THE ENGINE: seeded a PWU to RECOMPOSING with ZERO RecompositionContract objects in the store, ChangePwuState -> RECOMPOSED => ACCEPTED. Stronger still, the condition is currently unsatisfiable in principle: verif/arrow-command-census.baseline.json lists `RecompositionContract.status COMPOSABLE -> SATISFIED` as uncovered, so no contract in this engine can ever reach status SATISFIED — yet the PWU arrow the guard protects fires freely."
	},
	["recomposition may fail even when all children individually satisfied (§14.1)"]: {
		disposition: "UNENFORCED",
		evidence: "THE CONDITION THAT DRIVES THIS ARROW IS CALLER-DECLARED, NOT DERIVED. `completeRecomposition` (packages/rph-application/src/handlers/decomposition.ts:629-718) computes `evaluateRecomposition(buildRecompositionInput(...))` at :666 and derives the target at :674. But the CONFLICTED branch of the kernel (packages/rph-domain/src/decomposition.ts, `evaluateRecomposition`) fires on exactly one input: `if (input.detectedConflicts.length > 0)` — and `buildRecompositionInput` at decomposition.ts:610 sets `detectedConflicts: (p.detectedConflicts ?? [])`, straight off the COMMAND PAYLOAD. `parentCompletionClaimSupported` and `parentConstraintsHoldAgainstWhole` (:616-617) are likewise caller-supplied with `?? true` defaults. Only `requiredChildResults[].acceptable` (:603) is derived from the store, and that limb produces INSUFFICIENT, not CONFLICTED. So nothing in production decides whether a conflict exists, nothing refuses a CONFLICTED the facts do not support, and nothing forces CONFLICTED when a real conflict exists but the caller declares none. The two refusals this handler does carry (:633 contract must exist, :656 the named parentCompletionClaimId must match the contract's) are about identity, not about the guard."
	},
	["recovery confirmed"]: {
		disposition: "UNENFORCED",
		evidence: "MEASURED LIVE (temporary probe through engine.dispatch, since deleted): RegisterValidator -> ACTIVE, MarkValidatorDegraded -> DEGRADED, then RestoreValidator with `reason: 'vibes — no run, no evidence, no health check'` -> ACCEPTED, status back to ACTIVE. CODE: `restoreValidator` (packages/rph-application/src/handlers/validator-registry.ts:95) is `statusChange('ACTIVE', 'ValidatorRestored', ['DEGRADED'])`, and the shared factory at :67-89 builds an `advanceStatus` whose ONLY constraint is `precondition: fromStates(...from)` (:81). There is no `guard:` in the whole file. The caller's `reason` is written to the event only (:86, with the comment '`reason` rides the event, not the snapshot'), so it is a free-text string nothing evaluates. No health check, no successful-execution evidence, no ValidatorRegistryEntry field is consulted. Not NOT_MECHANICALLY_CHECKABLE: 'recovery confirmed' is expressible (e.g. require a successful validator result since the degradation event) — it is simply absent."
	},
	["Replacement intent identified"]: {
		disposition: "ENFORCED",
		evidence: "~~All six arrows Intent.intentStatus {RAW, UNDER_DISCOVERY, PROVISIONAL, FORMALIZED, APPROVED, REVISED} -> SUPERSEDED. Same search as the WITHDRAWN row: the registry (packages/rph-application/src/handlers/registry.ts:113-118) registers six INTENT commands and none targets SUPERSEDED; I printed the live Object.keys(HANDLERS) and there is no SupersedeIntent among the 95 types; intent.ts is the only production writer of `intentStatus` (writes at intent.ts:61, 73, 149, 183, 229, 256, 311, targets RAW/UNDER_DISCOVERY/PROVISIONAL/FORMALIZED/APPROVED/REVISED only); there is no generic Intent-mutation command. The repository records the identical finding at packages/rph-domain/src/enforcement-register.ts:1473-1479 (RPH-INT-007): \"`SupersedeIntent` and `WithdrawIntent` occur nowhere in the repository ... SUPERSEDED and WITHDRAWN are declared terminal states of the ratified machine and both are unreachable.\" All six arrows appear in verif/arrow-command-census.baseline.json `uncovered`.~~ ⚠ EXPIRED 2026-08-13 BY REG-F-131 — FOUR COMMITS BEFORE ANY GATE EXISTED TO SAY SO. `SupersedeIntent` is registered and `supersedeIntent` (packages/rph-application/src/handlers/intent.ts:384) declares fromStates('RAW','UNDER_DISCOVERY','PROVISIONAL','FORMALIZED','APPROVED','REVISED') with target: 'SUPERSEDED' — exactly the six arrows this row said no command could perform. THE CITATION EXPIRED WITH IT: none of the six is in the baseline's `uncovered` any longer, while the three `-> WITHDRAWN` arrows still are, which is the positive control that the re-check discriminates. Its other citation is stale in the same way — RPH-INT-007 became ENFORCED at REG-F-132. THE NEW DISPOSITION IS UNENFORCED AND NOT ENFORCED, deliberately: `supersedeIntent` never resolves `supersedingIntentId`. It copies the caller's string into the emitted event and never loads the object that id names, so the guard text is not merely unenforced but unenforced-as-written — the REG-F-014 shape, a caller-supplied fact taken as true where the engine could have resolved it. AND THAT IS A LIVE DEFECT, not a labelling error: SUPERSEDED is terminal and `proposePwu` refuses a SUPERSEDED intent (REG-F-129), so a supersession naming a nonexistent successor STRANDS THE INTENT PERMANENTLY — no work authorized, no successor to propose against, no arrow out. Two increments one commit apart composed into a defect neither had alone. Enforcing the guard is the next increment; recording this as ENFORCED now would be exactly the false ENFORCED this ledger's skeptic pass was built to catch.~~ ✅ ENFORCED 2026-08-13, THE INCREMENT AFTER. `supersedeIntent` now resolves the successor through `ctx.store.loadObject` and refuses when it is absent, when it is not an INTENT, and when it is the target itself. THE TYPE CHECK IS A DEPARTURE FROM PWU-002, NOT A COPY OF IT: that check is existence-only and discloses its own hole at pwu.ts:255, so inheriting its shape would have let a PWU supersede an intent. That a command-created intent carries `objectType` at all was DRIVEN through the command bus before the guard was written — the literal `objectType: 'INTENT'` occurs elsewhere in this repository only in test seeds, so reading it would have been reading a fixture. MEASURED: the three refusal tests were observed RED (3 failed / 9 passed) before the guard existed, and the CONTROL — a real, existing INTENT successor — was green throughout, which is what stops a guard that refuses everything from re-closing the six arrows REG-F-131 opened. Two mutants declared, one per limb.",
		enforcingSite: "packages/rph-application/src/handlers/intent.ts:421",
		enforcingAnchor: "SupersedeIntent requires an existing INTENT as the superseding intent"
	},
	["requested capability is NOT granted capability; capability scope must be explicit (§22.1)"]: {
		disposition: "ENFORCED",
		evidence: "Both limbs are evaluated on the write path of this exact arrow. LIMB 1 ('requested is NOT granted'): the granted set is read from the command payload SEPARATELY from the request (packages/rph-application/src/handlers/runtime-binding.ts:184-185) and `grantedWithinRequest` (packages/rph-domain/src/execution.ts:393) REFUSES at runtime-binding.ts:204 with 'AuthorizeRuntimeBinding blocked (RPH_CAPABILITY_NOT_REQUESTED)'; and the arrow's TARGET is derived by `authorizationOutcome` (:124-129, `targetStates: ['AUTHORIZED','PARTIALLY_AUTHORIZED']`), so REQUESTED -> AUTHORIZED is reachable only when the grant actually COVERS the request — a narrower grant lands PARTIALLY_AUTHORIZED instead. LIMB 2 ('scope must be explicit'): a capability-less request is refused at creation, runtime-binding.ts:48-54 ('the request names no capability'), so the granted set is always compared against a non-empty explicit request. Live pipeline coverage: packages/rph-application/src/handlers/capbind-n4-grant-containment.test.ts:88 (kill), :103 (no partial write), :114/:122/:132 (three CONTROLs proving it discriminates), :166 (names every unrequested capability); packages/rph-application/src/handlers/partauth-derived-outcome.test.ts:86 (a covering grant yields AUTHORIZED).",
		enforcingSite: "packages/rph-application/src/handlers/runtime-binding.ts:204",
		enforcingAnchor: "AuthorizeRuntimeBinding blocked (${check.errorCode})"
	},
	["Required evidence available or deficit explicitly recorded"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState EVIDENCE_PENDING->UNDER_ASSURANCE. UNDER_ASSURANCE is not in PWU_SEMANTIC_LIFECYCLE_COMMANDS, so changePwuState is the sole writer; WORK_LIFECYCLE_CROSS_AXIS_GUARDS (packages/rph-domain/src/pwuGuards.ts:19-23) has no EVIDENCE_PENDING->UNDER_ASSURANCE entry, and none of the seven refusal functions in pwu.ts branches on newState==='UNDER_ASSURANCE'. I searched the handler write path for any read of evidence on this arrow — `evidenceRequirementIds` is written as `[]` by proposePwu (pwu.ts:253) and is read by NO handler; the admissibility machinery (floor-gate.ts, assurance.ts) hangs off the AssuranceAssessment aggregate and the assurance axis, never off this workLifecycle arrow. Neither disjunct is checked: no evidence need be available, and no deficit record need exist. RAN THE ENGINE: seeded a PWU to EVIDENCE_PENDING with zero Evidence objects, zero EvidenceRequirements and no deficit record anywhere, ChangePwuState -> UNDER_ASSURANCE => ACCEPTED, committed."
	},
	["required evidence becomes unavailable during assessment"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "NO COMMAND PERFORMS ASSESSING -> EVIDENCE_PENDING. Measured by running verif/arrow-command-census.ts `declaredArrows()` — no site returns for this arrow — and verif/arrow-command-census.baseline.json lists 'AssuranceAssessment.state ASSESSING -> EVIDENCE_PENDING' under `uncovered`. Confirmed independently by censusing every production write of the status field: grep 'assessmentState' over packages/*/src (non-test) yields exactly two direct writes, packages/rph-application/src/handlers/assurance.ts:1284 (the BIRTH, EVIDENCE_PENDING|READY) and :1457 (`advancesToReady`, which fires only when `assessmentState === 'EVIDENCE_PENDING'` and sets READY — the wrong direction), plus six `advanceStatus` sites whose targets are ASSESSING (:1568), CANCELLED (:1616), INDEPENDENCE_VIOLATION (:1933), ESCALATED (:1991) and the disposition range (:2056). None targets EVIDENCE_PENDING. The remaining hits in packages/rph-projections/src/assurance-view.ts are a read-model. The machine's own trigger for the arrow is 'evidence access failure — required evidence cannot be retrieved (§34.3)', and no handler exists for it. (Source state ASSESSING IS occupiable — that is not the reason; the reason is that no command drives the arrow.)"
	},
	["requires a supported claim — NOT merely because a related PWU is completed (§10.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "BOTH ARROWS, SAME REASON — the Obligation.status machine has no mover at all. MEASURED LIVE (temporary probe, since deleted): AssertObligation -> ACCEPTED, obligation status = PROPOSED, and there is no further command. CENSUSED: `AssertObligation` is the ONLY entry in COMMAND_SPECS with `targetAggregateType: 'OBLIGATION'` (packages/rph-contracts/src/messages.ts:1988-1990) and the only Obligation row in the handler registry (packages/rph-application/src/handlers/registry.ts:204); `assertObligation` (packages/rph-application/src/handlers/obligation-constraint.ts:21-49) is a `createObject` that mints the object in PROPOSED (:33) and declares NO `births`. grep \"'Obligation.status'\" over packages/rph-application/src returns ZERO hits — no `advanceStatus` names the machine. verif/arrow-command-census.ts confirms both facts mechanically: 'Obligation.status' appears in the baseline's `orphans` list (no command declares any arrow on it) and its `birthStates()` entry is empty. So ACTIVE and ALLOCATED are never occupied and neither ACTIVE -> SATISFIED nor ALLOCATED -> SATISFIED can ever fire."
	},
	["requires an explicit EFFECTIVE promotion decision (§23.2, §24.2); promotion evidence must be retained; promotion is a governance event, not an execution step (§24.2)"]: {
		disposition: "ENFORCED",
		evidence: "THE ONE REFUSABLE LIMB IS REFUSED, ON THIS ARROW, FROM STORE FACTS. `promoteBaseline` (packages/rph-application/src/handlers/governance.ts:841) resolves the cited decision by id from the store and reads its real status/decisionType/authority (:854-868), passes it into `canPromoteBaseline` (packages/rph-domain/src/governance.ts:368) at governance.ts:893, and REFUSES at packages/rph-application/src/handlers/governance.ts:910-914 `return reject(command, 'RPH_INVARIANT_VIOLATION', 'Cannot promote baseline ...: ' + codes)`. The kernel arm is explicit: `d.status === 'EFFECTIVE' && d.authorityHeld && (decisionType === 'PROMOTE_BASELINE' || 'APPROVAL')` else NO_EFFECTIVE_PROMOTION_DECISION (rph-domain/src/governance.ts:373-382), plus REG-F-073's scope arm PROMOTION_DECISION_OUT_OF_SCOPE (:402-408) requiring the decision to NAME this baseline. Independently corroborated by packages/rph-domain/src/enforcement-register.ts:3520, which records ENFORCEMENT as 'promoteBaseline's guard' with `refusalMarker: 'NO_EFFECTIVE_PROMOTION_DECISION'` (:3522) — a probe that runs live. The same reject line is proven reachable from the bus by packages/rph-application/src/handlers/baseline-open-blocking-observation.test.ts:191. LIMBS 2 AND 3 ARE NOT REFUSABLE CONDITIONS and are noted rather than counted: 'promotion evidence must be retained' is an EFFECT the handler performs unconditionally (`mutate: (base) => ({ ...base, promotionDecisionId: p.promotionDecisionId })` at governance.ts:966 + the BaselinePromoted event), and 'promotion is a governance event, not an execution step' is a categorisation, not a testable precondition.",
		enforcingSite: "packages/rph-application/src/handlers/governance.ts:910",
		enforcingAnchor: "Cannot promote baseline ${command.targetAggregateId}: ${codes}"
	},
	["Revised intent receives authorization"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow Intent.intentStatus REVISED->APPROVED is performed by `approveIntent` (packages/rph-application/src/handlers/intent.ts:235, `precondition: fromStates('FORMALIZED', 'REVISED')` at :246). Its ONLY two prechecks are INT-004 desired-outcome (intent.ts:261-267) and the semantic-version pin (intent.ts:275-281). `decisionId` is `z.string()` (packages/rph-contracts/src/messages.ts:113) and is NEVER resolved — grepped every production reference to ApproveIntent and to `decisionId` in intent.ts: it is read once, at intent.ts:254, only to fill the event payload. RAN THE ENGINE: drove CaptureIntent->...->ApproveIntent->ReviseIntent->ApproveIntent with `decisionId: 'dec_nope_2'`; result ACCEPTED, intentStatus APPROVED, and `store.loadObject('dec_nope_2')` === undefined. A string naming no object is the whole of the 'authorization'. The shipped test packages/rph-application/src/handlers/dwp03-precondition-coverage.test.ts:127-133 already asserts this same arrow ACCEPTED with `decisionId: 'dec_2'`, an id the fixture never creates."
	},
	["revision preserves parent identity but increments semantic version (§13.2)"]: {
		disposition: "UNENFORCED",
		evidence: "ALL THREE ARROWS ARE ONE HANDLER (census `declaredArrows()` maps VALID/CONDITIONALLY_VALID/INVALID -> SUPERSEDED to decomposition.ts:451), so the disposition is uniform. NOTHING EVALUATES EITHER LIMB. 'increments semantic version' is an unconditional EFFECT: `bumpSemanticVersion: true` at packages/rph-application/src/handlers/decomposition.ts:462 — the handler performs it, and no code asks whether it happened. 'preserves parent identity' holds by construction of a whitelist, not by a check: `mutate: (base) => ({ ...base, ...revisions })` (:452) where `revisions` is filtered to HONOURED_REVISION_FIELDS = ['obligationAllocations','constraintPropagations'] (:413), so `parentWorkUnitId` is untouchable. The one refusal in the handler (:419-429) is about a DIFFERENT field — it refuses `childWorkUnitIds` because DEC-2's impact analysis is unbuilt — not about parent identity or versioning. So a future `mutate` or a widened HONOURED_REVISION_FIELDS could break the declared guard with no refusal and no production control to catch it (only decomposition-revise-conformance.test.ts asserts the bump, and a test is not enforcement)."
	},
	["revocation triggers impact analysis; a decision cannot retroactively change evidence (§23.2)"]: {
		disposition: "UNENFORCED",
		evidence: "LIMB 1 IS UNBUILT AND THE HANDLER SAYS SO IN ITS OWN COMMENT. `revokeDecision` (packages/rph-application/src/handlers/governance.ts:436-475) carries `precondition: fromStates('EFFECTIVE')` (:444) and a `guard:` (:465-471) that enforces a DIFFERENT rule — REG-E-031/REG-F-048 authority symmetry, refusing an AGENT issuer with RPH_AUTHORITY_INSUFFICIENT. No impact analysis is triggered or required: packages/rph-application/src/handlers/governance.ts:456 states verbatim 'the impact analysis that would make that safe is itself unbuilt', and I confirmed the plane does not exist — grep `impactedObjects` across packages/ (non-dist) returns only packages/rph-domain/src/traceability.ts:208 (definition), its own unit test, and three prose citations (decomposition.ts:400/408 'impactedObjects has no caller and needs a TraceLink-minting surface'; enforcement-register.ts:1460). LIMB 2 ('cannot retroactively change evidence') is satisfied by construction — the advanceStatus writes only `status`/revocation fields and touches no Evidence aggregate — but nothing evaluates it, so there is no control."
	},
	["Runtime bindings authorized"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow PWU.workLifecycleState PLANNED->EXECUTING. EXECUTING is NOT in PWU_SEMANTIC_LIFECYCLE_COMMANDS (packages/rph-application/src/handlers/pwu.ts:328-335), so `changePwuState` (pwu.ts:1057) is the write path and REG-F-072's route closure (pwu.ts:934-955) does not apply. The four 'may not be asserted' guards it chains (pwu.ts:1106-1117) cover disposition / baselining / execution-success / abandonment — none reads a RuntimeBinding. WORK_LIFECYCLE_CROSS_AXIS_GUARDS (packages/rph-domain/src/pwuGuards.ts:19-23) has exactly three entries and PLANNED->EXECUTING is not one of them. RAN THE ENGINE: drove a PWU to READY then `ChangePwuState` READY->PLANNED then PLANNED->EXECUTING with `supportingObjectIds: []`; ACCEPTED, workLifecycleState EXECUTING, and zero RuntimeBinding events in the store. The shipped test packages/rph-application/src/handlers/generic-setter-scope.test.ts:167 performs the identical arrow and asserts ACCEPTED."
	},
	["Shape readiness policy satisfied (§9 Shape Readiness Profile)"]: {
		disposition: "ENFORCED",
		evidence: "packages/rph-application/src/handlers/pwu.ts:496 — `return reject(` inside `markPwuReady`, guarded by `if (!readiness.ok) {` at :495, after `const readiness = checkPwuShapeReadiness(readinessFactsOf(ctx, loaded.state));` at :494. Code RPH_VALIDATION_SEMANTIC_FAILED; message names every unmet limb. The kernel predicate is packages/rph-domain/src/pwuGuards.ts:125 `checkPwuShapeReadiness`, and it IS called by a handler (pwu.ts:494) — not a dead predicate. The only OTHER route to READY is closed: READY is in PWU_SEMANTIC_LIFECYCLE_COMMANDS (pwu.ts:330) so `changePwuState` refuses it at pwu.ts:945, proven by generic-setter-scope.test.ts:153. DISCLOSED PARTIALITY: 4 of the 10 §9.1 limbs are deliberately withheld and the withholding is documented at pwuGuards.ts:50-59 — the completion-claim, mandatory-constraints, current-assumptions and identified-authority limbs are not checked. Ran pwu-readiness-contract.test.ts + generic-setter-scope.test.ts at HEAD: 42 tests green.",
		enforcingSite: "packages/rph-application/src/handlers/pwu.ts:496",
		enforcingAnchor: "does not satisfy the shape readiness contract"
	},
	["step outputs recorded or explicit no-output"]: {
		disposition: "UNENFORCED",
		evidence: "THE ARROW IS PERFORMABLE (so not ARROW_UNREACHABLE, despite the census reporting 'no command': verif/arrow-command-census.ts reads only `advanceStatus` sites + STEP_COMMAND_SPECS, and PWU.executionState is driven by the generic setter instead). `ChangePwuState` moves it: packages/rph-application/src/handlers/pwu.ts:1092 runs `checkTransition('PWU.executionState', current, p.executionState)` and :1159 commits the caller's value. THE GUARD ON THAT PATH ASKS A DIFFERENT QUESTION. `rejectUnbackedExecutionSuccess` (pwu.ts:773-819) fires only for `executionState === 'SUCCEEDED'` (:780) and refuses at packages/rph-application/src/handlers/pwu.ts:809-818 with RPH_EVIDENCE_MISSING — but what it demands is a cited EXECUTION_PLAN whose `workUnitId` matches and whose `planEvidencesExecutionSuccess` verdict holds (status ACTIVE|COMPLETED, every step SUCCEEDED|SKIPPED, at least one SUCCEEDED). It reads `steps[].stepState` and the plan status. It never reads outputArtifactIds, proposedEvidenceIds, noOutputResult or outputBindings. THE OUTPUT RULE IS REAL BUT ON THE OTHER MACHINE, under its own distinct guard text: ExecutionStep.stepState RUNNING -> SUCCEEDED declares 'must record outputs or an explicit no-output result; step success does NOT imply PWU success (§21.1)' and IS enforced by `validateStepCompletion` (packages/rph-domain/src/execution.ts:582) refusing at packages/rph-application/src/handlers/execution.ts:1351. Concretely, packages/rph-application/src/handlers/execrem-wp12-execution-success.test.ts:326 ('SKIPPED alongside SUCCEEDED is ACCEPTED') drives the PWU to executionState SUCCEEDED over a plan containing SKIPPED steps, which recorded neither outputs nor an explicit no-output."
	},
	["subject semantic version changed (INV-16) OR supporting evidence invalidated (INV-15)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: AssuranceAssessment.state SATISFIED -> INVALIDATED. transitions.data.ts:1277 names its trigger `invalidateAssuranceAssessment (AssuranceAssessmentInvalidated)` — NEITHER exists. I enumerated the WHOLE command surface at runtime, not by reading: a live probe printed `Object.keys(HANDLERS).filter(/Invalidate/i)` = ['InvalidatePwu','InvalidateEvidence'] (registry.ts:118-227). The only two production sites that target INVALIDATED are assurance.ts:608 (machine `Evidence.status`) and pwu.ts:606 (machine `PWU.workLifecycleState`); grep for INVALIDATED across packages/rph-application/src + packages/rph-engine/src (non-test) returns no third. So no command writes assessmentState='INVALIDATED' and the arrow cannot be performed — corroborated by verif/arrow-command-census.baseline.json listing 'AssuranceAssessment.state SATISFIED -> INVALIDATED' under `uncovered`. The SOURCE state is occupiable (CompleteAssuranceAssessment reaches SATISFIED), so this is an unbuilt arrow, not an unoccupiable source. Nothing evaluates INV-15/INV-16 for this axis anywhere."
	},
	["subject semantic version changed (INV-16) OR supporting evidence invalidated (INV-15) OR a required condition ceased to hold"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: AssuranceAssessment.state CONDITIONALLY_SATISFIED -> INVALIDATED. Identical mechanism to the SATISFIED->INVALIDATED row: transitions.data.ts:1284 names `invalidateAssuranceAssessment (AssuranceAssessmentInvalidated)`, and the live registry dump shows no such command (only InvalidatePwu / InvalidateEvidence). No advanceStatus/createObject site in any of the 12 production handler files that write objects targets INVALIDATED on machine `AssuranceAssessment.state`. verif/arrow-command-census.baseline.json lists 'AssuranceAssessment.state CONDITIONALLY_SATISFIED -> INVALIDATED' under `uncovered`. The third limb — 'a required condition ceased to hold' — has no representation anywhere either: no CONDITIONALLY_SATISFIED assessment records its conditions as checkable objects."
	},
	["superseded by a stronger constraint (§11.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: Constraint.status ACTIVE -> SUPERSEDED. Unreachable TWICE OVER. (1) No command performs it: the only production write to a CONSTRAINT object in the repository is obligation-constraint.ts:71 `createObject(... objectType: CONSTRAINT ...)`, which hardcodes `status: 'PROPOSED'` (obligation-constraint.ts:69). I confirmed the write surface is closed by enumerating every production caller of kit.ts's commitState/createObject — 12 handler files, and there are ZERO direct `store.saveObject`/`writeObject` calls outside the kit — then grepping each for `objectType: CONSTRAINT`: one hit, the mint. A live registry dump gave `Object.keys(HANDLERS).filter(/Constraint/i)` = ['AssertConstraint']. (2) The SOURCE state is never occupied: Constraint.status PROPOSED -> ACTIVE is itself uncovered (verif/arrow-command-census.baseline.json), so no constraint is ever ACTIVE. `Constraint.status` is also in that baseline's `orphans` list (no handler declares any arrow on the machine) and in verif/binding-row-truth.test.ts:43's UNANALYSED list (no declared birth). decomposition.ts:130 `LIVE_CONSTRAINT_STATUSES = new Set(['PROPOSED','ACTIVE'])` merely READS a status; it is a decomposition input, not a write path, and 'stronger constraint' is evaluated nowhere."
	},
	["the assessment is ACTIVE — it has not reached a disposition"]: {
		disposition: "REDUNDANT_WITH_MACHINE",
		evidence: "4 arrows: AssuranceAssessment.state {REQUESTED,EVIDENCE_PENDING,READY,ASSESSING} -> CANCELLED, driven by cancelAssuranceAssessment (assurance.ts:1604), which declares `precondition: fromStates('REQUESTED', 'EVIDENCE_PENDING', 'READY', 'ASSESSING'),` at assurance.ts:1619. That looks like enforcement and I nearly recorded it as such — I MUTATED IT INSTEAD. Widening assurance.ts:1619 to all fifteen states left packages/rph-application/src/handlers/assessment-cancellation.test.ts ENTIRELY GREEN (4/4 pass, including its :171 'is REFUSED once the assessment has concluded'), because the transition table has CANCELLED in-arrows from exactly the four ACTIVE states and no other, so kit.ts:172 checkTransition refuses every concluded source on its own. The handler's own comment at assurance.ts:1617-1618 says the same: 'this precondition is the machine's own shape, not a second opinion about it.' assessment-cancellation.test.ts:166 pins the control (`classifyTransition('AssuranceAssessment.state','SATISFIED','CANCELLED')` is not LEGAL). Caveats recorded rather than hidden: (a) the precondition IS strictly stronger in one case the four ledger arrows do not cover — the CANCELLED->CANCELLED self-edge, which kit.ts:670 says checkTransition admits as NOOP; my throwaway probe showed the mutant then ACCEPTS a re-cancel, and NO existing test covers that, so today the surplus is untested. (b) REQUESTED -> CANCELLED is additionally listed in the baseline's `dead` set (REQUESTED is never occupied — requestAssuranceAssessment births at EVIDENCE_PENDING/READY, assurance.ts:1295-1300), but the other three arrows are live: my probe drove READY -> CANCELLED to ACCEPTED against the real engine."
	},
	["validator crashed / timed out / errored during execution"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: AssuranceAssessment.state ASSESSING -> VALIDATOR_FAILED. Proven at the engine, not read: I dispatched CompleteAssuranceAssessment from ASSESSING with `dispositionRecommendation: 'VALIDATOR_FAILED'` and it was refused at the contract boundary — RPH_VALIDATION_SCHEMA_FAILED, 'Invalid option: expected one of \"SATISFIED\"|\"CONDITIONALLY_SATISFIED\"|\"REJECTED\"|\"INCONCLUSIVE\"|\"ESCALATED\"'. That is enums.ts:96-102 AssuranceDispositionRecommendationSchema, which is literally the `targetStates` of the only general completion site (assurance.ts:2064), so VALIDATOR_FAILED is outside the reachable target range by construction. No other production site targets it: grepping VALIDATOR_FAILED across packages (non-dist) finds it only in rph-assurance as a BOUNDARY CLASSIFICATION (assurance-rules.ts:262, floor.ts:332) which recording.ts:83 then explicitly folds OUT of recordable dispositions, plus validator-registry.ts:91 which moves a DIFFERENT machine (ValidatorRegistryEntry.status ACTIVE->DEGRADED) and says so at validator-registry.ts:23. Baseline agrees: 'AssuranceAssessment.state ASSESSING -> VALIDATOR_FAILED' is `uncovered`. Nothing can perform the arrow, so the crash/timeout condition cannot fire."
	},
	["validator or policy cannot resolve the issue within its authority/competence (§10.1); may also be the disposition for an open CRITICAL finding (§10.3)"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: AssuranceAssessment.state ASSESSING -> ESCALATED, and it IS performable — I drove it live. From ASSESSING I dispatched CompleteAssuranceAssessment with `dispositionRecommendation: 'ESCALATED'` and EMPTY claimResults/observations/limitations/residualUncertainty, no open finding of any severity, and no policy escalationRules: result ACCEPTED, assessmentState = ESCALATED. The write path is assurance.ts:2064-2065 (`targetStates: AssuranceDispositionRecommendationSchema.options`, `target: disposition`), whose only precondition is `fromStates('ASSESSING')` (assurance.ts:2071) — a source-state check, not the guard. Nothing evaluates 'cannot resolve within its authority/competence': there is no authority/competence field on the validator result or the policy that any handler reads for this decision. The nearby Gate D (assurance.ts:1975-2000, `target: 'ESCALATED'` at :1991) is the OPPOSITE of a refusal — it FORCES escalation when a CRITICAL observation is open — and Gate A's own comment at assurance.ts:2007-2010 states 'Negative dispositions (REJECTED / ESCALATED / INCONCLUSIVE) are deliberately NOT gated.' So an unjustified ESCALATED verdict is admitted; no line refuses."
	},
	["validator selected AND required independence verified (AssuranceIndependenceVerified) AND context assembled"]: {
		disposition: "UNENFORCED",
		evidence: "Arrow: AssuranceAssessment.state READY -> ASSESSING, driven by beginAssuranceAssessment (assurance.ts:1562). Its ONLY precondition is `precondition: fromStates('READY'),` (assurance.ts:1569) — the source state, not any of the three limbs. Driven live: from a freshly requested assessment sitting in READY I asserted `state.evaluator === undefined` (no SelectAssuranceEvaluator ever dispatched), then dispatched BeginAssuranceAssessment → ACCEPTED, assessmentState = ASSESSING. Limb 1 'validator selected' is therefore refused by nothing; selectAssuranceEvaluator (assurance.ts:1501) exists but is optional and explicitly 'does NOT move the machine' (assessment-ready-arrow.test.ts:189), and its own precondition test at :211 shows Begin succeeding first with no evaluator at all. Limb 2 'required independence verified' IS checked — but on a DIFFERENT arrow, at CompleteAssuranceAssessment (checkIndependence → INDEPENDENCE_VIOLATION, assurance.ts:1925-1957), and the handler comment at assurance.ts:1865 says so ('WHY HERE AND NOT AT SELECTION'). Checked-later-elsewhere is not this arrow's enforcement. Limb 3 'context assembled' has no representation in any handler; grep for the phrase and for any assembled-context predicate across packages/rph-application/src finds nothing."
	},
	["waived through authority (§11.2)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: Constraint.status ACTIVE -> WAIVED. Same closed write surface as the ACTIVE -> SUPERSEDED row: the ONLY production write to a CONSTRAINT object is obligation-constraint.ts:71 createObject, hardcoding `status: 'PROPOSED'` (obligation-constraint.ts:69); there is no advanceStatus site anywhere naming machine 'Constraint.status' (grep across packages, non-dist, finds the string only in transitions.data.ts:723, a messages.ts binding row at :2972, verif/binding-row-truth.test.ts:43 and one comment in obligation-constraint.ts:12). Live registry dump: the only constraint command is AssertConstraint. And the source state ACTIVE is never occupied — PROPOSED -> ACTIVE is itself `uncovered` in verif/arrow-command-census.baseline.json. Note the CONTRAST that makes this a real gap and not a naming issue: GrantWaiver DOES exist (registry.ts:194) but governance.ts:627 builds it from makeDecisionEffective, which writes `objectType: DECISION`, `machine: 'Decision.status'`, target EFFECTIVE — it never touches a Constraint. Authority IS checked there (governance.ts:311 `authority?.actorType === 'HUMAN'`) for a decision, which is a different object; nothing carries that check onto a constraint."
	},
	["waiver authority separately defined (§18.1)"]: {
		disposition: "UNENFORCED",
		// ⚠ PARTIALLY CLOSED 2026-08-09 BY JAN-PWUWP W-3, AND DELIBERATELY NOT PROMOTED TO ENFORCED. The hole this
		// row MEASURED — `ChangePwuState` reaching WAIVED with `supportingObjectIds: []` and no authority of any
		// kind — is closed: `rejectUnauthorizedWaiver` (pwu.ts) now requires an EFFECTIVE decisionType=WAIVER
		// Decision naming this PWU at its current semanticVersion. RE-DRIVEN through the bus: nothing cited, a
		// dangling id, a non-DECISION, an APPROVAL, and a PROPOSED waiver are each REJECTED, and the identical
		// dispatch is ACCEPTED once a granted waiver is cited (waiver-authority.test.ts).
		//
		// BUT THE ROW'S OWN RULE IS NARROWER THAN THE HOLE, WHICH IS WHY THE DISPOSITION STANDS. §18.1 / ASR-14's
		// last sentence says *"A policy cannot waive its own blocking finding unless waiver authority is
		// separately defined"* — a SELF-WAIVER check keyed on the relationship between the waiving policy and the
		// finding it waives. Nothing checks that, and no field records a waiver authority TIER for it to check
		// against (the same gap `waiver-authorization.ts` records for ASR-14's tiering clause). Promoting this to
		// ENFORCED on the strength of the general guard would be the false-ENFORCED this ledger exists to catch.
		evidence: "3 arrows: PWU.assuranceState {ASSESSING, EVIDENCE_REQUIRED, CONDITIONALLY_SATISFIED} -> WAIVED. The arrow-command census calls PWU.assuranceState an `orphan` — that is an ARTEFACT of the census only walking advanceStatus call sites; pwu.ts moves this axis by a different idiom and the arrow is REAL. I drove it: ChangePwuState UNASSESSED -> EVIDENCE_REQUIRED (ACCEPTED), then ChangePwuState assuranceState -> WAIVED with `supportingObjectIds: []`, `reasonCode: 'CONTROLLER'`, no Decision, no WaiverGranted event, no authority of any kind → ACCEPTED, and the committed PWU object reads `assuranceState: 'WAIVED'`. The write path is changePwuState (pwu.ts:1057); the sub-axis check at pwu.ts:1093 is `checkTransition` only (pure machine legality); the substance guard at pwu.ts:1107 is rejectUnbackedDisposition, which SHORT-CIRCUITS on this exact value at pwu.ts:686 — `if (!ASSESSMENT_BACKED_DISPOSITIONS.has(p.assuranceState)) return undefined;` — because pwu.ts:642-647's set is {SATISFIED, CONDITIONALLY_SATISFIED, REJECTED, ESCALATED}. The repository states the gap itself at pwu.ts:636-639: 'NOT here, deliberately: WAIVED is authorized by a WAIVER, not an assessment (§18.1 ...). Both need their own citation and their own guard'. GrantWaiver (governance.ts:627) writes only the DECISION aggregate and is never consulted here. One arrow proven live; all three share the identical code path with no per-source branch."
	},
	["waiver includes scope, rationale, authority, duration; human override must not erase prior findings (§23.2, Scenario 4)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow: AssuranceObservation.disposition OPEN -> WAIVED. The observation's disposition is WRITE-ONCE at mint and never moved again. Only production write to an ASSURANCE_OBSERVATION object: assurance.ts:2177 `objectType: OBSERVATION` inside recordAssuranceObservation, with `disposition: 'OPEN'` hardcoded on both the object (assurance.ts:2162) and the event (assurance.ts:2191). I closed the search over the whole write surface rather than one file: every production caller of kit.ts commitState/createObject is one of 12 handler files (no direct store.saveObject/writeObject exists outside the kit), and only that one site names OBSERVATION. Live registry dump: `Object.keys(HANDLERS).filter(/Observation/i)` = ['RecordAssuranceObservation']. GrantWaiver exists but governance.ts:627 targets machine 'Decision.status' on the DECISION aggregate. The machine is also in the baseline's `orphans` list and all five of its arrows are `uncovered`. WATCH-OUT recorded: governance.ts:121-128 asserts WAIVED is 'reached only by the WaiverGranted event' and rph-projections/src/assurance-view.ts `foldWaiverResolved` does derive a waived view — that is a READ MODEL, not the write path, and it does not make the object arrow performable. Second limb, 'human override must not erase prior findings', is separately unenforceable here for want of any deletion path to refuse."
	},
	["WaiverRule.maximumDuration / recorded expiration reached, OR revalidationTrigger fired, OR a new subject semantic version (waiver does not apply to future semantic versions unless explicitly renewed — §12.2, INV-14)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow AssuranceAssessment.state WAIVED->WAIVER_EXPIRED. Enumerated every AssuranceAssessment.state target in the codebase: packages/rph-application/src/handlers/assurance.ts:1568 ASSESSING, :1616 CANCELLED, :1933 INDEPENDENCE_VIOLATION, :1991 ESCALATED, :2064 `targetStates: AssuranceDispositionRecommendationSchema.options`, plus the direct commitState to READY at assurance.ts:1457. That enum is exactly [SATISFIED, CONDITIONALLY_SATISFIED, REJECTED, INCONCLUSIVE, ESCALATED] (packages/rph-contracts/src/enums.ts:96-102) — WAIVED is not in it. Grepped `WAIVER_EXPIRED` across all packages/*/src non-test files: it occurs only in the enum, the transition table, a doc comment and rph-domain/src/governance.ts:609 (a status list), never as a write. `GrantWaiver` (packages/rph-application/src/handlers/governance.ts:627) is `makeDecisionEffective` — it moves Decision.status, not AssuranceAssessment.state. So the source state WAIVED is never occupied and no command targets WAIVER_EXPIRED. Matches verif/arrow-command-census.baseline.json:18 and :13."
	},
	["WaiverRule.waiverAllowed; criterion ∈ eligibleCriteriaIds; finding severity ∉ prohibitedFindingSeverities; required authority present; rationale/compensating controls recorded (§12). NOT waivable: unauthorized intent alteration (§15.11), critical baseline-integrity failures by ordinary authority (§26.7)"]: {
		disposition: "ARROW_UNREACHABLE",
		evidence: "Arrow AssuranceAssessment.state ASSESSING->WAIVED. No command drives this machine to WAIVED: `completeAssuranceAssessment`'s range is `targetStates: AssuranceDispositionRecommendationSchema.options` (packages/rph-application/src/handlers/assurance.ts:2064) = [SATISFIED, CONDITIONALLY_SATISFIED, REJECTED, INCONCLUSIVE, ESCALATED] (packages/rph-contracts/src/enums.ts:96-102), and the four other advanceStatus sites on this machine target ASSESSING/CANCELLED/INDEPENDENCE_VIOLATION/ESCALATED (assurance.ts:1568, :1616, :1933, :1991). Grepped `target: 'WAIVED'` across every packages/*/src non-test file: zero hits. Waiver machinery in this engine lives on Decision.status (governance.ts:514 requestWaiver, :627 grantWaiver, :682 denyWaiver) and on the floor-waiver read path, neither of which writes an assessment state. Matches verif/arrow-command-census.baseline.json:13."
	},
};
