# W2 Evidence — Persistence, Events, and Projections Current-State (code-grounded)

**Method:** direct code inventory of `packages/rph-persistence`, `packages/rph-projections`, `packages/rph-ports`, and the engine/host call sites (fan-out reconnaissance, 45 reads). Every verdict below is file:line-grounded. This discharges the master §15 requirement that the W2 detailed roadmap be code-grounded, not a restatement of the master WPs.

## Port contract

`packages/rph-ports/src/ports/storage.ts` defines the `StorageAdapter` interface (`getReceipt`, `loadObject`, `commit`, `transaction`, `readAggregateEvents`, `readAllEvents`, `readPendingOutbox`, `markOutboxPublished`, `close`) plus `StoredObject`/`CommandReceiptRecord`/`OutboxRecord`/`CommitInput`/`CommitResult`. Two implementations: `SqliteStorageAdapter` (durable) and `SnapshotOverlayStorageAdapter` (in-memory fork for authoring). The header states the load-bearing invariants (atomic events+outbox, optimistic concurrency, idempotency, append-only) live here so a future Postgres/RLS adapter "slots in behind the same interface."

## Per-WP verdicts

| WP | Verdict | Evidence |
| --- | --- | --- |
| **2-001 Schema & migrations** | **Tables EXIST; migrations ABSENT** | `schema.ts` `SCHEMA_SQL` — 5 tables: `professional_work_objects` (snapshot), `professional_work_object_versions` (append-only per-revision history), `domain_events` (event store; `global_sequence` PK, `UNIQUE(aggregate_type,aggregate_id,aggregate_revision)` no-gap guard), `outbox_messages` (`status PENDING/PUBLISHED`, `idx_outbox_pending`), `command_receipts` (`idempotency_key` PK). Created inline (`CREATE TABLE IF NOT EXISTS`, `sqlite-storage-adapter.ts:49`). **No `PRAGMA user_version`, no migration/versioning table, no ALTER path.** |
| **2-002 Aggregate repos & optimistic concurrency** | **EXISTS** | `commit()` (`sqlite-storage-adapter.ts:88-190`) wholly inside `db.transaction`. Optimistic check `:90-100` (expected=undefined ⇒ must-not-exist; else revision must match) returns `REVISION_CONFLICT` (not throw); `UPDATE … WHERE id=? AND revision=?` second guard. Version history inserted every commit `:137-141`. Handler tier honors `command.expectedRevision` (`kit.ts:92-102`). Replay equivalence proven RPH-PER-006. **Gap:** `expectedRevision` honored-if-sent, not required on every update (`kit.ts:88-91`). |
| **2-003 Event store & outbox** | **Store+outbox+atomic EXIST; relay PARTIAL** | Single transaction boundary `sqlite-storage-adapter.ts:89` writes state+version+events+outbox+receipt atomically. `Engine.drainOutbox()` (`command-bus.ts:256-263`) reads PENDING → in-process subscribers → `markOutboxPublished`. **Gaps:** subscribers are in-process closures; no broker/retry/dead-letter/cross-process relay; **nothing calls `drainOutbox()` at startup** (only caller: `authoring-turn.ts:413`). |
| **2-004 Receipts & idempotency** | **EXISTS (successes)** | Dedup at dispatch `command-bus.ts:104-116` — a repeat `idempotencyKey` returns `{status:'DUPLICATE', producedEventIds:[…]}` and does NOT route to the handler. Receipt written inside the atomic commit (`kit.ts:280-289`). **Gaps:** only ACCEPTED commands write receipts (a re-sent *failed* command re-executes — safe, it produced no side effect); replay returns only `producedEventIds`, not the full prior `CommandResult`. |
| **2-005 Artifact & evidence storage** | **PARTIAL** | Artifacts/Evidence are ordinary `professional_work_objects` rows. `contentHash` (`rph-contracts/hash.ts:86`) hashes OBJECT STATE (used for `receipt.resultHash`). Artifact `contentHash` is caller-supplied, never computed over content (`artifact.ts:58-60`); §18.3 supersession NOT implemented (`artifact.ts:42-44`). Evidence `provenance.contentHash` is optional and never populated (`assurance.ts:340-375`). **Access control ABSENT** (`securityClassification`/`retentionClass` are open text; `ArtifactStore`/`CapabilityAuthorizer` are future ports, `ports/index.ts:2-3`). |
| **2-006 Rebuildable projections** | **Framework + Work/Assurance EXIST; Traceability + Compatibility gaps** | `projector.ts`: `Projector<V>`, `rebuildProjection(projector, events)` (fold from empty), `IncrementalProjection` (idempotent-by-eventId + checkpoint + `rebuild()`). Work view (`work-projection.ts` `workProjector`, proven rebuildable RPH-PER-007) and Assurance view (`assurance-view.ts`) are event-folds. **Gaps:** Traceability `TraceGraph` (`rph-domain/traceability.ts`) is an ad-hoc in-memory graph, NOT folded from events / NOT rebuildable; **Compatibility view is named in the RPH-PER-007 property text but NOT implemented**; no persisted projection table/checkpoint (all in-memory/on-read). |
| **2-007 Restart recovery & reconciliation** | **ABSENT** | Host `newEngine()` (`workbench.ts:74-86`) passes no filename ⇒ `:memory:` (`sql-driver.ts:28`); nothing on disk to recover. No startup routine, no PENDING-outbox re-drive on boot, no durable-wait replay, no external-op reconciliation ledger. `recover`/`reconcile`/`resume` grep → comments only. Runtime loads aggregates from the SNAPSHOT (`loadObject`), not event replay (replay is used for projection-rebuild + conformance only). |

## Consequence for the W2 roadmap

The durable-runtime **foundation substantially exists and is conformance-tested** (RPH-PER-006 aggregate-replay equivalence, RPH-PER-007 projection rebuild). The wave exit criteria already met: event+outbox atomicity, idempotency, concurrency-safety, Work/Assurance projection rebuild. The genuine W2 gaps — a far smaller wave than "build persistence" implies, the same honest-current-state pattern W0/W1 established — are:

1. **Durability proof + migration baseline (WP-2-001/002):** file-backed persist→reopen→identical-canonical-state (the "reference fixture persists and replays to the same canonical state" exit criterion) is untested end-to-end because the host runs `:memory:`; and there is no schema-version guard. **GENUINE — INC-1.**
2. **Restart / outbox recovery (WP-2-007):** re-drive PENDING outbox on engine open, idempotently (no duplicate delivery). The whole of WP-2-007's exit criterion "restart recovery avoids duplicate external side effects" is unenforced. **GENUINE — INC-2.**
3. **Compatibility + rebuildable Traceability projections (WP-2-006):** the two projections named in the exit criterion ("Work, Execution, Assurance, Traceability, and compatibility projections can be rebuilt") that are absent/not-rebuildable. Traceability also lands the W1-deferred typed trace-link plane (DEF-W1-002). **GENUINE — INC-3.**

DEFERRED-with-reason (not W2 blockers): artifact/evidence content-hashing + §18.3 supersession + access control (WP-2-005) → the ArtifactStore/CapabilityAuthorizer ports land with W3/W10 security (access control is auth-gated, C2); `expectedRevision`-required-on-every-update migration (WP-2-002) → a cross-cutting call-site sweep, low risk today (honored when sent); a durable cross-process outbox relay/broker (WP-2-003) → not needed until multi-process deployment (W10).
