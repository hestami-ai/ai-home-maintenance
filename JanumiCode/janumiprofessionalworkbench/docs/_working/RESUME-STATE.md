# JPWB Build — RESUME STATE (self-continuation checkpoint)

> **Purpose.** If context is compacted/summarized, re-read this file + `git log --oneline` + the TodoWrite
> list + the tracker (`docs/JPWB Implementation Roadmap and Tracker.md`) to resume losslessly. Updated at
> every milestone boundary. **This is the single source of "where am I and what's next."**

> **LATEST — 2026-07-12: Charter vocabulary remediation COMPLETE (post-M14 housekeeping).** All generated
> artifacts aligned to the Canonical Vocabulary Charter (RPH-DOC-000): package `rph-product-lens` →
> `rph-product-realization-pwa`; JPW→JPWB (+ both `JPW …` docs renamed); build-tooling "workflow"→"pass"; stale
> spec refs repointed. **Structural fix:** the pure `graph-view.ts` View seam moved `rph-engine` →
> `rph-projections`, so `apps/rph-demo` now builds off the pure seam (no `better-sqlite3`/`node:crypto` in the
> browser bundle). **Then sponsor-reviewed design fixes applied: C1** — moved the hardcoded Undertaking *instance*
> to `apps/rph-demo` (demo seed data); `rph-projections` keeps the reusable seam (`pwuGraphNode` + types, INV-5
> tested). **C2** — `createEngine({ ontology })` now REQUIRES an injected PWA; `rph-engine` imports no concrete
> PWA (`product-realization-pwa` is now a devDep). **C4** — DONE (Option 1): `hash.js` out of the contracts
> barrel, exposed as `@janumipwb/rph-contracts/hash`; barrel now browser-safe (verified). command-bus repointed.
> **All gates green incl. `bun run build` + `rph-demo`; 338 tests** (was 340; C1 traded 5 instance assertions for
> 3 stronger INV-5 tests). **Working tree NOT yet committed** — awaiting sponsor go-ahead.

## Mandate (from sponsor, 2026-07-10, before retiring for the night)
- Proceed autonomously through **all milestones** (M0→M13, then M14 UI). **MP (platform port) is OUT** (needs the `janumi/` monorepo; post-0.1.x).
- **100% correct, 100% complete. No shortcuts.** Verify each milestone before moving on; commit per milestone (local only, **no push**).
- **No GPU/Ollama** — a separate run holds it for hours. Author any live-LLM tests as `*.live.test.ts`, exclude from default/CI, leave a `run-live` script. The **engine core calls no LLM** (validators/executors are ports; conformance uses deterministic doubles), so live-model tests are not needed for M0–M13 correctness.
- Sponsor away for several hours. Log questions in `docs/_working/OPEN-QUESTIONS.md` instead of blocking.

## Key locked decisions
- D1 Home/role: JPWB **is** the engine; incubate here (`@janumipwb/rph-*`), port to `janumi/products/janumipwb/` later (MP).
- D2: **greenfield** (no migration apparatus). D3: **broader first slice** (include execution). D4: **Zod-as-source → JSON Schema**.
- Engineering Constitution (tracker §11) = my build standard (comments/observability/tests/Sonar), with library interpretations.

## CURRENT POSITION
- **ALL 14 MILESTONES (M0–M14) HAVE A DELIVERED, GATE-GREEN INCREMENT.** 13 packages, 340 tests, all gates green (check-types 17/17, lint, dependency-cruiser boundary 81 modules, prettier). M9–M12 were each adversarially reviewed (23 confirmed defects fixed). M13/M14 cores are built + tested (M13 replay not yet adversarially reviewed).
  - **M14 CORE COMPLETE (`a0d46b1`).** `rph-engine/src/graph-view.ts` `buildReferenceUndertakingGraph()` (tested, 5 tests) + `apps/rph-demo` SvelteKit + Svelte Flow surface over the engine seam (`npm install && npm run dev`; outside the root gate; not build-verified headless).
  - **REMAINING DEPTH (documented, none blocking — for a future session / after sponsor review):**
    1. **M13 live-command-drive replay** — command-bus handler-registry refactor + ~20 M9/M10/M11 command handlers (each calling its rph-domain kernel guard) + 8 missing commands (RuntimeBinding etc.) via m3 vocab+gen + execution sub-type tightening. Then drive all 72 commands (not just replay the event history).
    2. **M13 adversarial review** (the one unreviewed milestone).
    3. **M12** builders/doubles + mutation-testing (stryker); close RPH-PER/E2E/FIX PARTIAL families with #1.
    4. **M8** the other 6 of 12 core assurance policies.
    5. **M14** build-verify the SvelteKit app in a browser env; wire real command-drive (approve/waive) once #1 lands.
  - **Sponsor questions** accumulated in `docs/_working/OPEN-QUESTIONS.md` (per-milestone; the RphError code→category map, ControlAction 3-way definition, retry attempts-vs-retries, the 4 M13-added events' payloads, etc.). None blocking.
  - (historical per-milestone notes follow):
  - **M13 CORE COMPLETE (`4fddc82`).** NEW package `@janumipwb/rph-engine`: `createEngine(deps)` composition facade (the single public seam) + composition smoke test. **Reference Undertaking replay**: `packages/rph-engine/fixtures/expected-events.jsonl` (authored 72-step §26 trace) + `replay.ts` (seed/replay/conformance). Conformance asserts RPH-FIX-001..006 + P1/P5/P6 over the real trace — the intent→Architecture-Baseline loop proven, exec≠assurance, Behavior-PWU-satisfied-not-baselined, offline residual explicit. Closed RPH-FIX-002 by adding 4 events to the m3 vocab (98→102; registry 242). 335 tests green. `vocab/m13-replay.json` = grounding.
  - **M13 DEFERRED depth (a real follow-on, not blocking M14):** the LIVE-command-drive replay — refactor `rph-application/command-bus.ts` from the hardcoded CaptureIntent branch to a `Map<commandType,handler>` registry, then implement the ~20 deferred M9/M10/M11 command handlers (each calls its rph-domain kernel guard: ActivateExecutionPlan→canActivatePlan, PromoteBaseline→canPromoteBaseline, ApproveDecision→authorizeDecisionEffective, etc.) + add the 8 missing commands (BeginIntentDiscovery, StartAssuranceAssessment, MarkPwuSatisfied, RequestRuntimeBinding, AuthorizeRuntimeBinding, SubmitBaselineForReview, ApproveBaseline, + RuntimeBinding Deny/Revoke) via the m3 vocab+gen + BINDINGS, + tighten the execution placeholder sub-types (RetryPolicy/InputBinding/etc.). Also: the M13 replay conformance has NOT been adversarially reviewed (the only unreviewed milestone).
  - **M14 plan:** SvelteKit + Svelte Flow (`@xyflow/svelte`) demo UI — a CLIENT surface consuming ONLY the rph-engine seam (the engine NEVER renders). Use `sv create` (official CLI, per NOTA BENE). It: instantiates `createEngine()`, seeds/loads the Reference Undertaking, and visualizes the PWU decomposition graph (Svelte Flow nodes = PWUs, edges = decomposition/trace) with each node's 4-axis state (workLifecycle/execution/assurance/shapeIntegrity) + no-green-without-assurance coloring + the open offline residual; human-feedback affordances (approve/waive) as demonstration. Scaffold under `packages/` or `apps/` (workspaces glob already includes `apps/*`). Node/Vite; Svelte Flow is a client dep. This is a demonstration surface, not engine logic — GPU/LLM not involved.
  - (historical M12/earlier follow):
  - **M12 CORE COMPLETE (`e299fdf` core, `5be2d06` review fixes).** rph-domain: `properties.test.ts` (P1–P8 via fast-check, both-branch-exercising after review), `presentation.ts` (P8), `conformance-manifest.ts` + `conformance.test.ts` (125-rule accounting gate: fails CI on any unaccounted rule; honest COVERED/PARTIAL/DEFERRED + cited-file-exists check). Adversarial-reviewed (2 lenses/21 agents → 15 fixed, 4 refuted). 327 tests green. `vocab/m12-conformance.json` = grounding (7 layers, 125 rules, P1–P8, 9 mutations).
  - **M13 plan (the headline end-to-end proof + where all deferred wiring lands):**
    1. **Build `rph-engine`** — the composition facade `createEngine(ports, ontology)` (§4 table; does not exist yet). Wires rph-contracts + rph-persistence (SqliteStorageAdapter) + rph-application (command-bus Engine) + rph-projections + the rph-domain kernels + rph-product-realization-pwa ontology into one seam. This is M13's prerequisite AND M14's public seam.
    2. **Handler-registry refactor** (rph-application command-bus) — replace the hardcoded CaptureIntent-only branch with a registry keyed by commandType. Then implement the DEFERRED command handlers (M9/M10/M11): execution (ProposeExecutionPlan/ApproveExecutionPlan/ActivateExecutionPlan/StartExecutionStep/CompleteExecutionStep/FailExecutionStep/RetryExecutionStep), RuntimeBinding (Request/Authorize/Deny/Revoke — need new COMMANDS+BINDINGS via the messages vocab+gen), governance/baseline (Propose/Approve/Revoke decision, Create/Submit/Approve/Promote/Supersede baseline, waiver), + the M9/M10 lifecycle. Handlers call the M9–M11 domain kernels for their guards.
    3. **Tighten execution placeholder sub-types** (RetryPolicy/InputBinding/OutputBinding/Condition/etc.) via the m1 vocab + gen (deferred from M11).
    4. **Replay harness** — seed/replay/conformance modes over the fixture's expected-events trace (the 72-step §26 trace): RPH-FIX-001..006 (reference integrity, schema validity, expected-events.jsonl replay → approved Intent Baseline + satisfied Behavior PWU + authoritative Architecture Baseline + open offline residual), RPH-PER-006 (aggregate replay equivalence), RPH-E2E-* end-to-end, restart-reconciliation (RPH-E2E-006). Closes the M12 PER/E2E/FIX PARTIAL/DEFERRED families.
    Fixture data: the reference undertaking doc's §26 event trace + §3 object index (grounding passes already surfaced dec_fsm_arch_001, base_fsm_arch_001, plan_fsm_arch_001, the event seqs). May need an expected-events.jsonl fixture file. GROUND FIRST via a pass: the fixture's full §26 trace + §23 fixture-conformance rules + the message/command catalog + the exact createEngine/rph-engine shape from §4/DoD §7.
  - **M14 (last):** SvelteKit + Svelte Flow demo UI over the rph-engine seam (built in M13). `sv create`. Reads projections, visualizes PWU graph + 4 axes.
  - (historical M11/earlier follow):
  - **M12 core COMMITTED (`e299fdf`).** `properties.test.ts` (P1–P8 via fast-check@4.9.0, 300 runs each), `presentation.ts` (P8 predicate — the one previously-uncovered property), `conformance-manifest.ts` + `conformance.test.ts` (loads the 125-rule catalog from `vocab/m12-conformance.json`, gates on any UNACCOUNTED rule — the "CI fails on any invariant violation" goal). 325 tests; gates green. fast-check added as devDep to rph-domain. Numbered domain families COVERED; RPH-PER PARTIAL; RPH-E2E/FIX/CMP DEFERRED→M13.
  - **M12 review RUNNING:** `m12-adversarial-review` (runId `wf_12fa9a9d-77c`) — 2 lenses (property-test rigor/tautology; manifest coverage-honesty/over-claim). ON COMPLETION: triage CONFIRMED → fix → re-gate → close M12 (tracker ✅ + progress) → **M13**.
  - **M12 deferred → M13/follow-up:** builders/doubles + mutation-testing (stryker, not installed) + closing the RPH-PER/E2E/FIX families via the replay harness. The `rph-conformance`/`rph-engine` packages (§4 table) are NOT created — M12 lives in rph-domain to avoid scaffolding the not-yet-existent rph-engine composition facade (that facade is an M13 prerequisite for the replay).
  - (historical M11/earlier follow):
  - **M11 COMPLETE (`8f4c92d` kernel, `75cb61b` review fixes).** `rph-domain/src/execution.ts`: plan/step/binding lifecycle guards, retry cap, exec≠assurance (P1), restart reconciliation + idempotency (RPH-PER-012/002, RPH-EXE-007). Adversarial-reviewed (4 lenses/17 agents → 3 fixed [unsafe conflict-classification, attempt-idempotency, skip-mandatory], 9 refuted). 311 tests green. `vocab/m11-execution.json` = grounding.
  - **M12 plan:** Executable conformance suite (7 layers) + property tests P1–P8. The 7 conformance layers (per DOC-006 / the Conformance Spec) likely = (1) schema/contract validation, (2) state-machine legality, (3) invariant predicates, (4) command→event→state (bindings), (5) projection/read-model, (6) property-based, (7) end-to-end fixture replay. MUCH is already covered by per-milestone kernel tests (311 tests across contracts/domain/persistence/projections/assurance/application). So M12 = (a) a NEW `rph-conformance` package (or suite) that ORGANIZES the RPH-* tests into the 7 layers + a coverage manifest mapping every RPH-* id → its test, surfacing gaps; (b) PROPERTY tests P1–P8 via fast-check (add dep): P1 exec≠assurance, P2 obligation-conservation, P3 constraint-non-drop, P4 evidence-invalidation, P5 version-binding, P6 idempotency, P7 baseline-immutability, P8 presentation-independence — several already have deterministic-exhaustive stand-ins (P2 in decomposition.test) to upgrade to generative; (c) fill any RPH-* not yet asserted. GROUND FIRST: a pass reading Conformance Spec §s (the 7-layer structure + the full RPH-* catalog + Property P1–P8 definitions) + inventory which RPH-* ids already have tests across all packages → `vocab/m12-conformance.json` + a gap list. NOTE property tests are pure (no GPU/LLM) — safe to run.
  - **M13 (after M12):** Reference Undertaking (FSM) replay — seed/replay/conformance modes. THIS pulls in the M11/M9/M10-deferred handler-registry + command handlers + sub-type tightening (the replay drives the full command pipeline over the fixture's expected-events trace: RPH-FIX-003 → approved Intent Baseline, satisfied Behavior PWU, authoritative Architecture Baseline). Headline end-to-end proof.
  - **M14 (last):** SvelteKit + Svelte Flow demonstration UI — a CLIENT surface consuming the engine's public seam (engine NEVER renders). Use `sv create` (official CLI). Reads projections (Work-view etc.), visualizes the PWU graph + 4-axis state, human-feedback affordances.
  - (historical M10/earlier notes follow):
  - **M11 kernel COMMITTED (`8f4c92d`).** `rph-domain/src/execution.ts`: plan lifecycle (RPH-EXE-001/002, RPH-PWU-010), runtime binding (RPH-EXE-003/004), step (RPH-EXE-005/006/009), retry cap (RPH-EXE-008), exec≠assurance (Property P1 / RPH-PWU-005/007), restart reconciliation + idempotency (RPH-PER-012/002 / P6). Grounded via 3-reader pass → `vocab/m11-execution.json`. Reuses existing ExecutionPlan/ExecutionStep/RuntimeBinding schemas + machines (RUNNING reachable from QUEUED/WAITING not READY). 18 tests; 308 total; gates green.
  - **M11 review RUNNING:** `m11-adversarial-review` (runId `wf_e5623676-f47`) — 4 lenses. ON COMPLETION: triage CONFIRMED → fix → re-gate → follow-up commit → close M11 (tracker ✅ + progress) → **M12** (Executable conformance suite — 7 layers + property tests P1–P8). M12 likely a new `rph-conformance` package (or a suite in an existing one) that runs the RPH-* tests as an executable, layered harness + fast-check property tests; much is already covered by the per-milestone kernel tests, so M12 = organize + property-ize + fill gaps. Then **M13** (Reference Undertaking replay — needs the DEFERRED handler-registry + command handlers) and **M14** (SvelteKit + Svelte Flow demo UI, a client surface).
  - **M11 deferred → M13:** the Engine handler-registry (replacing the hardcoded CaptureIntent branch), execution + RuntimeBinding command handlers, and tightening the execution placeholder sub-types (RetryPolicy/InputBinding/OutputBinding/Condition/etc.). These are exercised end-to-end by the M13 Reference Undertaking replay and need the dispatch refactor.
  - (historical M10/earlier notes follow):
  - **M10 COMPLETE (`8bb3e05` kernel, `5d63799` review fixes).** `rph-domain/src/governance.ts`: authority verification (RPH-GOV-001/002), version-binding (RPH-GOV-003/P5), waiver scope+expiry+override-preserves-findings (RPH-GOV-004/005/006), baseline promotion gate (RPH-BAS-001..004 + §15.2 + no-green-without-assurance), immutability/supersession (P7/RPH-BAS-005/007), revocation impact (RPH-GOV-007), controller action selection (§37 normalize + decisive-no-fabricated-order select + INV-5 ruling). Adversarial-reviewed (4 lenses/22 agents → 2 fixed [invented precedence, RPH-GOV-004], 16 refuted). 290 tests green. `vocab/m10-governance.json` is the grounding.
  - **M11 plan:** Execution model, runtime bindings & restart recovery. Build (likely rph-domain execution.ts + possibly rph-application handlers): (a) **execution kernel** — ExecutionPlan/ExecutionStep/RuntimeBinding lifecycle predicates (machines already exist in transitions.data.ts: ExecutionPlan.status, ExecutionStep.stepState, RuntimeBinding.authorizationStatus); retry cap (RPH-EXE-008: no 4th retry after 3 failures); binding authorization gate (bindings authorized before execute, ties controller step 4); the INV-5 exec≠assurance boundary (ExecutionStepSucceeded MUST NOT yield AssuranceState=SATISFIED — §35.2). (b) **restart recovery** — deterministic rebuild of in-flight execution state from the event log / outbox (idempotent replay, no duplicate effects — ties the M4 idempotency + Persistence §33 "duplicate command replay does not create duplicate decisions/baselines"); resume a stalled step. (c) **command+BINDINGS wiring** deferred from M9/M10 (governance/baseline/obligation/constraint/assumption lifecycle commands + handlers in rph-application) — M11 is where handlers live. Exit: RPH-EXE-001..00N, restart-recovery conformance, Property P6/P8 (execution/idempotency). GROUND FIRST via a pass: Canonical Domain Model execution §s (§20 execution plan, §21 controller/runtime, §35.2 exec≠assurance) + Conformance RPH-EXE-* + Persistence restart/dual-run/replay §s + fixture execution event trace → `vocab/m11-execution.json`; inventory existing ExecutionPlan/ExecutionStep/RuntimeBinding schemas + machines + events + the M4 command-bus/outbox.
  - **M10 deferred:** baseline lifecycle commands+BINDINGS (SubmitBaselineForReview/ApproveBaseline/RevokeBaseline/ExpireWaiver) → M11/M13.
  - (historical M10-in-progress + earlier notes follow):
  - **M10 kernel COMMITTED (`8bb3e05`).** `rph-domain/src/governance.ts`: authority verification (RPH-GOV-001/002), version-binding (RPH-GOV-003/P5), waiver scope+expiry (RPH-GOV-005/006, RPH-CNS-004), baseline promotion gate (RPH-BAS-001..004+§15.2+no-green-without-assurance), immutability/supersession (P7/RPH-BAS-005/007), revocation impact (RPH-GOV-007), controller control-action selection (§37 normalize + strictest-unresolved select + INV-5 satisfied-ruling). Grounded via 3-reader pass → `vocab/m10-governance.json`. Reuses existing DecisionObjectSchema/BaselineObjectSchema + Decision.status/Baseline.status machines. 18 tests; 289 total; gates green.
  - **M10 review RUNNING:** `m10-adversarial-review` (runId `wf_a719ec34-1a1`) — 4 lenses (authority/version, promotion-gate, immutability/waiver, controller/fidelity). ON COMPLETION: triage CONFIRMED → fix → re-gate → follow-up commit → close M10 (tracker ✅ + progress) → **M11** (Execution model, runtime bindings & restart recovery; ExecutionPlan/ExecutionStep/RuntimeBinding machines exist; RPH-EXE-*, restart recovery from event log, retry cap RPH-EXE-008, the deferred command+BINDINGS wiring incl. governance/baseline commands). Ground M11 from Domain Model execution §s + Conformance RPH-EXE + Persistence restart/replay.
  - **M10 deferred:** baseline lifecycle commands+BINDINGS (SubmitBaselineForReview/ApproveBaseline/RevokeBaseline/ExpireWaiver) — events+machines exist; commands need M11/M13 handlers.
- **(prior) M10 NEXT plan** (superseded by the above):
  - **M9 COMPLETE (`8ab509b` kernel, `aa7ae7c` review fixes).** `rph-domain/src/decomposition.ts`: obligation conservation (P2), constraint per-relevant-child 5-disposition non-drop (P3), recomposition conflict resolution (RPH-DEC-006), assumption reify-or-reject gate + lifecycle predicates (RPH-ASM-001..006), validateDecomposition + §13.2 gate. Adversarial-reviewed (5 lenses/24 agents → 3 fixed, 16 refuted). +6 tightened rph-contracts sub-schemas. Fixed upstream M2 slash-compound-transition defect (+25 edges/16 machines). 271 tests green.
  - **M10 plan:** Controller decision sequence + governance decisions + baseline promotion. Build: (a) **governance** — authority-verified decisions bound to a specific object *version* (a decision on v_n does not carry to v_{n+1}); waiver lifecycle (scope/rationale/duration/affected objects, §23.2) incl. the RPH-CNS-004 expired-waiver **baseline-promotion-block** half deferred from M9; assumption ACCEPTANCE as a governance act. (b) **ControlAction superset** + controller decision sequence. (c) **baseline** — immutable baseline gate (a baselined object is frozen; supersede via successor, never mutate — ties M6 cross-run supersession); promotion requires assurance SATISFIED (no green without assurance). (d) command+BINDINGS + application handlers for the M9-deferred lifecycle events (waiver/acceptance/obligation/constraint governance commands). Exit: RPH-GOV-001..007, RPH-BAS-001..007, Property P5/P7. Likely NEW package `rph-governance` (or extend rph-domain + rph-application). GROUND FIRST: launch a resolve pass reading the Canonical Domain Model governance/baseline sections (§23 governance, baseline §s) + Conformance Spec RPH-GOV/RPH-BAS + fixture governance decisions → `vocab/m10-governance.json`; check existing GovernanceDecision/Baseline/ControlAction/Authority schemas + machines already in rph-contracts/rph-domain (Decision.status, Baseline.status machines exist).
  - (historical) M9 IN PROGRESS notes:
  - **M9 grounding DONE:** `packages/rph-domain/vocab/m9-decomposition.json` written (obligation-conservation equation, RPH-DEC-001..007, RPH-CNS-001..004, RPH-ASM-001..006, Property P2/P3 §25, RPH-DEC-006 tenant-conflict scenario, 11 enums, 12 open items). NOTE: real ids are RPH-DEC-006/002 + Property P2/P3 — the earlier "CT-2/CT-4/PBT-2/PBT-3" names do NOT exist in the docs.
  - **M9 CODE COMMITTED (`8ab509b`).** Resolve pass done. Built: (A) tightened 6 placeholder sub-schemas in rph-contracts (gen-objects FORCE_FULL + inline z.enum); (B) `packages/rph-domain/src/decomposition.ts` kernel — validateObligationConservation (P2/RPH-DEC-002/007), validateConstraintPropagation (P3/RPH-CNS-001..004/RPH-DEC-003), evaluateRecomposition (RPH-DEC-005/006 §14.1), assumption predicates (RPH-ASM-001..006), validateDecomposition (composes + §13.2 gate) + 24 tests. **FIXED upstream M2 defect:** gen-transitions.expandFrom dropped slash-compound froms → 25 legal edges missing across 16 machines; now splits `/`, regenerated (+25 edges). 268 tests, all gates green. (C) commands/BINDINGS deferred to M10/M11.
  - **M9 adversarial-review pass RUNNING:** `m9-adversarial-review` (runId `wf_89cd5212-38d`) — 5 lenses (obligation/constraint/recomposition/assumption/fidelity-scope) audit the kernel vs vocab, each finding refuted-or-confirmed. ON COMPLETION: triage CONFIRMED findings → fix → re-gate → follow-up commit → set tracker M9→✅ + progress log → then **M10** (Controller, governance decisions & baseline promotion: waiver/acceptance commands+handlers, RPH-CNS-004 baseline-block half, baseline immutability/promotion, RphError code→category). Ground M10 from the Canonical Domain Model §governance/baseline + DOC re governance decisions.
  - **M8 COMPLETE (`dfb472e` code, `f95d88a` docs)** — Product Realization PWA ontology data package. (6 of 12 core policies seeded; rest later.)
- **M8 done:** NEW package `@janumipwb/rph-product-realization-pwa` — `ontology.data.ts` GENERATED from `vocab/m8-ontology.json` (14 PWU templates incl. PRODUCT_REALIZATION root; 6 seed policies fully specified; 3 conformance profiles LIGHTWEIGHT/STANDARD/HIGH_ASSURANCE; 11 compatibility phases). `ontology.ts` accessors + `validateOntology` (OVR: unique root, seed policies well-formed, policy refs resolve w/ _vN normalization = INV-11). 6 tests; 243 total. **Note:** 6 of the 12 canonical DOC-004 core policies are seeded; the other 6 (fitness_for_purpose/baseline_promotion/requirement_coverage/historical_consistency/constraint_propagation/test_adequacy) are referenced by templates but authored later. M8 extraction had 18 reconciler conflicts + 10 open items (in the vocab; reconciler-resolved).
- **M9 plan (in rph-domain — pure kernel):** decomposition/recomposition enforcement. Build: (a) **obligation-conservation** validator — for a DecompositionContract, mandatory parent obligations = allocated + retained + satisfied + authorized-waivers (no obligation silently disappears, property P2); constraint propagation with strength preservation (no mandatory constraint silently dropped, P3); explicit sibling deps; recomposition strategy required; coverage claims. Pre-child-execution gate (CT-4: decomposition coverage gate blocks child execution). (b) **RecompositionContract evaluation** — aggregation + conflict resolution (may be CONFLICTED though all children individually satisfied; TENANT_IDENTITY_MISMATCH→REJECT_RECOMPOSITION etc.). (c) **Assumption reification** — material assumption→first-class object linked to affected PWUs; ACCEPTED≠VERIFIED; falsification→FALSIFIED + impact (ties M6 invalidation). Exit: RPH-DEC-001..007 + P2/P3; RPH-CNS-001..004; RPH-ASM-001..006; DOC-006 CT-2/CT-4. GROUNDING: launch DOC-002 §13/§14 extraction (decomposition-contract fields + obligation-conservation equation + recomposition conflict rules + coverage-claim types) → `vocab/m9-decomposition.json`. Pure functions: `validateDecomposition(contract, parentObligations)`, `evaluateRecomposition(contract, childResults)`, `reifyAssumption`. This is where dynamic decomposition (D3 broader-slice) is enforced.
- **M7 done:** NEW package `@janumipwb/rph-assurance` — `applicability.ts` (code-free DSL evaluator: ALL/ANY/NOT/EQUALS/IN/CONTAINS/EXISTS/RISK_AT_LEAST) + `assurance-rules.ts` (`dispositionFromFindings` §10.3 ladder; `aggregateDisposition` strictest-unresolved-never-average §28.2; `evidenceAdmissibility` 8 conditions; `checkIndependence` multi-dimensional; `isWaiverApplicable` exact-version-binding; `classifyValidatorResult` VALIDATOR_FAILED/BOUNDARY_REJECTED≠disposition-REJECTED). 26 tests; 237 total. Vocab `docs/_working/m7-assurance.json`. **Scope:** pure rule library is the M7 core; PolicyRegistry + AssuranceService orchestration (rules→persisted assessment via command bus + M2 AssuranceAssessment FSM) integrates in M10/M13; seed policies in M8.
- **M8 plan (NEW package `@janumipwb/rph-product-realization-pwa`, depends on contracts):** the Product Realization PWA ontology as VERSIONED DATA — PWU templates (Product Realization root + standard child PWUs, each: purpose/inputs/outputs/completion-claims/required-evidence/candidate-children/default-policies), artifact templates, the ~6 FSM-fixture seed AssurancePolicyDefinitions fully authored (Intent Fidelity, Intent Completeness, Assumption Disclosure, Decomposition Coverage, Architecture Coverage, Intent Preservation — each with criteria/findings/dispositions/independence, using the M7 ApplicabilityExpression + DispositionRule shapes), decomposition/recomposition rules, role defaults, 3 conformance profiles (Lightweight/Standard/High-Assurance → risk→mandatory-policy applicability), the legacy INTAKE..COMMIT compatibility-phase mapping. Reconcile the ~90-per-PWU vs 12-core policy naming onto ONE registry (DOC-004's 12 core = canonical, DOC-003 names = aliases). Exit: DOC-003 OVR-1..10 + §46; assessments always cite a policy version (INV-11). GROUNDING: launch DOC-003 extraction (PWU templates + the 6 seed policies' criteria/findings + conformance profiles + compatibility mapping) → `vocab/m8-ontology.json`. The Finding.* enums (12, skipped in M0 enum gen) live here as policy finding vocabularies.
- **M6 done:** `rph-domain/traceability.ts` — `TRACE_DIRECTIONALITY` (17 relations → source/target types, §25.1) + `validateLinkDirectionality`; `TraceGraph` (immutable links, directionality-validated addLink, outgoing/incoming, findPath BFS, superseded excluded); `ImpactClassification` (7) + `classifyEvidenceInvalidation` (CT-10/P4: evidence→claims REVALIDATION) + `impactedObjects` (conservative downstream). Vocab `rph-domain/vocab/m6-traceability.json`. 9 tests; 211 total.
- **M7 plan (NEW package `@janumipwb/rph-assurance`, depends on contracts+domain+ports):** the governed assurance layer where validators only RECOMMEND and the Assurance Service alone sets authoritative dispositions. Build: (a) the **ApplicabilityExpression DSL evaluator** (the code-free op set ALL/ANY/NOT/EQUALS/IN/CONTAINS/EXISTS/RISK_AT_LEAST from DOC-007 §18 — deferred from M0; author the AST schema in rph-contracts + evaluator in rph-assurance); (b) **PolicyRegistry** (versioned, DRAFT/ACTIVE/SUSPENDED/SUPERSEDED; only ACTIVE backs assessments); (c) **ValidatorContract** + registry + independence check (via an injected IdentityProvider port — add to rph-ports; risk→independence tier matrix); (d) **AssuranceService.evaluate** — evidence admissibility (8 conditions), criterion results (MET/PARTIALLY_MET/NOT_MET/NOT_APPLICABLE/UNABLE_TO_DETERMINE with 'unable'≠'met'), **strictest-unresolved aggregate disposition** (never numeric average), ValidatorResult boundary rejection (VALIDATOR_FAILED ≠ rejection), waivers (scope+version bound). Exit: DOC-004 Tests 1–12, INV-1..20, RPH-ASR-001..012, RPH-EVD-001..007; validator SATISFIED-with-blocking-criterion → REJECTED (Test 3); INDEPENDENCE_VIOLATION (Test 1); invalidated evidence cannot support a satisfied claim. GROUNDING: launch DOC-004 extraction of (validator contract fields, 8 admissibility conditions, disposition precedence rules, independence tiers, the ApplicabilityExpression/PolicyExpression grammar, waiver rules) → `vocab/m7-assurance.json`. The AssuranceAssessment.state machine already exists in M2 (rph-domain). The 6 FSM-fixture seed policies are authored in M8 (ontology).
- **M5 core done:** NEW package `@janumipwb/rph-projections` — `projector.ts` (`Projector<V>` pure fold; `rebuildProjection` deterministic rebuild-from-empty = RPH-PER-007; `IncrementalProjection` idempotent-by-eventId + checkpoint, incremental==rebuild); `work-projection.ts` (Work view: 4 PWU axes distinct + open-observation counts; **`isQualifiedSuccess`** = no-green-without-assurance green-node rule; folds IntentCaptured + PwuProposed, extensible). 6 tests. 202 total. **Remaining M5 views (Execution/Assurance/Traceability/Change-Impact/Compatibility) are framework-ready** — add a projector per view as M6/M7/M11 produce their events; the Compatibility (legacy INTAKE..COMMIT) view is a single derived projector over event types.
- **M6 plan (traceability + impact/invalidation):** where does it live? A new package `@janumipwb/rph-traceability` OR fold into rph-domain/rph-application. The TraceLink object exists (M1). Build: TraceLink relation vocabulary (18 types) with directionality rules; immutable links (corrections = superseding links); intent→baseline path query + mandatory-constraint-propagation query; invalidation triggers with typed-graph traversal + per-object classification (unaffected/review/evidence-refresh/revalidation/replanning/reshaping/invalidated); evidence-invalidation → dependent claims contested + baseline-readiness recompute. Exit: RPH-TRC-001..005, DOC-006 CT-3/CT-10, property P4. GROUNDING: launch an extraction of the TraceRelation directionality rules + invalidation-trigger rules from DOC-002 (§25 trace + §29 invalidation) → `vocab/m6-traceability.json`. Traceability is a pure graph over trace links + objects → could live in rph-domain (pure) or its own package; a Traceability PROJECTION (read view) lives in rph-projections.
- **M4 done:** two new packages. `@janumipwb/rph-persistence`: `SqlDriver` seam (`sql-driver.ts`, driver = **better-sqlite3** — works in vitest Node workers + Node hosts; Bun is only the task runner and can't dlopen it, irrelevant to the engine — see OPEN-QUESTIONS §C), `schema.ts` (SQLite DDL: professional_work_objects registry + append-only domain_events [contiguous aggregate_revision, AUTOINCREMENT global_sequence] + outbox_messages [same-tx] + command_receipts + object_versions), `SqliteStorageAdapter` implements the `StorageAdapter` port (NEW in rph-ports/ports/storage.ts): atomic `commit`, optimistic concurrency (REVISION_CONFLICT), idempotency, outbox drain, replay. `@janumipwb/rph-application`: `Engine.dispatch` pipeline (idempotency→validate payload→produce state+event→validate produced state fail-loud→commit atomic→map to CommandResult) + `drainOutbox`. M4 handler = CaptureIntent. **Walking skeleton e2e proven** (CaptureIntent→IntentCaptured→persist→outbox→projection; DUPLICATE on replay; CONFLICT on 2nd create; VALIDATION_FAILED on bad payload). 196 tests total.
- **M5 plan (NEW package `@janumipwb/rph-projections`, depends on contracts+ports+domain):** rebuildable read-model projectors built from the event stream (`StorageAdapter.readAllEvents()`). Views: Work (PWO graph: nodes with the 4 state axes distinct + open-observation counts), Execution, Assurance, Traceability, Change-Impact, and the DERIVED non-authoritative Compatibility (legacy INTAKE..COMMIT phase) projection via a SINGLE compatibility-derivation handler. Each projector: idempotent, per-aggregate-ordered, `projection_checkpoints` (last_global_sequence, handler_version), full rebuild-from-empty. **Green-node gating data**: no unqualified success indicator when executionState=SUCCEEDED but assuranceState incomplete (surface obligation expressed as projection data). For M5, build the Work projection fully (it's what the walking skeleton feeds) + the projection framework (checkpoints, rebuild) + stubs for the others, OR build all minimally. Exit: RPH-PER-007 (rebuild from empty reproduces fixture projections), RPH-PER-009 (projection lag never affects canonical validation), RPH-PRJ-001..005, RPH-CMP-001..004. Projections NEVER a write target; commands never validate against projections.
- **M5 conventions:** projection state can live in its own SQLite tables (via StorageAdapter or a separate projection store) OR in-memory rebuilt from events. Given the walking skeleton already has an in-memory projection, formalize a `WorkProjection` projector class that folds events → view; add a projection store for checkpoints. Keep projectors pure(ish) — event in, view-delta out. The engine's drainOutbox already delivers events; projections can subscribe there OR rebuild from readAllEvents.
- **M3 done:** `rph-contracts/src/messages.ts` GENERATED (`gen/gen-messages.ts`) from `vocab/m3-commands-events.json` (43 commands, 98 events, 43 bindings, 14 first-slice). Payload schema per command/event; registries `COMMANDS`/`EVENTS`/`FIRST_SLICE_COMMANDS`/`BINDINGS` (binding eventType = command emitsEvent). Registry now 238 schemas. Tests: RPH-CON-008 (ExecutionStepSucceeded payload has no assurance field); NEW `rph-domain/binding.test.ts` cross-checks every first-slice binding transition is legal in the M2 machines (integration guard). 185 tests. Open items: PwuMarkedReady→PwuStateChanged alias, CancelExecutionPlan ACTIVE→CANCELLED gap (→M11), 3 unschematized events.
- **M4 plan (NEW package `@janumipwb/rph-persistence` + `@janumipwb/rph-application`):** realize DOC-009's durable command pipeline on synchronous better-sqlite3. **Walking skeleton = CaptureIntent → IntentCaptured → persist → outbox → projection, end-to-end.** Design (from architecture synthesis, re-realizing the Postgres-shaped spec on SQLite):
  - **rph-persistence** (better-sqlite3 StorageAdapter; depends on rph-contracts + rph-ports): SQLite schema = `professional_work_objects` registry (id PK, objectType, revision, semanticVersion, current JSON) + `domain_events` (append-only; unique event_id; uq(aggregateType,aggregateId,aggregateRevision); monotonic global_sequence via INTEGER PK AUTOINCREMENT) + `outbox_messages` (committed in SAME tx as events; PENDING/PUBLISHED) + `command_receipts` (idempotency: commandId/idempotencyKey → prior result) + `object_versions` (history). WAL mode. Everything behind a dialect-neutral `StorageAdapter` port (in rph-ports) so a Postgres adapter slots in later. Optimistic concurrency: UPDATE ... WHERE revision=:expected → 0 rows → RPH_REVISION_CONFLICT. Atomicity: whole pipeline in one `db.transaction()`.
  - **rph-application** (command bus; depends on domain+persistence+contracts+ports): the fixed 10-step pipeline (authenticate→authorize→load→check expectedRevision→validate payload→enforce invariants/transition→produce event→persist events+outbox atomically→update projection→dispatch). For M4, one handler: CaptureIntent. Idempotency via command_receipts (duplicate idempotencyKey → prior result, no new event). In-process synchronous outbox drain → projection.
  - Exit: RPH-PER-001..010 (revision conflict; idempotent no-new-events; contiguous revisions; event+outbox atomic w/ rollback; aggregate replay == materialized state). Test with `:memory:` sqlite. Ports `StorageAdapter`/`EventSink`/`Clock`/`IdGenerator` go in rph-ports (IdGenerator/Clock defaults there too).
- **M4 conventions:** scaffold rph-persistence + rph-application like rph-domain (source export condition, tsconfig+build). better-sqlite3 ^12.8 already a dep candidate — add to rph-persistence package.json. Keep ALL SQL inside rph-persistence behind StorageAdapter. dependency-cruiser already forbids domain→persistence (domain-purity) — application may depend on both.
- **M2 done:** `rph-domain` package built. `transitions.data.ts` GENERATED (`gen/gen-transitions.ts`) from `vocab/m2-transitions.json` (23 state machines). Engine = `stateMachine.ts` (classifyTransition / canTransition / assertTransition → fail-loud RPH_ILLEGAL_STATE_TRANSITION) + `pwuGuards.ts` (canAdvanceWorkLifecycle w/ cross-axis guards; **property P1** enforced: UNDER_ASSURANCE→SATISFIED requires assuranceState=SATISFIED; EXECUTING→SATISFIED illegal; satisfiesP1). Codegen transforms: umbrella from-states expanded, cross-axis rules lifted to CROSS_AXIS_RULES, self-loop illegals dropped, guarded-legal edges (illegal-note-on-a-legal-edge = M7 guard) → `guarded`. 36 tests. Total 179 tests green.
- **M3 plan (in rph-contracts):** author the first-slice command + event PAYLOAD schemas (the ~14 commands: CaptureIntent, FormalizeIntent, ApproveIntent, ProposePwu, MarkPwuReady, ProposeExecutionPlan, ActivateExecutionPlan, CompleteExecutionStep, ProposeEvidence, AdmitEvidence, RequestAssuranceAssessment, CompleteAssuranceAssessment, ApproveDecision, PromoteBaseline) + their events, each via `domainCommandSchema(payload)` / `domainEventSchema(payload)`. Build the **command↔event↔transition binding table** (which command emits which event drives which state-machine transition — resolves the DOC-002/007 naming drift, e.g. MarkPwuReady↔PwuMarkedReady). Exit: RPH-CON-005..008 (validator-output/policy-version/subject-version rejection; ExecutionStepSucceeded provably cannot set assuranceState=SATISFIED). Grounding: launch extraction of the command/event payload fields + binding rows from DOC-007 §32/§33 → `vocab/m3-commands-events.json`.
- **M3 conventions:** commands/events live in rph-contracts `src/commands.ts` + `src/events.ts` (or generated). Binding table = data (`src/binding.ts` or generated) mapping commandType→{eventType, machine, from→to}. Register new command/event schemas in buildContractRegistry + emit JSON schemas. The binding table feeds M4's command handlers.
- **M1 done:** `objects.ts` GENERATED (`gen/gen-objects.ts`) from `vocab/m1-object-fields.json` (grounded from DOC-002/007). 17 object schemas compose `objectEnvelopeShape`; 8 full helpers; **40 placeholder helpers** (specs reference-but-never-define them → `z.record` permissive, tighten in M7/M9/M11 — NOT fabricated). Registered in `buildContractRegistry` (97 schemas). Fidelity tests bind objects.ts → the extraction. 143 tests green. RPH-FIX-002 (fixtures) deferred → M13.
- **M2 plan:** new package `@janumipwb/rph-domain` (pure, I/O-free kernel; depends on rph-contracts only). Encode transition tables + guards for: Intent (§6.2), PWU 4 axes (workLifecycleState §8.1/8.2 primary+exception, executionState/assuranceState/shapeIntegrityState), Obligation/Constraint/Assumption/Claim/Evidence/DecompositionContract/RecompositionContract/AssurancePolicy/AssuranceAssessment/AssuranceObservation/ExecutionPlan/ExecutionStep/RuntimeBinding/Decision/Baseline lifecycles. Reject §8.3 illegal transitions (RPH_ILLEGAL_STATE_TRANSITION). **workLifecycleState = controller-computed ROLLUP** (§5 decision) — sub-axes are independently-commanded facts. Property P1: SUCCEEDED never implies SATISFIED. Exit: RPH-INT-001..007, RPH-PWU-001..010, §8.3 rejections. Grounding extraction: `wf` (transition tables) → will write `vocab/m2-transitions.json`.
- **M2 conventions:** rph-domain is a NEW package — scaffold like rph-ports (package.json with `source` export condition, tsconfig + tsconfig.build, add to dependency-cruiser domain-purity rule already present). Transition guards = pure functions `canTransition(axis, from, event) / transition(...)`; illegal → throw RphErrorException(RPH_ILLEGAL_STATE_TRANSITION) or return typed result.
- **Ratified vocabulary:** `packages/rph-contracts/vocab/canonical-vocabulary.json` (84 enums / 22 id prefixes / 16 error codes / 22 envelopes / DSL) is the machine source of truth. §5 ratify sheet = `docs/JPWB Reconciliation Ratify Sheet (M0).md` (14 conflicts resolved + 11 open items → OPEN-QUESTIONS.md).
- **D4 pipeline (confirmed + built):** zod 4.4.3 `z.strictObject` = runtime unknown-key rejection + JSON-Schema `additionalProperties:false`; `z.iso.datetime()` = RFC-3339. **Convention: `z.strictObject` for all objects; `z.iso.datetime()` for timestamps.**
- **rph-contracts built (M0):** `hash.ts` (canonical JSON + sha256), `enums.ts` (GENERATED from vocab via `src/gen/gen-enums.ts`; 72 enums; fidelity-tested), `ids.ts` (RphId `<prefix>_<ULID>` + registry), `errors.ts` (15 codes + category map), `common.ts` (version quartet, RfcTimestamp, ExtensionPayload), `envelopes.ts` (ObjectEnvelope + shape, ActorReference, ProvenanceRecord, DomainCommand/Event factories, CommandResult), `validate.ts` (SchemaRegistry + validateAgainst + jsonSchemaFor + buildContractRegistry), `gen/emit.ts` (→ `schemas/` 80 JSON Schemas committed). **120 tests pass** (114 contracts + 6 ports). Whole workspace green: build/check-types/lint/boundary/format.
- **rph-ports built (M0):** Logger port + Noop/Structured/console defaults (6 tests).
- **KEY CONVENTIONS for M1+:** concrete object schemas = `z.strictObject({ ...objectEnvelopeShape, ...fields })` (import `objectEnvelopeShape` from envelopes.ts); enum schemas come from `enums.ts` as `<Name>Schema`; regenerate enums with `bun run gen:enums`, all schemas with `bun run gen`; after any gen, run `bun run format`. Register new object schemas in `buildContractRegistry()` + add to `gen/emit.ts` OBJECTS list.
- **Immediately next (M1):** author the 17 object-type Zod schemas (Intent, PWU with 4 state axes + WorkRiskProfile, Obligation, Constraint, Assumption, Claim, Evidence, AssurancePolicy, AssuranceAssessment, AssuranceObservation, Decision, Artifact, DecompositionContract, RecompositionContract, ExecutionPlan, RuntimeBinding, Baseline, TraceLink) + the ~30 helper sub-schemas, each composing `objectEnvelopeShape`; validate DOC-006 fixture objects against them (RPH-FIX-002). Get envelope/field detail from vocab `envelopes` + the specific DOC-002 sections.

## How to resume mechanically
1. `cd e:/Projects/hestami-ai/JanumiCode/janumiprofessionalworkbench`
2. `git log --oneline` (per-milestone commits) — last commit tells you the last completed step.
3. Read this file's CURRENT POSITION + the TodoWrite list (one item `in_progress`).
4. Read tracker §6 (milestone table) + §5 (ratify sheet) for the spec of the active milestone.
5. Continue; verify (build + typecheck + tests + dependency-cruiser + Sonar) before marking a milestone done.

## Progress log (newest first)
- 2026-07-10 — M0 started: toolchain verified, Turborepo baseline scaffolded (scratchpad), heartbeat monitor + working docs created.
