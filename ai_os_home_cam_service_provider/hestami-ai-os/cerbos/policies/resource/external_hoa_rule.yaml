# External HOA Rule Resource Policy

apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "external_hoa_rule"
  importDerivedRoles:
    - common_roles
  rules:
    # Admins and managers: full access
    - actions: ["view", "create", "update", "delete"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - org_admin
        - org_manager

    # Concierge staff: can view, create, and update rules
    - actions: ["view", "create", "update"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - org_concierge

    # Property owners: can view rules
    - actions: ["view"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - org_owner
