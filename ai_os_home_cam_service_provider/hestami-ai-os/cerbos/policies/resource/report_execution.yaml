apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "report_execution"
  rules:
    - actions: ["view", "create", "edit"]
      effect: EFFECT_ALLOW
      roles:
        - admin
        - manager
        - staff
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles:
        - board_member
