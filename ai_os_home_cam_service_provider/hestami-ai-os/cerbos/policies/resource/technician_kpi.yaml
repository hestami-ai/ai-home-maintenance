apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  importDerivedRoles:
    - common_roles
  resource: technician_kpi
  rules:
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles:
        - admin
        - manager
        - vendor
