apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "job_attachment"
  importDerivedRoles:
    - common_roles
  rules:
    - actions: ["view", "create", "delete"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - org_admin
        - org_manager
    - actions: ["view", "create"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - org_vendor
        - org_technician
